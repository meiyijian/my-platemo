# 2026-09-21

> 详细项目日志在 `PlatEMO-master/.workbuddy/memory/2026-09-21.md`。

## 00:00 FE500 / M=20 七算法全量重新启动（14 路，用户要求"开最大进程"）

承接 09-20 23:52 的暂停。后台任务 `ofzUVI`，预计 **≈14 h**（约 14:00 结束）。

`ALGS="SSDE SAMOEATL2M CSEA PCSAEA REMO PACDIS HES_EA" MAXJOBS=14 THREADS=1 RUNS=1-20
CHUNK=10 ROUNDS=8 SLICE_TIMEOUT=14400 MAX_SLICE_FAILS=2 bash driver.sh`

- **14 路而不是 16**：16 逻辑核 / 31.3 GB 内存，启动时空闲仅 14.5 GB；历史上 12 路已把 CPU
  压到 ≈98%（有效并行度约 12），加到 16 只增内存压力。`MAXJOBS` 是环境变量，随时可加。
- **顺序便宜→贵**，中断点能换来最多"完整算法"；两个最贵的 REMO/PACDIS 在最后（竞争小、单跑更快）；
  **有卡死风险的 HES_EA 放最后**，不拖住其他六个。

### 新增护栏：确定性卡死 run 的"投毒"机制
种子固定（`22260912+1000×题号+run`）⇒ **卡死的 run 重驱必定再卡**，旧 driver 会在一个槽位上
反复烧满 `SLICE_TIMEOUT`（8 轮 × 4 h），扫描永不收敛。做法：driver 记每个切片的退出码
（`logs/<KEY>/rc_part<NN>.txt`，`<runs>|<rc>`）；`timeout` 杀进程返回 **124**；
同一片同一 run 列表被 124 杀 **≥2 次** → `mark_poison.py` 把**列表里第一个磁盘上没有 .mat 的 run**
写进 `logs/<KEY>/poison.txt`（切片按序执行且完成的 run 都原子落盘，所以第一个缺的就是卡死的那个，
它后面的只是没轮到）；`missing_runs.py` 据 `FE500_POISON` 跳过这些对；driver 收尾记 `poisoned=<n>`；
**验收脚本会把这些缺口报成 MISSING，不会被悄悄吞掉**。要求 2 次是为了不把"只是慢"误判成卡死。

### 启动前干跑校验（全部通过）
`bash -n driver.sh` 语法 OK；SSDE 出 32 片（runs 1–18，每片 9+9）、PACDIS 出 32 片；
`mark_poison.py` 能正确判"已投毒"；`missing_runs.py` 确认跳过被投毒的 run。

### 现场
00:00:33 driver 启动 → SSDE 复用 09-20 已落盘的 32 个文件（`present=32/320`），
32 片错峰启动（20 s/片，2→14 路爬坡）。数据 `D:\REMOandDREMO测试集\20目标\FE500\`。

### git
`eec5918`（实验框架 + harness `FESlack`）、`e5c9225`（记忆）已在本地。
**推送被拒 `ahead 2, behind 2`**，按红线没做 pull/merge。
本次的 `mark_poison.py` + driver/missing_runs 改动本轮提交。

---

## 00:05–00:15 用户反馈"CPU 利用率还是很低" → 定位并修掉启动节流 + 加内存闸

**诊断**：采样 6 次只有 **2–3 个 MATLAB** 在跑（上限 14），CPU ≈12–15%。日志显示切片启动
被卡在**固定每 22 s 一片**（`sleep 20`），而一个 SSDE 切片只要 ~25 s ⇒ 池子永远填不满。
`sleep 20` 本是为防"多进程同时**冷启动**导致 ntdll 堆损坏"，但对切片比它短的算法就成了纯吞吐限制。

**修复 1 —— 冷/热分段节流**：`LAUNCH_GAP=20` 只用在每次 pass 的**前 `COLD_STARTS=4` 次**启动，
之后用 `LAUNCH_GAP_WARM=5`。危险的是冷启动，不是稳态。效果：间隔 22 s → ~8 s，
SSDE 两分钟内 149 → **256/320**。

**修复 2 —— 内存闸**：实测每 worker 0.6 GB（SSDE）~1.2 GB（patternnet/dacefit 类）；
本机 31.3 GB 但 OS+应用已吃 ~16 GB，**基线空闲仅 16.8 GB**。
14 路跑 REMO/PACDIS/HES 时可能换页，**换页代价远大于多两个槽位**。
所以每次启动前查 `MIN_FREE_MB=3500`，不够就 `wait -n` 等一个切片结束
⇒ **池子自动按内存缩容，够时保持 14**，不用手调 MAXJOBS。

**定时巡检**（用户要求）：自动化 `852747ad-1eec-4afc-a421-6ca505c29ed6`，**每小时**，
有效到 **09-21 20:00**。prompt 写全了数据/日志路径、满额 2240、进程数与空闲内存、当前算法、
ETA、异常排查（`poison.txt` / `rc_part*.txt` 的 124 / `*.tmp.mat`），并且
"进程数为 0 且进度 < 2240 ⇒ driver 死了，报警但**不要自己重启**"。

**00:14 现场**：SSDE 256/320、SAMOEATL2M 5、HES_EA 1；MATLAB 8 并爬向 14；空闲内存 9.3 GB。

**操作教训**：Bash 单条命令要压在 120 s 内。我一条 `sleep 70` + 多次采样的命令被 SIGTERM 截断，
只剩第一行输出（glob 顺序里 `HES_EA` 排第一），差点误判成"SSDE/SAMOEATL2M 目录被删"。采样要短、分批。

---

## 00:35 🔴 重大 bug：`ALGS="... SAMOEATL2M ..."` 把整轮 driver 干掉了

任务 `r45VsU` 只跑了 11 分 37 秒就"完成"。日志末尾：
```
[00:34:47] ---------- SSDE done: present=320/320 ----------
unknown algorithm key 'SAMOEATL2M'
```
⇒ **SSDE 320/320 跑完，但后面 6 个算法一个都没启动**（`case` 落到 `*)` 分支执行了 `exit 2`）。

**两层根因**
1. **键名 ≠ 类名**：registry/python/driver 的规范键是 **`SAMOEA`**，
   而**类名与输出目录名是 `SAMOEATL2M`**。我把类名当键名传进 `ALGS`。
   危险的是这个 `case` 的兜底是 `exit 2`，**不是跳过**——一个拼写错误带走了整轮 16 h 的扫描。
2. **更隐蔽**：`missing_runs.py` / `mark_poison.py` 拼 MAT 文件名时用的是**键名**，
   但真实文件名用的是**类名**（harness 用 `R.cls`）。七个算法里只有 SAMOEA 两者不同 ⇒
   用 `FE500_ALG=SAMOEA` 时会去找 `SAMOEA_*.mat`、**一个都找不到 ⇒ 判成 320 个全缺**。
   （爆出来之前就一起修了。）

**修复**：driver 的 `case` 收别名 `SAMOEA|SAMOEATL2M` 并把 key 归一；**未知键改成打印+跳过+末尾汇总，
绝不 `exit`**；python 侧加 `CLASSES` 表 + `resolve()`，**文件名一律用类名**、目录用 `FOLDERS`
（可被 `FE500_FOLDER` 覆盖，driver 已导出，让类覆盖换目录时也同步）；
`run_FE500_M20.m` 也把 `SAMOEATL2M` 归一成 `SAMOEA`；顺手修掉 `poison.txt` 不存在时的 bash 噪声。

**干跑**：两个键给出完全一致的 32 片、且都正确认出已存在的文件；`bash -n` OK。

**00:38 重启（任务 `Of9LG9`，用规范键）**：**SSDE 320/320 完成**、成功跨过 SAMOEA 边界、
SAMOEATL2M 已开始落盘（3→4 进程、空闲内存 12.4 GB）。巡检自动化的恢复命令同步成规范键版。

---

## 01:23 用户"开最大进程跑吧，我已经把大部分后台清除了"（第五次重启）

### 硬件实测
**16 个物理核、无超线程**（`/proc/cpuinfo` 的 `cpu cores: 16` 与 `^processor` 计数都是 16）
⇒ **16 路正好 1:1**。基线空闲内存 **16.8 GB → 19.9 GB**（用户清后台后涨了约 3 GB）。

### 决策：`MAXJOBS=16` + `MIN_FREE_MB=2000`（纯应急刹车）
上一轮"不要内存闸"的前提是"12 路有余量"，**16 路这个前提不成立**，所以给一个 2000 MB 的兜底。
**仓库默认仍是 0=关闭**，只在本次命令行显式传 2000 —— "默认不要闸"与"这次要兜底"不冲突。

### 实测：每进程 ≈ **1.04 GB**，16 路约剩 3.3 GB，闸正常不触发
01:27 稳态：10 进程 / 空闲 9.5 GB（基线 19.9）⇒ 1.04 GB/进程。外推 16 路 ⇒ 约 3.3 GB，
高于 2000 MB ⇒ **闸触发次数实测 0**，16 路应该能跑满。

### ⚠️ "进程数只有 10"不是节流问题，是活儿不够
SAMOEATL2M 已有 246 文件、**只剩 74 跑**，`missing_runs.py` 只排出 **8–10 片**
（每片 9–10 跑）⇒ **池子必然填不满**。这就是"碎片化波尾"，与节流/内存无关；
进入 CSEA（32 片全新）才会跑满 16。
⇒ **排查顺序更正为：① 数据在不在涨 → ② 还剩几片（跑一下 `missing_runs.py` 数）→ ③ 才怀疑节流/内存。**

### 现场
停止前 `SSDE 320/320`、`SAMOEATL2M 246/320`、`HES_EA 1/320`，**总计 567/2240** 全部保留。
新后台任务 **`7CMxJT`**。巡检 prompt 同步 16 路 + `MIN_FREE_MB=2000`，并新增一条排查项：
日志里出现 `[mem] ... holding next launch` 就说明闸在起作用（内存吃紧）。
**ETA 约 13 h**，约 09-21 14:00 结束。
