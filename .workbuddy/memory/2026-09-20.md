# 2026-09-20

> 本目录的详细项目日志在 `PlatEMO-master/.workbuddy/memory/2026-09-20.md`（那份更全、含 FE500 PIEA / SAMOEA / SSDE / HES 等）。
> 这里只记本工作区（`D:\PlatEMO-master`）当天与其等价的关键结论。

## FE500 / M=20 / D=30 七算法全系列：框架已建、跑过一轮、23:52 用户要求暂停

**用户规格**：7 个算法（REMO / PC-SAEA / CSEA / HES-EA / SSDE / SAMOEA-TL2M /
`REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist`）× 16 题（DTLZ1–7 + WFG1–9）× **20 跑**，
`maxFE=500`、`M=20`、`D=30`（WFG2/WFG3 实际 D=31）、初始 `N=100`、**每题都要 IGDp**；
落盘 `D:\REMOandDREMO测试集\20目标\FE500\<算法>\`。
**用户中途追加明确**：`PCSAEA` / `SAMOEATL2M` / `HES_EA` **一律用原版类，不用 `_N100` 变体**。

### 交付物：`PlatEMO/Experiments/FE500_M20_SevenAlgs/`
`fe500_m20_registry.m`（唯一真源：key / 类名 / 输出目录 / 参数 / 选它的理由）、`run_FE500_M20.m`、
`probe_FE500_M20.m`（阶段1 体检）、`missing_runs.py`（按算法算缺口切片）、`driver.sh`
（分算法顺序、可重驱、带 `SLICE_TIMEOUT`）、`verify_FE500_M20.m`（验收 + 打印每题 FirstFE）、
`build_FE500_M20_table.m`（阶段5 出 IGD/IGDp 均值±std + ranksum 对 PACDIS）、`README.md`。

种子 `22260912 + 1000×题号 + run`（= `20260912 + M*1e5`），与 `20目标` 既有 FE300 批次**同一套种子**
⇒ FE300 ↔ FE500 可逐跑配对做 Wilcoxon 符号秩检验。基线**一律传空参数 `{}`** 走 published 默认。

### 阶段1 体检：PROBE OK
7 个类全解析成功；`Solve` 前被抢占的自带 helper（REMO 7 个、PC-SAEA 2、CSEA 2、SSDE 1、PACDIS 2）
在 `Solve` 后全部纠正回本目录、0 个残留；16 题 D 表正确（仅 WFG2/WFG3 = 31）。

### 阶段2 计时 + 预热（`RUNS=19,20`）：实测单跑成本（M=20 / FE500 / threads=1）
| 算法 | 单跑 wall | 末次 FE | 快照 | FirstFE |
|---|---|---|---|---|
| SSDE | ≈2–5 s | 500–543 | 15–30 | 100 |
| SAMOEATL2M | ≈110 s | 504 | 30 | 329 |
| HES_EA | ≈628 s | 504 | 30 | 329 |
| CSEA / PCSAEA / REMO / PACDIS | 还没轮到（暂停） | | | |

已落盘且**全部有效可复用**：`FE500\SSDE\` 32 个（16 题 × run 19,20，约 6 min 跑完）、
`FE500\SAMOEATL2M\` 5 个、`FE500\HES_EA\` 1 个。均含 `result + metric{runtime, IGD, IGDp}`。

### 两个必须记住的结论

1. 🔴 **末次 FE 严禁严格判等 maxFE**。按批发评估的算法末次 `NotTerminated` 落在 maxFE **之上**
   （实测 M=20：REMO 301–305、CSEA 301、SSDE 在 FE500 档 **500–543**、`11D-1` 型在 D=31 是 340）。
   旧判据 `feList(end) == maxFE` 会把这些文件一律判无效 ⇒ 可重驱的 driver **每轮重算、永不收敛**。
   共享 harness `run_UniformMixPrunedFullSeries.m` 已加 `FESlack` 参数（**默认 0 = 旧行为逐字不变**），
   本实验取 **100**。SSDE 那 32 个文件里有 12 个 FE>500，全靠这个容差才算有效。

2. ⚠️ **「原版 vs `_N100` 变体」要按 maxFE 档重新判断**。同一个原版类在 maxFE=300 下
   「主循环零次执行」（`11D-1 = 329 > 300`，存下来的是纯初始种群、只有 1 个快照），
   在 maxFE=500 下**却能正常跑**（329 < 500，只是只剩 ~171 次给搜索）。
   本机 `20目标\PCSAEA`、`20目标\KRVEA` 各 480 个文件就是「FE=329、单快照」的废数据。
   另：`HES_EA.m` 与 `HES_EA_N100.m` **只差 `InitN` 一行**，两个文件都已含
   `ang(isnan(ang) & ~isinf(ClObj(:,1))) = pi;` 那段 NaN 修补。

### ⚠️ 对既有判断的重要修正：原版 `HES_EA` 在 DTLZ2 没有死锁
实测 `HES_EA`（原版）DTLZ2 / run19 **正常跑完**：FE 329→504、IGD 1.40313 → 1.01623、runtime 628.5 s。
之前看到的「10 分钟零输出」是 **HES 在 M=20/FE500 的正常单跑时长**，不是卡死。
⇒ 那个聚类死锁是**题 × 种子**相关的（旧诊断用 run 1 的种子，本次是 run 19），
**单样本不能排除**，`SLICE_TIMEOUT`（driver 默认 10800 s）必须保留。

### ⚠️ 预算分配不等，报数必须交代
`PCSAEA` / `HES_EA` / `SAMOEATL2M` 初始设计 `NI = 11*D-1 = 329`，**吃掉 66% 预算**，
只剩 ~171 次给搜索；`REMO` / `CSEA` / `SSDE` / `PACDIS` 从 100–109 起步、有 ~400 次。
`verify_FE500_M20` 会打印每题 **FirstFE** 把这件事实锤下来。

### 23:52 暂停时的现场（已收干净）
两个后台任务（driver `tc3iEJ`、HES 死锁裁决 `iUkz1j`）已 TaskStop，**MATLAB 进程 0 个**，
**无 `*.tmp.mat` 残留**。数据与日志全部保留（别删 `FE500\SSDE` 那 32 个）。

### 恢复
```bash
cd PlatEMO/Experiments/FE500_M20_SevenAlgs
ALGS="CSEA PCSAEA REMO PACDIS HES_EA SAMOEATL2M SSDE" bash driver.sh   # 幂等，只补缺
matlab -batch "addpath('<本目录>'); verify_FE500_M20"                  # 验收
```
`FE500_CLS_HES_EA=HES_EA_N100_guard` 可切 guard 版。

### 顺手固化的经验
已写进 `PlatEMO-master/.workbuddy/pipelines/algorithm_full_series/README.md` 踩坑清单**第 10、11 条**。
