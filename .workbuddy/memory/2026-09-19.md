# 2026-09-19 工作日志

## 06:52 M=20 消融扫描完成：REMO_noBatchDict_noCDIS / _noPAQC（640/640）

- **结果**：一次 attempt（round 1）内全部跑完，**耗时 6 h 55 min**（2026-09-18 23:57:03 → 2026-09-19 06:52:51），无 MATLAB 崩溃、无重跑。
- **数据**（各 320 = 16 题 × 20 跑）：
  - `D:\REMOandDREMO测试集\20目标\REMO_noBatchDict_noCDIS\`
  - `D:\REMOandDREMO测试集\20目标\REMO_noBatchDict_noPAQC\`
- **全量校验通过**：两算法 **OK=320 / BAD=0 / TOTAL=320**，`result`(30×2, 末 FE=300) + `metric{IGD,runtime}` 齐全；WFG2/WFG3 正确落 D=31，其余 D=30。
- 汇总 CSV：`Experiments/REMO_noBatchDict_Ablation_M20/summary_REMO_noBatchDict_{noCDIS,noPAQC}_M20.csv`
- 实测单跑 runtime 均值：noCDIS ≈ **389 s**（373–411），noPAQC ≈ **438 s**（372–466）；12 路并行、threads=1。

### 四臂消融对照（`compare_Ablation_M20.m` → `compare_M20_ablation.csv`）
四臂全部共用 SeedBase=22260912 的同一组 20 跑 → **逐跑配对**（Wilcoxon signed rank + 各题 p 值 Fisher 合并）。消融链：full(PACDIS) → −批次距离项(=NoBatchDist) → −PAQC / −CDIS。

16 题最终 IGD 均值（越低越好）：

| 题 | full | NoBatchDist | noPAQC | noCDIS |
|---|---:|---:|---:|---:|
| DTLZ1 | 77.5300 | 71.5813 | 82.2643 | 76.9825 |
| DTLZ2 | 1.1481 | 1.1207 | 1.0919 | 1.2304 |
| DTLZ3 | 316.6443 | 296.4324 | 377.2746 | 347.6976 |
| DTLZ4 | 1.0640 | 1.0473 | 1.0671 | 1.0305 |
| DTLZ5 | 0.3073 | 0.2991 | 0.3155 | 0.2772 |
| DTLZ6 | 6.6906 | 7.0594 | 7.6406 | 6.1674 |
| DTLZ7 | 19.4211 | 22.9996 | 28.2815 | 23.7089 |
| WFG1 | 5.5674 | 5.6092 | 5.5922 | 5.6004 |
| WFG2 | 5.0241 | 4.8619 | 6.2701 | 5.8705 |
| WFG3 | 2.7531 | 2.7425 | 2.4575 | 2.7786 |
| WFG4 | 24.1806 | 23.9986 | 24.1672 | 25.2463 |
| WFG5 | 22.6822 | 22.6384 | 22.2352 | 23.8016 |
| WFG6 | 19.3615 | 19.3838 | 20.0707 | 19.3851 |
| WFG7 | 20.7537 | 20.4670 | 21.1263 | 21.0820 |
| WFG8 | 20.8360 | 20.7998 | 21.5553 | 20.6991 |
| WFG9 | 23.5270 | 23.3171 | 23.2538 | 25.2976 |

配对检验汇总（正 gap = 前者更差）：

| 对比 | 平均 gap | 前者胜/320 | 16 题 Fisher p | 结论 |
|---|---:|---:|---:|---|
| noPAQC vs NoBatchDist | +6.27 | 142 | 1.4e-08 | **PAQC 有正贡献（强）** |
| noCDIS vs NoBatchDist | +3.91 | 132 | 4.3e-09 | **CDIS 有正贡献（强）** |
| noPAQC vs noCDIS | +2.36 | 160 | 9.2e-15 | PAQC 比 CDIS 更关键 |
| NoBatchDist vs full | −1.45 | 156 | 0.0257 | 去掉批次距离项**略微变好**（弱显著） |
| noPAQC vs full | +4.82 | 138 | 1.1e-10 | — |
| noCDIS vs full | +2.46 | 130 | 2.4e-09 | — |

- **核心结论**：PAQC 与 CDIS **都不冗余**，各自移除都在合并检验下显著变差（p≈1e-8~1e-9）；且两者不能互相补偿。
- 逐题细节（显著项）：
  - 去 PAQC 主要输在 DTLZ3(+80.8)\*、DTLZ7(+5.28)\*\*、WFG2(+1.41)\*\*、DTLZ6(+0.58)\*、WFG8(+0.76)\*；仅在 DTLZ2(−0.029)\*、WFG3(−0.285)\*\* 略好。
  - 去 CDIS 主要输在 DTLZ3(+51.3)\*、WFG9(+1.98)\*\*、WFG4(+1.25)\*、WFG5(+1.16)\*\*、WFG2(+1.01)\*\*、DTLZ2(+0.11)\*\*；在 DTLZ6(−0.89)\*\* 明显更好。
  - 去掉批次距离项：DTLZ2(−0.027)\*、DTLZ4(−0.017)、DTLZ1(−5.95)/DTLZ3(−20.2) 变好，DTLZ7(+3.58)\*\*、DTLZ6(+0.37)\* 变差。
- ⚠️ **报告口径提醒**：「16 题平均」被 DTLZ1/DTLZ3（量级 70–380）主导，**主口径应看逐题均值 + 配对胜负比 + 逐题 p**，不要用 16 题总平均下结论。

### 框架与复用
- `Experiments/REMO_noBatchDict_Ablation_M20/`：`run_noCDIS_M20.m`、`run_noPAQC_M20.m`、`driver.sh`（MAXJOBS=12/threads=1/10 跑切片/rounds=10）、`missing_runs.py`、`verify_noBatchDict_Ablation_M20.m`、`probe_M20_k.m`、`smoke_Ablation_M20.m`、`compare_Ablation_M20.m`；**该目录在 .gitignore 内**（与 pMixSweep / RWMOP11 同例）。
- 🔴 复用坑位（已写入 MEMORY.md）：`run_UniformMixPrunedFullSeries` 的 `'Runs'` **标量会被展开成 `1:Runs`**，要跑单个 run 必须传向量。
- 冒烟残留目录 `D:\REMOandDREMO测试集\_smoke_noBatchDict_M20`（8 个 mat）仍在，可按需删除。
- 巡检自动化 `fbadb817-08f1-40bc-bc0a-ae68d72d3f0a` 已置 **PAUSED**（实验结束）。

## 09:31–10:05 HES_EA_N100 @ M=10 扫描启动（仅十目标，20 目标按用户要求不跑）

- **任务**：HES_EA_N100（IEEE TEVC 2024，层次+集成代理模型 EA）在 **M=10 / D=30 / N=100 / maxFE=300 / SaveCount=30** 跑 DTLZ1-7 + WFG1-9 = 16 题 × 20 跑 = **320 跑**；用户额外要求**保存 IGDp**。数据 → `D:\REMOandDREMO测试集\10目标\n30\HES_EA_N100`。用户明确：**先只跑十目标，20 目标不跑**。
- 算法位置：`PlatEMO/Algorithms/Multi-objective optimization/HES-EA/HES_EA_N100.m`（与该文件夹内 `HES_EA.m`、`HES_EA_100.m` 共用目录）。超参用论文默认 `{wmax,WN,KMeans}`=`{20,190,4}` —— **必须传 `'Parameters',{}`**，否则 harness 默认的 5 元组会把 KMeans 设成 0.25 直接炸。
- 种子 SeedBase=**21260912**（=20260912+10×100000），与 10 目标数据集其余算法逐跑配对。
- **IGDp 采用「边跑边存」而不是事后 merge**：给共享 harness `run_UniformMixPrunedFullSeries.m` 增加了**可选**参数 `ExtraMetrics`（默认 `{}`，对所有既有实验零影响），内部对每个名字调用 `ALG.CalMetric(name)`。因此 HES_EA 的每个 MAT 直接含 `metric{runtime, IGD, IGDp}`（各 30 快照），省掉一次 320 文件重算（M=20 那次 merge 实测约 11 文件/min）。已验证落盘正确。
- 新增框架 `Experiments/HES_EA_N100_M10/`：`run_HES_EA_N100_M10.m`、`driver.sh`（MAXJOBS=12/threads=1/10 跑切片/rounds=10）、`missing_runs.py`、`verify_HES_EA_N100_M10.m`（额外校验 IGDp 齐全）、`smoke_HES_EA_N100_M10.m`。该目录在 .gitignore 内。
- 启动 2026-09-19 10:03，32 个切片；单跑实测（DTLZ2）≈ **166 s**（低并发），预计 **2–3 h**。
- 🔴 **坑位修正（重要）**：harness 的 `'Runs'` 只有 `isscalar` 判断，而 **MATLAB 里 `isscalar([7])` 就是 `true`** → 传单元素向量**仍然**会被展开成 `1:7`。**该 harness 无法只跑一个 run**，最短请求是两元素向量。此前记忆里写的「传向量如 `[99]` 即可跑单个 run」是错的，已修正；本次因此白跑了两轮冒烟（约 25 min），已止损。
- 伴生问题：HES_EA_N100 第 75 行的 `pdist2(...,'cosine')` 在归一化目标含零行时（WFG3 等）会刷 `stats:pdist2:ZeroPoints` 警告，单条 7 行。已在 **runner（非算法）** 里 `warning('off','stats:pdist2:ZeroPoints')` 压制，日志恢复正常体积。
- 冒烟残留目录 `D:\REMOandDREMO测试集\_smoke_HESEA_M10`（DTLZ2 4 个 + 可能 WFG3 若干 mat）可删。
- 巡检自动化 `80de3726-1133-48c1-a98b-b3ca0f816a04`（每小时，有效至今天 20:00）。

## 10:14–10:18 HES_EA_N100 M=10 扫描暂停 + 框架入库推送（换机器跑）
- 用户要求先暂停，并把 `Experiments/HES_EA_N100_M10` 推到远端，**要在另一台电脑跑**。
- **暂停干净**：0 个 MATLAB 进程、无 `.tmp.mat`、已落盘 **8/320**（DTLZ1/2/3/4 各 1–2 跑，全部有效）。无算法报错。
  - 注意顺序：先杀 MATLAB 再停 driver 会让 driver 的补缺循环又拉起一个切片（part 7），必须**先 TaskStop driver 再杀 MATLAB**。本次已一并清干净。
- `.gitignore:8` = `PlatEMO-master/PlatEMO/Experiments/` 整目录被忽略 → **必须 `git add -f` 按文件名逐个加入**（不能加目录，否则会把 logs/ 一起带进去）。
- 本次入库 **6 个文件**（commit `b8dbafd`，已推送，远端 master = b8dbafd）：
  - `Experiments/HES_EA_N100_M10/{driver.sh, missing_runs.py, run_HES_EA_N100_M10.m, smoke_HES_EA_N100_M10.m, verify_HES_EA_N100_M10.m}`
  - `Experiments/REMO_new2_AdaMaO_UniformMix_Pruned_FullSeries/run_UniformMixPrunedFullSeries.m`（**共享 harness，含 ExtraMetrics 新参数，另一台机器必需**）
  - 算法 `HES-EA/`（含 `HES_EA_N100.m`、`GP model/`）本来就在库里，无需额外处理。
- **换机器可移植化改造**：
  - `driver.sh` 改为**自定位**（`BASH_SOURCE` + `pwd -W`），`MP`/`PY`/`DATADIR`/`MAXJOBS`/`ROUNDS` 全部可用环境变量覆盖，默认值 = 本机路径；脚本头注释写明了另一台机器的用法。
  - `missing_runs.py`：`ROOT` 可用环境变量 `HESEA_M10_DATA_DIR` 覆盖。
  - `run_HES_EA_N100_M10.m`：`OutputRoot` 可用 `HESEA_M10_OUTPUT_ROOT` 覆盖（FolderName 固定 `HES_EA_N100`）。
  - 路径假设不变：仓库布局 `PlatEMO-master/PlatEMO/`、数据根 `D:\REMOandDREMO测试集\10目标\n30`、算法目录 `Algorithms/Multi-objective optimization/HES-EA`。
- 换机器续跑方式：`git pull` → 进 `Experiments/HES_EA_N100_M10` → 先 `powercfg /change monitor-timeout-ac 0 && powercfg /change standby-timeout-ac 0` → `bash driver.sh`（**完全可续跑**：已有有效 MAT 自动跳过，只补缺；本机已跑的 8 个可以拷过去也可以不要）。
- 巡检 `80de3726` 已置 **PAUSED**（实验暂停）。

## 10:53 本机恢复 HES_EA_N100 M=10 扫描（320 跑）
- 用户要求继续在本机跑（仍是只跑十目标、含 IGDp）。driver 重启：`present=8/320, 32 片`，**已完成的 run 1/11（DTLZ1-4）自动跳过**，切片起点从 run 2 / run 13 继续 —— 可续跑语义验证通过。
- 重启前先做了 `bash -n driver.sh` 与 `ast.parse missing_runs.py` 语法检查（因为 driver 刚被重写成自定位版）。
- 巡检 `80de3726` 重新置 ACTIVE（validFrom 11:55，每小时，至今天 23:59）。
- ETA 维持上次口径：单跑 ≈318 s（12 路满负荷）、每片 10 跑 ≈53 min、32 片 / 12 并发 ≈ 3 波 → **预计 ~2 h 45 min，完成约 13:40 前后**（从 10:53 起算）。

## 10:56–11:12 🔴 git merge 事故（已完全恢复）+ 实验重启

- **起因**：本机 push 被拒（远端已前进到 `5687a4e` = 另一台机器 10:34 推的 2 个提交，`论文写作/` 图表等 83 文件、+48196/−3405）。执行 `git merge --no-edit FETCH_HEAD` 时命令被 SIGTERM，留下 **0 字节的 `.git/index.lock`**，并把工作区搞坏：**905 个文件被删**（磁盘 .m 只剩 2117/2859），含 `Algorithms/ALGORITHM.m`、`HES-EA/HES_EA_N100.m`、`GP model/dacefit.m`、`.workbuddy/memory/**` 等。
- **对实验的影响**：文件缺失期间 driver 新拉的 MATLAB 全部秒退（源码没了），实例从 12 掉到 3；已载入内存的 3 个实例继续跑完（期间还正常产出，11:03 仍有 `[done]`）。
- **恢复步骤（可复用）**：
  1. 发现 `Another git process seems to be running` → `tasklist` 确认无 git 进程 + `index.lock` 时间戳 = merge 被杀时刻 → 确认陈旧锁；
  2. `rm .git/index.lock`；
  3. `git reset --hard HEAD`（HEAD=a76d21b 一切都有提交，零丢失）→ 磁盘 .m 恢复 2859/2859，status 归零；
  4. **TaskStop driver（先）→ 杀 MATLAB（后）** → 用 `run_in_background` 重启 driver（⚠️ 前台 `nohup ... &` 会在工具调用结束时被连带杀掉，matlab 归零，必须用后台任务方式）。
- **11:10 重启后**：attempt 1 `present=19/320`，缺失切片正确稀疏化（如 part1 从 run 4 起），12 路满负荷爬升中。无数据丢失（所有 .mat 都在 `D:\REMOandDREMO测试集`，不在仓库内）。
- **事后核实**：`merge-base(HEAD, 5687a4e) = 392674d`（正确），正规合并只该动 83 文件 —— 905 删除是被杀时的异常产物，不是合并本意。
- **决定：本机 merge 推迟到实验结束后再做**（合并期间再出锁/工作区事故会再次打断实验）。本机现有 2 个未推的 memory 提交（a76d21b + 本条），push 会一直被拒直到合并。备选：让**另一台机器** `git pull`（它在 10:34 已证明能正常 merge）。
- 🔴 教训入库：**长实验运行期间不要在本机做 git merge/pull 这类会重写工作区的操作**；`git status` 出现大量 `D` 时先查 `index.lock`。
