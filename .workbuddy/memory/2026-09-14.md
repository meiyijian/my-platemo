
## M=5 D=10 Weighted WFG sweep（09:44–）
- 凌晨兜底巡检：链在 ~06:33 死亡（阶段2 207/270，锁心跳停 06:25），07:37 接管重启后 08:07:44 chain COMPLETE；M=15 与 M=8(D=10) 各 270/270 verify COMPLETE。报告 `tmp/weighted_wfg_m15/chain_final_report_20260914.md`，巡检自动化已删。
- 新建 `run_WeightedWFG_M5D10.m`（薄封装 M=5/D=10/输出 5目标\n10），checkcode 通过；smoke 2 跑通过（WFG2 在 M=5 保持 D=10，seed 公式验证正确，单跑 ≈100s）。
- 09:48 后台启动全量 270 跑（4 分区 supervise_part.sh，scratch `tmp/weighted_wfg_m5/`），吞吐 ≈1.9 跑/min，ETA ≈12:10；12:15 一次性自动化（d14b8594）负责 verify 与异常接管。
- 已按此前系统提醒完成 MEMORY.md 瘦身重写（约 101 行 → 精简版）。
- 11:05 用户要求暂停 M=5 sweep：已停止后台任务 bbSeqH（1h17m），MATLAB 进程清空，产出停在 159/270，无 .tmp 残留；12:15 校验自动化（d14b8594）已设 PAUSED 防止其自动接管重启。恢复方式：同环境变量重跑 supervise_part.sh 4 分区即可断点续跑（~111 跑 ≈ 55 min）。

## 13:45 MATLAB 进程诊断（PWGGP ETA + M5 sweep 停滞）
- 正在跑：PWGGP（GoodGroupPrecision formal，run_TopFourPartition(k,8)），11:56:27 启动，8 分区 × 25 跑 = 200。13:40 时点 70/200，单跑 wall 715→816s 缓慢上浮（~13 min/跑）。按最慢分区剩 17 跑估算：还需 ~3.5–4h，预计 17:15–18:00 完成。
- 结果/日志在内层路径 D://PlatEMO-master//PlatEMO-master//PlatEMO//Experiments//REMO_new2_AdaMaO_PrunedWeighted_GoodGroupPrecision//results//（外层 D://PlatEMO-master//PlatEMO//... 是另一份，统计时别用错）。
- 发现：M5 Weighted WFG sweep（run_WeightedWFG_M5D10.m）自 11:05 起停滞：159/270（WFG6 仅 9/30），4 个 MATLAB 进程已消失，分区日志尾部均为正常 [done] 无报错——疑似进程被终止（11:56 PWGGP 启动前）。剩余 111 跑（WFG6 21 + WFG7/8/9 各 30）约 55 min（4 并行），需重启 supervise 续跑，建议等 PWGGP 结束后错峰。
- 工具坑：本会话 bash shim 缺 coreutils（wc/tail/head/wmic 均不可用，wmic 已被系统移除）；PS 工具 stdout 不回显，可 Out-File 落盘再 Read；psutil 未装，用 ctypes GetProcessTimes + tasklist csv 读进程启动时间/CPU。

## 18:20 PWGGP 正式跑收尾 + PAQC 准确率分析
- PWGGP formal 200/200 于 17:00 完成（实际比 ETA 快）。analysis/formal 已由 MATLAB 管线自动产出 5 个 CSV（CheckpointMetrics 63MB / PerRunStage / PairedComparisons / LabelDynNative / Coverage）。
- 核心结论：score_hybrid 的 Precision@25% 随 FE 阶段单调升（population_final 口径 0.088→0.733，200/200 run 全升，Wilcoxon p=1.4e-34）；但 Chance 同期 0.036→0.672，Lift 2.57→1.11、AUC 0.786→0.573——净判别力早期最强、晚期收窄。DTLZ7 S1 仅 0.003（不连通前沿早期不可预测）。score_hybrid vs anchor_margin 显著优（21/32 Holm），vs score_v 仅轻微不稳定优势（12/32）。
- 分析报告：results/analysis/formal/PWGGP_PAQC_accuracy_progression_report_20260914.md。论文警示：只报 Precision 曲线会被审稿人戳穿，必须配 Chance/Lift/AUC。
- 工具坑：bash 里用 `python || python` 双 heredoc 会把第二段当交互输入产生海量 REPL 报错，禁止这种写法；analysis CSV 需要的 pandas/scipy venv 里已装好。

## 0914 Git 仓库灾难与完全恢复（14:00-15:30 前后）
- 触发：git pull --rebase 在本机 git 环境报 could not mark as interactive，连锁清空 refs/reflogs/packed-refs 及部分零散对象；HEAD 树 20bcefc7 与 12 个 blob 丢失。
- 恢复：从 .git_rescue_20260914 备份还原 7 个对象；12 个缺失 blob 用工作树文件 hash-object -w 原样重建（sha 全部一致）；write-tree 精确复现原树。
- 合并绕开 git merge（其 checkout 会中途被杀且误删文件）：用 GIT_INDEX_FILE 临时索引 read-tree 远端树 + update-index 叠加本地 14 文件 + write-tree + commit-tree 构造合并提交 7e0b5582（父=00b4593+5419861），推送成功，ls-remote 已确认。
- 期间 2190 个 Algorithms 已跟踪文件被短暂删除（并发 git 会话互相踩踏所致），已用 checkout-index 从索引完整恢复；唯一内容冲突 MEMORY.md 判定远端==基线、取本地现行版，零丢失。
- 用户另一台电脑 VS Code 提交报 index.lock：指导删除锁文件；已提醒两台电脑勿并发操作同一仓库、提交前先 pull。
- 恢复脚本与诊断存于 tmp/git_rescue/（可复用）；.git_rescue_20260914 为对象备份，暂留。

## 19:41 分析产物清理（用户确认执行）
- 应用户中断后恢复的指令，清理 PWGGP 实验目录 results\analysis\：formal+smoke 共 9 个分析产物（CheckpointMetrics 60MB / PairedComparisons / LabelDynNative / Coverage / PAQC 准确率报告 md，合计 61.5MB）移入 results\analysis\_recycle_20260914\（回收站式，未永久删除）。
- 保留：PerRunStage.csv（formal 3.1MB + smoke）、raw 201 个 .mat（1080MB）、manifests 206 个、logs 32 个、根目录实验脚本与 algorithms 快照。
- 分析结论要点已留在本日志前文与 MEMORY.md；如需重跑分析，可从 raw .mat 重新生成全部产物。
- 用户指示：M5 Weighted WFG sweep（停滞于 159/270）先不重启，等用户安排。
