# 2026-09-12 工作日志

## 00:43 Pruned 全系列实验首次巡检
- 目标：`D:\REMOandDREMO测试集\10目标\n30\REMO_new2_AdaMaO_SDEOnly_UniformMix_Pruned`（288 文件 = 16 题 × 18 跑）。
- 进度 50/288；4 分区均正常在跑，处于各自第 1 个问题（DTLZ1/2/3/4）第 12–13 次运行。
- part2/part3 早期各崩过一次 0xc0000374，supervise 脚本自动重启且正确 skip 已写盘结果；4 个 MATLAB 进程存活。
- 单跑 wall 4.3–5.4 min，全系列预计 05:10–05:30 收尾。未做任何重启/删除操作。

## 04:58 Pruned 全系列实验收官 + 完整性校验 COMPLETE
- 288/288 文件全部就位（16 题 × 18 跑），4 分区均打出 `part N/4 done: 72 runs`，stdout 末尾均为 `part N COMPLETE`。
- 分区耗时：part1 5.3h(0 skipped)、part2 5.1h(1 skipped)、part3 5.1h(1 skipped)、part4 5.3h(0 skipped)。part2/3 的 1 skipped 即早期崩后重启的断点跳过。
- 全程吞吐极稳：0.93–0.95 文件/min；总 wall 约 5.3 h（23:24 起 → 04:58 止）。
- 校验：`verify_pruned_run.py --runs 18 --expect-fe 300` → mat 288 / valid 288 / missing 0 → **RESULT: COMPLETE**。
- 产物：`_verify_runs.csv`（逐跑）、`_runlog_part{1..4}of4.csv`（分区汇总）落在目标目录。
- 全 16 题最终 IGD 均值已汇总（DTLZ3 ≈961.6 最大、DTLZ5 ≈0.828 最小；WFG 家族 2.18–6.57）。
- 巡检任务已完成使命，可删除。

## 04:58 收尾
- 巡检自动化 `Pruned全系列实验巡检`（id a447f706…）**已删除**，实验结束不再需要。
- 补充产物：`_verify_summary.xlsx`（Sheet1=IGD_summary 逐问题均值/标准差/中位/极值，Sheet2=per_run 逐跑明细），落在目标数据目录。
- 新 18 次结果与历史 9 次 Pruned（无种子、GUI 口径）比对，6 个重叠问题均值高度一致（DTLZ2 1.1668 vs 1.2388、DTLZ5 0.8285 vs 0.8556、DTLZ7 10.4918 vs 10.7667、WFG4 6.5677 vs 6.4036、WFG7 5.4287 vs 5.4629、WFG9 6.3193 vs 6.6198）→ 复现口径正确。
- 崩溃恢复实测生效：分区 2、3 各触发过一次 supervisor 重启（`attempt 2`，各跳过 1 个已完成 run 后继续），未丢任何数据。

## 06:41–06:50 新建 qKeep=0.80 变体并启动第二组实验
- 背景：Pruned 源码 `ParameterSet` 默认本来就是 **qKeep=0.80**；上次正式实验是**配置覆盖成 0.70** 跑的。用户要求再跑一组 0.80，并**新建算法名**以免数据互相覆盖。
- 新算法 `REMO_new2_AdaMaO_SDEOnly_UniformMix_Pruned_qKeep080`（目录与 `Algorithms/Multi-objective optimization/` 下同名文件夹）：
  - **源码同构副本**：`private/` 11 个模块 + 根级 3 个 .m **全部字节一致**；主文件与上游仅有 2 处差异（第 1 行 classdef、头部新增 5 行说明注释）。已 diff 逐行核验。
  - 未复制 `diagnostics/`（旧诊断产物引用旧路径，带过去会误导）。新增 `source_provenance.json` 记录来源、差异清单与两份主文件 sha256。
  - `tests/PrunedIntegrationTest.m` → `PrunedQKeep080IntegrationTest.m`（classdef 与 4 处算法构造同步改名）；`README_详细流程.md` 加了「本目录身份」说明。
  - 行尾统一为 CRLF（Write 工具产出 LF，需 `sed -i 's/$/\r/'` 补回；本仓 .m 都是 CRLF）。
- **等价性验证（重要做法）**：同种子同问题（DTLZ2, maxFE=106）跑三组 ——
  - A = 上游类 + 0.70；B = **上游类 + 0.80**；C = **新类 + 0.80**
  - 结果：**B == C（差 0）** → 克隆忠实；**A ≠ B（差 6.15e-3）** → qKeep 确实起作用。
  - 这条路子（"新类+新参数 == 旧类+同参数" 且 "≠ 旧参数"）值得作为日后所有"参数变体复制"的验收标准。
- 数据目录 `D:\REMOandDREMO测试集\10目标\n30\REMO_new2_AdaMaO_SDEOnly_UniformMix_Pruned_qKeep080\`，16 题 × 18 次 = 288 跑，参数 `{3000,0.50,0.25,0.80,6}`，其余口径（N/D/M/maxFE/种子/`run` 分流）与 0.70 那组完全一致 → 两组可直接配对比较。
- `tmp/supervise_part.sh` 已**泛化**：支持 `ALG` / `FOLDER` / `PARAMS` / `HARNESS` 环境变量覆盖，默认指向 qKeep080 这组；日志按 FOLDER 分目录，避免与上一组混写。
- 06:47 启动 4 路并行，预计 ~5.3 h（约 12:00 完成）。已建巡检自动化 `Pruned_qKeep080实验巡检`（id aba96248…），完成后会并排给出两组逐问题 IGD 对比。

## 07:51 qKeep080 实验巡检 #1（启动后首巡，64 min）
- 进度 **46/288**；4 分区全部存活（8 个 MATLAB 进程 = 4×[launcher+engine]），无分区处于中断态（无任何 stdout 以 `exited rc=` 结尾）→ **未做重启**。
- 各分区当前只跑完各自第 1 个问题（part1 DTLZ1 run13、part2 DTLZ2 run9、part3 DTLZ3 run11、part4 DTLZ4 run13），run 编号 1..N 连续无缺口，无 `*.tmp.mat` 残留。
- **关键认知修正**：supervise 脚本记的 `exited rc=127` **不是**"命令找不到"，而是 MATLAB 以 `0xc0000374` 崩溃后 bash 侧拿到的退出码；stdout 里紧邻的 `ERROR: MATLAB error Exit Status: 0xc0000374, Heap corruption` 才是真因。**该崩溃一律发生在结果写盘之后**（attempt 内先 `[done] …ok` 再崩），重启后 skip 校验正确跳过已完成文件，数据零丢失。
- 崩溃频次差异大：part1/part4 各 0 次；**part2 已崩 7 次（attempt 8）、part3 崩 5 次（attempt 6）**——重启开销使 part2 吞吐降到 0.156 文件/min，成为全场瓶颈。attempt 上限 200，余量充足。
- 吞吐/ETA：全局 46 文件 / 64 min ≈ 0.72 文件/min；单跑 wall 4.1–5.5 min。分区剩余估算 part1 ≈4.8h、part2 ≈6.6h、part3 ≈5.3h、part4 ≈4.8h → **预计 13:00–14:30（瓶颈 part2）收尾**。
- 参照组 qKeep=0.70 目录 288 文件齐备，最终可直接并排比对。

## 12:25 qKeep080 完成 + 两组配对对比
- **288/288 完成**，06:47→12:25，约 5.6 h。崩溃恢复强度：part1 1 次、part4 1 次、**part3 6 次、part2 9 次**，全部自动续跑，零丢数据。
- 核验 `verify_pruned_run.py --runs 18 --expect-fe 300` → **COMPLETE**（mat 288 / valid 288 / missing 0），另出 `_verify_summary.xlsx`。
- 新增可复用脚本 `Experiments/REMO_new2_AdaMaO_UniformMix_Pruned_FullSeries/compare_qkeep.py`：**配对** Wilcoxon 符号秩（两组同种子、run 号一一对应 ⇒ 配对设计，比独立样本更有效）+ Holm 校正 + 逐问题胜负计数 + 可选 xlsx/csv。
- 对比结论（A = qKeep0.70，B = qKeep0.80，rel% = (B−A)/A）：

| 问题 | rel% | 更好 | p_paired | p_holm |
|---|---:|---|---:|---:|
| WFG6 | −2.00 | q080 | 0.0019 | **0.0309 ✓显著** |
| DTLZ5 | −7.01 | q080 | 0.1084 | 1.0000 |
| WFG6 之外 | — | — | — | — |

  - 全 16 题 **8 胜 8 负**；Holm 后唯一显著的是 **WFG6（q080 好 2.0%）**。DTLZ5 有 −7.0% 趋势（p=0.108）、WFG9 +5.2%（p=0.067）、WFG4 +4.9%，均未过校正。
  - **结论：qKeep 0.70 vs 0.80 在 16 题上没有系统性差异** → 可排除 qKeep 是 Pruned 退化的主因。这与 09-11 的诊断（"持续质量约束是第一嫌疑、qKeep/模糊度奖励次之"）一致，现在有了全系列 16 题的干净证据。
- 产物：`_compare_q070_vs_q080.xlsx` / `.csv`（放在 qKeep080 数据目录，`_` 前缀）。
- 巡检自动化 `Pruned_qKeep080实验巡检`（aba96248…）**已删除**。两组数据各 288 文件、互不覆盖。

## EMaO 知识点总结文档（16:03）
- 应用户要求网络检索并整理《昂贵超多目标优化_知识点总结.md》，存放 `论文写作/`（与已有写作指南同级）。
- 内容：定义/四挑战、SAEA 工作流、三大结合范式（回归近似/聚合近似/分类关系）、infill 准则、代表算法谱系表（ParEGO→CSEA→KTA2→REMO→扩散代理）、基准与指标（IGD/HV/Wilcoxon-Holm 规范）、未来方向、与 PACDIS 的关联定位 + 文献清单。
- 论文写作可复用点：Related Work 按"回归→聚合→分类/关系"谱系写；标准对照基线 = K-RVEA/CSEA/KTA2（PlatEMO 易得）+ MCEA/D（高维决策空间）；四挑战出处 = KTA2/DASAEA。
