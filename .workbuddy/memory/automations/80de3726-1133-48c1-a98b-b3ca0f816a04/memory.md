# 自动化执行记录：HES_EA_N100 M10/M15 巡检

> 只记高层结论，详细数据见当日日志 `../../2026-09-19.md` 与 `REFERENCE.md`「HES_EA_N100 扫描」。

## 2026-09-19 15:00（首次执行）
- **结论**：M10 = 94/320（**停滞**，非运行缓慢）；M15 = 0/320（未启动，链式看门狗死等）。两阶段都未完成 → 未跑 MATLAB 校验。
- **关键判定证据**：数据目录最后 .mat = 12:18:38，driver.log 末行 = 12:18:46，此后 2h41m 零产出；但 13 个 MATLAB 进程仍活着且各占用一个核（30 s 采样 CPU 增量 ≈29 s）。12 个切片进程自 11:10–12:18 启动后从未退出（正常切片 38–47 min），卡在 DTLZ2/3/5/6、WFG1/2/3 的部分 run 上。
- **连带影响**：`chain_to_M15.sh` 需 MATLAB 进程数归零才拉起 M15 → 只要卡死进程在，**M15 永远不会自动开始**（不是"M10 完成 30 分钟内未开始"的情形）。
- **同一病理在 M15 复现**：M15 冒烟 `WFG3` 13:42 起卡到现在；同批 `DTLZ2` 冒烟正常（285 s/264 s）。指向**运行级死循环**（疑似 `pdist2('cosine')` 退化 → KMeans 不收敛）。
- **未做的动作**（巡检约定只读）：未重启、未杀进程、未启动 M15、未改代码。
- **建议下一步**：先定位死循环根因再重跑，否则会在同一批 run 上复卡；如决定强行续跑，顺序必须是「停 chain_to_M15.sh → 停 M10 driver → 杀 12 个卡死 MATLAB → 后台重启 M10 driver」。
- **复用要点**：PowerShell 工具 stdout 不回显 → 采样结果写临时文件再读；`Get-CimInstance Win32_Process` 的 `CommandLine` 可直接看出每个 worker 卡在哪一题哪几个 run。
