# 2026-07-10 工作记录

## REMO_new2_AdaMaO 教学任务启动
- 用户已掌握 REMO 与 PIEA 基础，要求手把手教 REMO_new2_AdaMaO。
- 已通读源码与文档：REMO_new2_AdaMaO.m、算法详解.md、组会汇报稿.md、AdaMaOSelection.m、PIEAOnlySelection 消融详解.md。
- 已建教学文件夹：`D:\PlatEMO-master\REMO_new2_AdaMao\笔记\`
- 已产出：`00_教学大纲.md`（11 讲模块拆解 + 继承链）、`01_算法总览与定位.md`（模块0：主链4步、两开关一传感器、借来vs自创）。
- 关键认知：AdaMaO 的 novelty 在「运行时诊断 + 动态策略切换」；PIEA 指标子系统是借来的、消融证明可删（推荐 Lite 版投稿）。
- 下一步：按大纲逐讲产出笔记，已建 11 个任务跟踪。

## 简化版(Simple) 效果评估 + 超参数回退建议
- 新数据：`Desktop\AdaMao实验表\消融实验\简化参数\`（M=10 / M=20 两表，新增 `REMO_new2_AdaMaO_Simple` 列）。
- 结论：M=10 Simple 显著差于 Full（几何均值 S/F=1.100，符号检验 p≈0.021，13/16题更差）；M=20 基本持平（1.030，p≈0.455）。最差单题 DTLZ7（断开PF）仍远好于 PIEAOnly/FixedInd，guarded 机制兜底有效。
- 根因（对照源码）：k 内生化=等价(Full 本来就是 1.5*M)；w_min/q_keep/n_min-n_max 内生化低风险；**元凶=① gmax 3000→700(砍4.3×) ② 删除关系对三模式切换(curriculum/weighted/conservative)→常开软权重**；model_gain 替代探索权重为中嫌疑。
- 关键推论：M=10 指标 dormant 故 Full≈Lite，但 Simple<Lite → 丢的关系模式/参数 > 留指标的增益；M=20 指标激活补足亏损。
- 建议：保留 k/w_min/q_keep/n_min-n_max 内生化；回退 gmax、关系模式切换(tau_err+阈值)、lambda0 探索权重 → 得 "Simple v2"（参数 10→4）。投稿前做逐步回退消融(A/B/C/D)分离元凶。
- 交付：`简化版效果评估与超参数回退建议.md`、`简化版退化热力图.html`。

## 创建 Simple-A 消融变体（仅恢复 gmax=3000）
- 复制 `REMO_new2_AdaMaO_Simple` → `REMO_new2_AdaMaO_SimpleA`；主类改名 `REMO_new2_AdaMaO_SimpleA`。
- 唯一改动：`wmax = Problem.N*ceil(log2(N+1))`（Simple 中=700）恢复为 `wmax = 3000`（完整版），验证 gmax 是否元凶。
- 其余与 Simple 完全一致（2 外部参数、k 内生化、软置信权重、coverage_gap 连续控制、指标子系统保留）。
- 附 `REMO_new2_AdaMaO_SimpleA_说明.md`（消融目的+解读口径）。
- 用户明确：只创建算法，不代跑实验。

## 笔记/文档归档约定（项目级）
- 分析笔记、评估报告、热力图等**统一放进对应算法文件夹下的 `notes/` 子目录**，不要放 PlatEMO 总目录（D:\PlatEMO-master 根）。
- 已完成：把 Simple 的 3 份资料（变更说明 + 效果评估 + 退化热力图）移入 `REMO_new2_AdaMaO_Simple/notes/`。
- 后续各算法（Simple/SimpleA/Full 等）的分析文档都遵循此规。
