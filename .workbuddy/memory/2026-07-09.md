# 2026-07-09 指标模式消融 M=20 维度分析

## 任务
用户新增 20 目标（M=20）消融 5 表（`C:\Users\lsx\Desktop\AdaMao实验表\消融实验\指标模式\二十目标\`），要求结合此前 M=10 深入分析应保留哪个版本。

## 关键结论（推翻前一版"倾向 Lite"）
- **保留 Full（`REMO_new2_AdaMaO`，退化度≥0.45 触发式 guarded 指标模式）**。
- M=10：Full 与 Lite 并列（平均秩均 3.06，Friedman p=0.844）；删指标不亏。
- M=20：Full 平均秩 2.75（5 变体第1），Lite 跌到 3.19；#best Full=5（M=10 仅 3）。
- 全场 11 算法同场平均秩 M=20：**Full 4.06 排第1**（超过 PIEA 4.50、PIEAOnly 4.56、Lite 5.03）；M=10 时 Full/Lite 仅并列第4（4.81）。
- Full 是唯一随 M 升高而变强的变体 → 指标模块价值恰在高维显现（验证最初假设，此前 M=10 触发不到看不出）。
- DTLZ7（断开PF）崩塌 persists：M=20 Full=18.9 / Lite=20.2 / PIEAOnly=34.8 / FixedInd=37.3 → 无闸门变体 ~1.85-2× 崩，Full guarded 兜住。
- Friedman 单维度仍不显著（M=10=0.844, M=20=0.951），但维度扫描方向性明确，作决策依据。

## 决策含义
- 主投算法定型 = Full（guarded 指标模式），方法章节定位为"degeneracy-triggered indicator-assisted selection"而非"搬用 PIEA"，回应原创性质疑。
- 红线：不保留 PIEAOnlySelection / FixedIndicatorAlways（崩塌）。
- 收尾：补 HV、M=15、Wilcoxon（Full vs Lite / PIEAOnly，重点 DTLZ1/3/7）、可复现声明。

## 产出
- `指标模式模块去留_最终建议_M10+M20.md`（取代仅 M=10 的旧决策文档，含 11 算法排名、逐问题、组会话术）。
- `指标模式消融_热力图_M10vsM20.html`（16 题 × 5 变体，M10/M20 并排，行内归一化，黄底=Full 夺冠）。
- 复算脚本 `analyze_m20.py`（解析双文件夹、Friedman+平均秩+逐问题+11算法排名）、`make_heatmap_m20.py`。

## 逐模块讲解（投 Swarm）：混合 PBI
- 用户要求逐模块讲解算法，先讲混合 PBI（HybridPBI_Classification），讲清原理+动机+可写故事点+配图。
- 读透 `HybridPBI_Classification.m`、`GetOutput_PBI.m`、`AdaptiveReferenceVectors`、已有 HPC 讲解文档。
- 用 show_widget 出 3 张 SVG 图：①PBI 几何(d1/d2) ②双信号融合数据流 ③均匀vs自适应参考向量。
- 讲解结构：干什么(贴好/坏标签供关系学习)→PBI直觉→两套信号(全局score_v+局部label_dyn)+α融合+置信度→自适应参考向量→动机表→Swarm故事点风险评级。
- 故事点结论：SP1混合PBI标注(★★★但实现两套公式不一致需先统一)、SP2自适应参考向量(★★★最干净、需固定种子+UniformOnly消融)、SP3线性α融合(★别当贡献)、SP4置信度(★★需验证与正确率正相关)。
- 产出 `REMO_new2_AdaMaO_模块讲解_混合PBI.md`（含3图引用+动机表+故事点风险表+代码七步对照）。

## 生成超参数削减版 REMO_new2_AdaMaO_Simple（新算法）
- 用户决定：保留完整版（含指标子系统+degeneracy≥0.45 触发），但削减超参以便发文章。
- 依据 `AdaMaO_简化与超参数削减建议.md`，按用户拍板的三点生成新算法（**不动原版本**）：
  1. diversity acquisition：保留 0.75/0.25 固定内部权重（仅去 n_min/n_max 依赖）。
  2. use_indicator：保留为参数（默认1），方便开/关消融。
  3. 命名：REMO_new2_AdaMaO_Simple。
- 改动：外部参数 10→2（`[use_indicator, debug]`）；8 个外部超参内生化（k/gmax/q_keep/lambda0/w_min/n_min/n_max/tau_err）；第五章前四隐藏参数：①关系训练始终软置信权重(删 curriculum/weighted/conservative 切换) ②coverage<0.60→coverage_gap 连续 ③p_err/0.45→多数类基线 model_gain ④diversity 保留0.75/0.25。
- 未动：指标子系统、degeneracy≥0.45、theta=5、GetOutput_PBI区间、N/4、K-means、N设置（保持与 M=10/M=20 实验可比）。
- 实现：复制原文件夹→删 5 个变体主文件+构建产物→写新主文件/AdaMaOSelection.m(改签名+select_explore)/GetRelationPairs_confidence.m(权重改0.5+0.5*sqrt)。
- 验证：MATLAB pcode 编译 18 个 .m 全通过（沙箱 checkcode 因 Java 编辑器服务故障不可用，改用 pcode）。
- 产出：新文件夹 `REMO_new2_AdaMaO_Simple/`(18 .m) + `REMO_new2_AdaMaO_Simple_变更说明.md`。
