# 2026-09-13 工作日志

## 巡检快照（自动化 c6bae755，17:33）
- 4 分区 16:29:54 启动，零报错；进度 54/270（WFG1 30 完成、WFG2 24/30），吞吐 ≈0.86 跑/min，ETA ≈21:45。

## Weighted WFG M=15 全系列 30 跑（本会话核心任务）
- 需求：`REMO_new2_AdaMaO_SDEOnly_UniformMix_Pruned_Weighted` 跑 WFG1–9 × 30 跑，M=15/D=30/N=100/maxFE=300，结果放 `D:\REMOandDREMO测试集\15目标`。
- 考古发现：inventory（昨日 23:05）显示 Weighted M=15 existing=0 → 本次正是填洞；老基线 15目标 各算法 480 文件（16题×30跑）完好。
- **C 盘数据丢失事件**：`RunPaperWeighted30.m` / `RunWeightedM20_18.m` 的数据根 `C:/Users/lsx/Desktop/REMOandDREMO测试集` 已不存在（C 盘清理，清理清单未记录该目录）。丢失：C 盘独有的 KRVEA_100 / PCSAEA_N100（M=10/15/20 各 288=16×18）、Weighted M20 288、09-12 晚 23:05 启动的 498/2208 补跑（M=10 部分）。RUNNING.lock 残留于 `...Weighted/diagnostics/paper_30runs/`。→ 已在最终回复中提醒用户，待用户决定是否重建。
- 部署：`Experiments/REMO_new2_AdaMaO_Weighted_WFG_M15/`（run_WeightedWFG_M15.m + verify_weighted_wfg_m15.py + README.md）。
  - 口径与 RunPaperWeighted30 完全一致：种子 `20260912+15*100000+p*1000+r`（WFG1→8..WFG9→16）、modeRunId=1、save=30、参数 {3000,0.50,0.25,0.70,6}；每 .mat 带 metadata（seed/rngBeforeSolve/起止时间），后续可被 inspectResult 认作 metadata-checked。
  - M=15 下 WFG2/3 无奇偶提升（L=16 偶）→ 全部 `_M15_D30_`，与老基线一致（双向实测验证）。
  - supervise_part.sh 再泛化：新增 FUNC/OUTDIR/SCRATCHROOT 环境变量（默认值保持旧行为）。
- 踩坑：① harness 首版漏了 `ALG.CalMetric('IGD')`（Solve 后 metric 无 IGD 字段即崩）——smoke 抓住并修复；② Edit 工具两次报成功但未落盘（EBUSY 同源），关键编辑后必须 Grep 复核，.m 行插入改用 sed；③ venv python 实际路径 `.../envs/default/Scripts/python.exe`（Windows 布局，记忆里的 bin/ 是错的）。
- Smoke：WFG2 M15 单跑 wall 184.8s，全路径（含 verify 校验 seed/快照/IGD）通过 → 270 跑 4 分区预计 wall ≈4.5h（区间 4–5.5h）。
- 执行：4 分区后台启动（16:29–16:30），每分区 MATLAB maxNumCompThreads(4)，WFG2#1 由 smoke 产物复用。挂每小时巡检自动化（id c6bae755-4b1b-4680-b35b-095e8cd3b604，完成后自动 verify + 自删）。

## 暂停（18:52，用户指令"帮我暂停并保存"）
- TaskStop 终止 4 个守护任务（进程树整体清理：MATLAB 引擎 0 残留、tmp.mat 0 个），无需 taskkill。
- 已保存 **126/270**（WFG1–4 完整 30/30，WFG5 6/30，WFG6–9 未开始），verify 126/126 全部有效。唯一损失 = 中断时在跑的 4 个 run（WFG5 r7–10 附近）。
- 中间结果（最终 IGD 均值）：WFG1 4.122、WFG2 3.322、WFG3 2.296、WFG4 13.977、WFG5(n=6) 12.751。
- 巡检自动化 c6bae755 已同步 PAUSED；续跑指南落盘 `Experiments/REMO_new2_AdaMaO_Weighted_WFG_M15/RESUME.md`（补 144 跑 ≈ 2.4h，harness 自动跳过已有文件）。

## 定时接力链（20:40–21:05 部署，14 日 00:00 触发）
- 用户需求：14 日 0 点继续未完成的 M=15；跑完后接 **M=8/D=10** 全系列（其他参数一样），数据放 `D:\REMOandDREMO测试集\8目标\n10\<算法名>\`（该目录已有老基线 KRVEA/PCSAEA/REMO）。
- ⚠️ **schtasks.exe 在 WorkBuddy 沙箱程序黑名单里，被拦截且不可绕过** → Windows 计划任务方案放弃，改用平台自动化：T1 一次性（id 8cef4702，14日00:00，启动链并全程守候）+ T2 每小时（c6bae755 改造，validFrom 14日00:30 / validUntil 15日12:00，监控 + 兜底重启 + 完工自删）。链日志 `tmp/weighted_wfg_m15/chain_20260914_0000.log`。
- 链脚本 `tmp/weighted_wfg_m15/resume_chain.sh`：阶段1 M=15 续跑（4 分区）→ 阶段2 M=8/D=10（4 分区）→ 两目录 verify（--m 15 / --m 8）→ "chain COMPLETE"。**带 mkdir 锁 + 10 分钟心跳**（活链不误判、死锁 30 分钟后可接管；重复启动自动退出）。
- 阶段2 落地：`run_WeightedWFG_M8D10.m` 薄包装（只覆盖 M=8/D=10/OutputRoot，其余继承 M15 harness）；种子族扩展 `20260912 + 8*100000 + p*1000 + r`（M*1e5 项天然隔离各 M）；verify 脚本加 `--m` 参数。Smoke 通过：WFG2 M=8 → 实际 D=11（文件 `_M8_D11_`，与 M=10 同规则）、seed 21069913、**单跑仅 101s**（D=10 显著快于 M15 的 185s）→ 阶段2 270 跑约 2.5h，全链预计 00:00→~05:00。
- 算法族细节：D≤10 时 Weighted 内置初始 LHS N=11D−1=109（WFG2/3 因 D 升 11 走 N=100），与 REMO 家族同规则，非参数可调。
