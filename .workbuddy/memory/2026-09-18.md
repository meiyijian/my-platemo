# 2026-09-18 工作日志

## 00:50–02:50 交互分支消融：删批内距离项的新算法类 + M10/M20 全系列跑
### 需求（用户原话要点）
- "把我当前 lambdat030 版本的探索分支的批次距离分数给删除，不是有个 0.75 和 0.25 两个参数吗，现在把 0.25 后面的批次分数删除，但是模糊度分数别删除"。
- 创建新算法跑实验：**10 目标 30 维、一样的参数、WFG+DTLZ 全系列、每问题 20 次**，数据存 `D:\REMOandDREMO测试集`；随后追加"跑完继续跑 20 目标"。

### 新算法类（受 git 管理）
`Algorithms/Multi-objective optimization/REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist/`
- 唯一功能改动：探索分支批构造由 `0.75*A_t~ + 0.25*d~` 改为**纯 `A_t` 排序**（0.25 批内距离项删除）；因重新归一化单调，批 = 保留集合按 `A_t` 降序前 target 个，**全程不算距离**；0.30 模糊度奖励保留。
- 逐字节审计：类文件与基类差异仅 3 处（类名、诊断全局名 `ADAMAO_NOBATCHDIST_DIAG`、诊断字段加 `distWeight=0.00`）；`private/` 11 文件**只有 `DiversifiedInfillSelection.m` 不同**（改调 `NoBatchDistWeightedBatchSelection` + 全局改名）；其余文件与基类逐字节相同。公开参数仍 5 个 `{3000,0.50,0.25,0.70,6}`。
- ✅ **改动生效已证明**：同种子同预算（DTLZ2、M=10、D=30、N=100、maxFE=160、gmax=100，两进程各跑一类）——首个 IGD 快照相同（1.72526），终态 **基类 1.54566 vs 新类 1.47996**，轨迹分岔。

### 实验框架（`Experiments/REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist_M10|M20/`）
- `run_Lambdat030_NoBatchDist_M{10,20}.m`：复用共享 harness `run_UniformMixPrunedFullSeries`；16 题（DTLZ1-7、WFG1-9）× 20 跑，N=100、D=30、maxFE=300、SaveCount=30、参数同上；种子 `SeedBase + 1000*题号 + run`，M=10 用 21260912、M=20 用 22260912。
- 数据：`D:\REMOandDREMO测试集\10目标\n30\<类名>` / `D:\REMOandDREMO测试集\20目标\<类名>`。
- `driver.sh`：**分块切片（每片 10 跑，共 32 片）** + 12 路池 + 25 s 错峰 + 外层 8 轮"只补缺失"重试 + 收尾跑 `verify_*`；`missing_runs.py` 输出缺失块。
- `chain_to_M20.sh`：M=10 完成后自动启动 M=20 驱动。

### 运行
- M=10：01:02 启动；01:26 33/320、02:40 170/320；单跑 370–395 s；稳态 1.9 跑/分；**ETA ≈ 04:05**。
- M=20：框架就绪，等接力；单跑成本与 M=10 基本同（房规）。

### 踩坑（两条，都值得长期记）
- 🔴 **POSIX 路径传给 Windows 程序会变成 `d:\d\...`**：`python.exe /d/...` 报 `can't open file 'd:\d\...'`——driver 里所有交给 python.exe / matlab.exe 的路径（脚本路径、`-logfile`）必须用 `D:\...` 形式（与 `pwd -W` 同源问题）。首次因此让驱动"0 缺失即结束"秒退。
- 🔴 **接力看门不能只 grep 日志里的完成标记**：`driver.log` 里还留着 00:59 那次中止启动写的 `DRIVER_DONE present=0`，导致看门在 M=10 只有 170/320 时就启动了 M=20（两臂抢 CPU）。**修法：完成判定 = "MATLAB 池连续两次为空" 且（有 DRIVER_DONE 或 数据 320 齐）**；已停 M=20 驱动（其数据集 0 文件，无污染），M=10 未受影响。
- 另记：`run_UniformMixPrunedFullSeries(part,nParts,...)` 传单题列表时必须 `part=1,nParts=1`（传 2/16 会得到空切片）。

## 04:10 M=10 全系列完成（320/320，零重试一次跑齐）
- 04:10:24 池子跑完即 320/320，**attempt 2 直接"无缺失"结束，整轮零重试**（当夜未遇堆损坏）；04:11:48 `DRIVER_DONE present=320`。
- `verify_Lambdat030_NoBatchDist_M10`：**OK=320 BAD=0**，`result` 30×2 / FE=300 / IGD 全有限；逐题 IGD 均值与平均单跑耗时写入 `summary_Lambdat030_NoBatchDist_M10.csv`（单跑 287–417 s）。
- 数据：`D:\REMOandDREMO测试集\10目标\n30\REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist`（320 MAT）。
- 04:14 接力看门按新条件放行，**M=20 驱动已自动开跑**（种子基 22260912，数据落 `20目标\<类名>`）；ETA ≈ 07:30。

### 消融对照（M=10，6 题可直接配对：`_pMix050` = 同参数原始臂，同种子/同协议/同 threads=1，n=20）
| 问题 | 基类（含 0.25 距离项） | NoBatchDist | Δ | 相对 | 胜/负 | Wilcoxon p |
|---|---:|---:|---:|---:|---|---:|
| DTLZ2 | 1.1224 | 1.0669 | −0.0554 | **−4.94%** | 13/7 | 0.053 |
| DTLZ4 | 1.2515 | 1.1287 | −0.1228 | **−9.81%** | 17/3 | **0.0001** |
| DTLZ7 | 10.6024 | 12.2159 | +1.6136 | **+15.22%** | 5/15 | **0.0009** |
| WFG1 | 3.1815 | 3.1932 | +0.0116 | +0.37% | 10/10 | 0.729 |
| WFG3 | 1.6900 | 1.6859 | −0.0041 | −0.24% | 8/12 | 0.927 |
| WFG8 | 5.7184 | 5.6693 | −0.0491 | −0.86% | 11/9 | 0.498 |
- **结论口径**：删距离项的效果**强烈依赖问题**——DTLZ2/DTLZ4 显著受益（DTLZ4 −9.81%, p=1e-4），DTLZ7 **显著受损（+15.22%, p=9e-4）**，WFG 三题中性。不是全局改进，属"问题相关"的取舍。
- 未覆盖：WFG 其余 7 题在 pMix 数据集中没有同参数原始臂；M=20 的旧基线是 threads=2 跑的，**不可与 threads=1 新臂同表比较**（红线）。若要全 16 题对比需补跑原始臂。

## 07:08 M=20 全系列完成（320/320，同样零重试）→ 两臂收官
- M=20：04:14 由接力自动开跑 → 07:08 池子跑完即 320/320，attempt 2"无缺失"结束，**整轮零重试**；`verify_..._M20`：**OK=320 BAD=0**。
- 数据：`D:\REMOandDREMO测试集\10目标\n30\`、`20目标\` 下的 `REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist` 各 320 MAT（合计 640 跑，全部由本机 12 路×threads=1 错峰池跑出）。
- 两臂耗时：M=10 01:02→04:10（3h08m）、M=20 04:14→07:08（2h54m）。M=20 单跑与 M=10 基本同价（房规再次验证）。

### 新算法（NoBatchDist）逐题终态 IGD 均值（20 跑）
| 问题 | M=10 | M=20 | | 问题 | M=10 | M=20 |
|---|---:|---:|---|---|---:|---:|
| DTLZ1 | 208.65 | 71.58 | | WFG1 | 3.1932 | 5.6092 |
| DTLZ2 | 1.0669 | 1.1207 | | WFG2 | 2.0103 | 4.8619 |
| DTLZ3 | 883.42 | 296.43 | | WFG3 | 1.6859 | 2.7425 |
| DTLZ4 | 1.1287 | 1.0473 | | WFG4 | 6.5724 | 23.9986 |
| DTLZ5 | 0.5915 | 0.2991 | | WFG5 | 5.9195 | 22.6384 |
| DTLZ6 | 15.2098 | 7.0594 | | WFG6 | 5.3140 | 19.3838 |
| DTLZ7 | 12.2159 | 22.9996 | | WFG7 | 5.3620 | 20.4670 |
| | | | | WFG8 | 5.6693 | 20.7998 |
| | | | | WFG9 | 6.2431 | 23.3171 |

- 归档：新类 + 两套实验框架（runner/driver/missing_runs/verify/chain + 逐跑 IGD 日志 + summary CSV）已入库推送，commit **4c39ab9**（128 文件）；原始 640 个 MAT 不入库（在 `D:\REMOandDREMO测试集`）。已删两目录的冗余 MATLAB `-logfile` 日志（p*_a*.log），保留 driver/verify/part*/chain 日志。
- 待办（若要论文级全题对比）：补跑原始臂 M=10 全 16 题 + M=20（threads=1），否则只能报 6 题（M=10）的配对消融。

## 13:12 追加：M=15 全系列（NoBatchDist）已启动
- 新目录 `Experiments/REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist_M15/`（runner/driver/missing_runs/verify，与 M10/M20 同构）。
- 协议：M=15、D=30、N=100、maxFE=300、参数 `{3000,0.50,0.25,0.70,6}`、16 题 × 20 跑 = 320；**种子基 21760912**（=20260912+15×100000，与 15 目标既有数据一致——该目录是扁平结构 `15目标\<类名>\`，无 n30 层）。
- 数据：`D:\REMOandDREMO测试集\15目标\REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist`。13:13 首片开跑（M=15/D=30 确认无误），ETA ≈ 16:20。
- 📌 **15 目标现只有 λ0.50 的 Weighted 基线**（`REMO_new2_AdaMaO_SDEOnly_UniformMix_Pruned_Weighted`，270 文件），**没有 Lambdat030 基类** → 想在 M=15 做配对消融，还得补跑基类 320 跑。

## 15:13 追加需求：M=15 的 MAT 要含 IGD+（其他目标不动）
- 方案：**不改正在跑的 harness**（否则半程文件会"有 IGDp / 无 IGDp"口径不一），改为跑完后对全部 320 个 M=15 MAT 做一次性合并 —— 新脚本 `Experiments/..._NoBatchDist_M15/merge_IGDp_M15.m`：
  - 从 MAT 里存的 30 个快照种群用 PlatEMO 的 `PROB.CalMetric('IGDp',Pop)` **重算 IGD+ 轨迹**（与 harness 算 IGD 同一机制），写回 `metric.IGDp`；
  - 安全规则沿用房规 `MergeIGDpIntoRaw.m`：只 load/append `metric`（不碰 result）、写后回读校验、已匹配则跳过（幂等）、单进程串行（避免并发写触发堆损坏）；
  - 交叉校验：**首/末快照**用同法重算 IGD 与存盘 `metric.IGD` 比对（全 30 个都算会让耗时翻倍，位置型映射抽检边界已足够）；
  - 产物：`IGDp_M15_perrun.csv`（逐跑首/末 IGDp + 校验残差）。
- ✅ 试跑通过：3 文件 = 1 写入 + 2 跳过（幂等生效）；写入文件 `metric` 字段 = `{IGD, IGDp, runtime}`，IGD 尾 272.5446 vs IGDp 尾 272.5445（量级合理，DTLZ1 M=15）。
- 作用域**仅 15 目标**；M=10/M=20 的 640 个 MAT 保持原样（用户明确"其他目标的暂时不写"）。
- 注意：既有 `PlatEMO-master/.workbuddy/run_scripts/MergeIGDpIntoRaw.m` 是从"预算好的 IGDplus 产物目录"合并，本臂没有该产物，故改为直接从快照重算。

## 16:14 M=15 收官 + IGD+ 已写入全部 320 个 MAT
- M=15（13:13→16:14，3h01m）：**320/320 落盘、校验 OK=320/BAD=0、零重试**。数据 `D:\REMOandDREMO测试集\15目标\REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist`。
- IGD+ 合并（merge_IGDp_M15）跑完：**320/320 文件 metric = {IGD, IGDp, runtime}**，逐跑表 `IGDp_M15_perrun.csv`；M=10/M=20 未动。
- 逐题均值（20 跑）：

| 问题 | IGD | IGD+ | | 问题 | IGD | IGD+ |
|---|---:|---:|---|---|---:|---:|
| DTLZ1 | 140.7549 | 140.7548 | | WFG1 | 4.1429 | 3.5753 |
| DTLZ2 | 1.0694 | 0.8177 | | WFG2 | 3.4985 | 3.0194 |
| DTLZ3 | 592.5373 | 592.5371 | | WFG3 | 2.2501 | 2.0573 |
| DTLZ4 | 1.1068 | 0.7921 | | WFG4 | 13.6777 | 11.8316 |
| DTLZ5 | 0.4482 | 0.4175 | | WFG5 | 12.8277 | 11.2868 |
| DTLZ6 | 11.0738 | 11.0661 | | WFG6 | 10.3211 | 8.0913 |
| DTLZ7 | 18.6240 | 18.5110 | | WFG7 | 11.5131 | 9.6878 |
| | | | | WFG8 | 11.6779 | 9.9794 |
| | | | | WFG9 | 13.3353 | 11.8437 |

- 归档：commit **2dca9e3**（M15 框架 + merge 脚本 + summary/IGDp CSV + 逐跑日志，72 文件）。
- 📊 **三臂进度总览**：NoBatchDist 已有 M=10（320）、M=15（320+IGDp）、M=20（320）= **960 跑**；仍缺"基类 Lambdat030 臂"（M=15/M=20 完全没有，M=10 只有 6 题）。

## 16:55–17:12 M=20 的 IGD+ 补写（用户中途叫停 → 断点保留）
- 用户要求把 IGD+ 也写进 20 目标的 MAT。新建 `Experiments/..._NoBatchDist_M20/merge_IGDp_M20.m`（与 M15 版同法：只 append `metric`、写后回读校验、首末快照抽检 IGD、幂等跳过、单进程串行）。
- 17:12 用户"暂停任务" → 已 TaskStop（11m14s），**暂停点 123/320**；MATLAB 归零、无损坏（每个写入文件都当场回读校验过）。
- 恢复方式：重发同一条命令 `matlab -batch "... merge_IGDp_M20;"` 即从断点续（已完成的 123 个会跳过）。
- ⚠️ 速度观察：M=20 的 IGD+ 重算明显慢于 M=15（123 文件/11 min ≈ 11 文件/min，M15 是 ~55 文件/min）→ 剩余 197 文件预计还需 **~18 min**。原因待查（可能 M=20 的 IGDp 参考集/非支配判断更重）。
- 其余目标（M=10）**仍未写 IGD+**，等用户发话。

## 22:00 论文 Introduction 正文写入（PACDIS / HPDC-MaOEA.tex）
- 用户提供 Introduction 成品文本，要求写入 `论文写作/HPDC-MaOEA.tex` 并提交推送。
- 动作：替换 `\section{Introduction}` 下的 `\TODO{}` 四段式纲要 + 原有两段草稿（REMO/PAQC/CDIS 雏形），写入完整五段结构：
  1. 昂贵 MaOP 背景 → SAEA 目标级回归在 M 大时失效 → 关系学习替代（引 ref:remo）；
  2. REMO 用 RSEA 径向空间代表解建组 → 代表集稀疏、可能低估原目标空间方向信息 → **PAQC**（PBI 直接由当前非支配解构造方向，融合代表式二元标签，保留 REMO 三类关系学习，含阶段依赖融合）；
  3. 关系分选批可能聚集 → **CDIS**（关系质量筛选 + 探索/指标双准则，定位为支撑性机制）；
  4. 集成成 PACDIS + 三条贡献 + 结果摘要；
  5. 全文组织（sec:related/method/exp/conclusion 五个 \ref）。
- **事实核对（用权威快照 `experiments/main_performance/igd_snapshot.csv` 复核）**：
  - 引言称 "4, 2, 3 of the 16" lowest mean → ✅ 实测 9/48 格 = M10:4 / M15:2 / M20:3（WFG1/WFG2@M10、WFG6@M20，加 WFG7/WFG8 三档全胜）。
  - 引言称 "consistent advantages on WFG7 and WFG8 across all three objective counts" → ✅ 实测 WFG7、WFG8 在 M=10/15/20 全部 6 格均为 PACDIS 最低均值。
  - 引言称 "PIEA remains stronger on several DTLZ instances" → ✅ 实测 PIEA 均值更低题数 M10=9/16、M15=8/16、M20=8/16；且 DTLZ5/DTLZ6 三档全输 PIEA，与实验节第 915-918 行一致。
  - 措辞为 "competitive overall performance … does not establish overall superiority"，与实验节保守口径一致（未越界声称全面占优）。
- 编译：`env -u LC_ALL -u LANG latexmk -pdf HPDC-MaOEA.tex` → **14 页、成功**；undefined reference 0、Overfull 0。
  - ⚠️ 本次踩坑：MiKTeX 找不到 perl（`could not find the script engine 'perl'`）→ 需把 `/c/Strawberry/c/bin:/c/Strawberry/perl/bin` 一并加进 PATH（原先只加了 miktex/bin/x64）。
- 归档：commit **f383c3d**（+71/-31，仅 `论文写作/HPDC-MaOEA.tex`），已推送；`git ls-remote origin master` 确认远端 = f383c3d。
- 🔧 顺手修掉 `refs/remotes/origin/master` 的 `[gone]`（按老办法直接文件写入远端 SHA），`git status -sb` 恢复 `## master...origin/master`。

## 23:39–24:00 新增 M=20 消融实验：REMO_noBatchDict_noCDIS / _noPAQC（640 跑，已启动）
- 用户要求：把这两个新变体在 DTLZ1-7 + WFG1-9（16 题）、M=20、D=30、N=100、maxFE=300 上跑完，数据存 `D:\REMOandDREMO测试集\20目标`；**跑之前先确认 k 是否为 1.5M**。
- ✅ **k 结论：两个算法本来就已经是 1.5M，未做任何改动**。源码 `k_eff = min(Problem.N,max(6,ceil(1.5*Problem.M)))`（noCDIS 第 44 行 / noPAQC 第 55 行），与 REMO 家族其它成员（Lambdat030 / NoBatchDist / pMix*）完全同口径。
  - **运行时实测**（`probe_M20_k.m`，在真实 Problem 对象上求该表达式）：**16/16 题 k=30 = 1.5×20 精确成立**；D=30>10 → N=100 → min(100,30)=30。
  - ⚠️ 顺带确认：**WFG2/WFG3 在 M=20 时真实 D=31**（其余 14 题 D=30），MAT 文件名用真实 D。
- 新增框架 `PlatEMO/Experiments/REMO_noBatchDict_Ablation_M20/`：`run_noCDIS_M20.m`（参数序 `{gmax,rGood,qKeep,nMax}`=`{3000,0.25,0.70,6}`）、`run_noPAQC_M20.m`（`{gmax,pMix,qKeep,nMax}`=`{3000,0.50,0.70,6}`）、`driver.sh`、`missing_runs.py`、`verify_noBatchDict_Ablation_M20.m`、`probe_M20_k.m`、`smoke_Ablation_M20.m`；复用 harness `Experiments/REMO_new2_AdaMaO_UniformMix_Pruned_FullSeries/run_UniformMixPrunedFullSeries.m`。
- **种子口径与既有 20 目标数据集完全一致**：SeedBase=22260912（=20260912+20*100000），`base+1000*题号+run` → 与 Lambdat030 / NoBatchDist / pMix000..100 **逐跑配对**，可直接做配对检验。SaveCount=30。
- 规模：2 算法 × 16 题 × **20 跑** = 640 跑；切片 10 跑/片 = 64 片；MAXJOBS=12、threads=1、rounds=10（外层自动补缺）→ 预计 **~6.5–7.5 h**。
- 冒烟实测（4 路并行、threads=1）：noCDIS ≈ 318 s/跑（DTLZ2 334/308、WFG3 318/311），noPAQC ≈ 355 s/跑（DTLZ2 343/360、WFG3 363）。两者都能正常产出 `result`+`metric`，`ok` 校验通过。
- 🔴 **踩坑（harness 语义）**：`run_UniformMixPrunedFullSeries` 的 `'Runs'` 若是**标量**会被展开成 `1:Runs`；冒烟时传 `99` 导致它开始跑 99 个 run，及时 taskkill 止损。**跑单个 run 必须传向量**（如 `[99]`）。数据落在临时目录 `D:\REMOandDREMO测试集\_smoke_noBatchDict_M20`（8 个 mat，用户可自行删），**正式数据目录未被污染**。
- 启动时间 2026-09-18 23:57（driver 后台）；driver 日志 `.../REMO_noBatchDict_Ablation_M20/logs/driver.log`，逐跑进度在各 `*_p<题号>_a<轮次>.log`（行格式 `[done] <prob> run k | IGD A -> B | runtime Xs | wall Ys | ok`）。
- 附带发现：`addpath(genpath(PlatEMO根))` 会遮蔽内建 `exit`（`-batch` 收尾报 `函数或变量 'Settings' 无法识别`），无副作用，既有 runner 同症状。
