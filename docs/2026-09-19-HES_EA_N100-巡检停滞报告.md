# HES_EA_N100 M10 / M15 巡检报告（2026-09-19 15:03）

> 巡检性质：只读日志与数据目录，未重启/未杀进程/未改动任何运行中的实验。

## 一、结论

| 阶段 | 进度 | 状态 | 预计剩余 |
|---|---|---|---|
| **M=10** | **94 / 320（29.4%）** | 🔴 **已停滞 2 h 41 min**（最后产出 12:18:38），12 个 worker 占满 12 核空转 | **无法估算**（吞吐 = 0） |
| **M=15** | **0 / 320** | ⛔ **未启动**（数据目录尚未创建），链式看门狗在死等 | 依赖 M10 解除停滞 |

- 健康节奏下本应早已完成（12 路并行 ≈ 174 跑/h，226 个剩余跑 ≈ 1.3 h）；当前 **吞吐为 0**。
- 两阶段均未完成 → 按巡检步骤未运行 MATLAB 校验，**无 OK/BAD/IGDp-missing 结果**。
- **不适用"M10 完成后 30 分钟内 M15 未开始"的补启动条件**：M10 根本没完成，且看门狗被卡死进程永久阻塞。

## 二、判定"停滞"而非"跑得慢"的决定性证据

| 证据 | 观测值 | 含义 |
|---|---|---|
| 数据目录最后 .mat | `12:18:38`（DTLZ7 run 20），此后 2 h 41 min 零新文件 | 零 I/O 产出 |
| `driver.log` 末行 | `[12:18:46] start part 10 runs [1..10]` | driver 一直阻塞在 `wait`，未再调度新切片 |
| 13 个 MATLAB 进程的 30 s CPU 增量 | 每个 **28.8 ~ 29.2 s**（共 12.6 核满载） | 进程活着、在满核空转，不是死进程 |
| 进程 CPU 总量 vs 启动时刻 | ≈13,200 s ↔ 11:10–11:14 启动（占空比 97%） | **排除待机冻结/睡眠**（若 S0 冻结过，CPU 会显著小于墙钟） |
| 健康切片耗时对照 | 10 跑/切片 = 2295–2787 s（38–47 min） | 当前已超时 **5–8 倍**且零输出 ⇒ 卡死 |

## 三、M=10 逐题进度

| 题目 | 已有/20 | 缺失 run | 是否被卡死切片占用 |
|---|---|---|---|
| DTLZ1 | 20 | — | 完成 |
| DTLZ2 | 8 | 5–10, 12, 14, 15, 18–20 | 🔴 两个切片均卡死 |
| DTLZ3 | 10 | 3–10, 12, 14 | 🔴 slice[3–14] 卡死（[15–20] 已正常完成） |
| DTLZ4 | 20 | — | 完成 |
| DTLZ5 | **0** | 1–20 | 🔴🔴 自 11:13 起连**一个 run 都未完成**（3 h 45 min） |
| DTLZ6 | 9 | 1–10, 20 | 🔴 两个切片均卡死 |
| DTLZ7 | 20 | — | 完成 |
| WFG1 | 6 | 4–10, 14–20 | 🔴 两个切片均卡死 |
| WFG2 | 1 | 除 11 外全部 | 🔴 两个切片均卡死 |
| WFG3 | **0** | 1–20 | 🔴🔴 自 12:19 起零完成（2 h 39 min） |
| WFG4–WFG9 | 0 | 1–20（各） | 尚未调度（driver 排队中） |

12 个卡死切片（由 `Win32_Process.CommandLine` 直接读出，含 part 号与 run 列表）：
`part2[4-15]`、`part2[16-20]`、`part3[3-14]`、`part5[1-10]`、`part5[11-20]`、`part6[1-10]`、`part6[11-20]`、`part8[1-10]`、`part8[11-20]`、`part9[1-10]`、`part9[11-20]`、`part10[1-10]`
—— 全部在 11:10–12:18 之间启动，**此后从未退出**，即 driver 的 12 个并发槽位已被全部占死。

## 四、异常：同一病理在 M=15 也已复现

| 切片 | 启动 | 结果 |
|---|---|---|
| `smoke_HES_EA_N100_M15(2,[7 8],1)` DTLZ2 | 13:42:18 | ✅ 正常：285.5 s + 263.8 s，2 跑完成 |
| `smoke_HES_EA_N100_M15(10,[7 8],1)` **WFG3** | 13:42:50 | 🔴 至今无任何输出，CPU 已累计 4,363 s |

→ 说明**不是"整题不可跑"**，而是**特定 run 触发死循环/超常耗时**：DTLZ3 的 slice[15–20] 正常完成而 slice[3–14] 卡死；M15 的 DTLZ2 冒烟正常而 WFG3 卡死。
首要嫌疑点：`HES_EA_N100` 第 75 行 `pdist2(...,'cosine')` 在退化种群上产生 NaN → KMeans 迭代不终止（此前一直被当作"无害警告"压制）。

## 五、下一步建议（本次均未执行，需你决定）

1. **推荐：先定位死循环根因再重跑**。可在单进程里对 `DTLZ5 run 1`、`WFG3 run 1` 加超时/迭代上限断言，确认是否 KMeans 不收敛；否则重跑会在同一批 run 上再次卡死。
2. 若决定强行续跑，恢复顺序必须严格遵守（反序会被补缺循环重新拉起切片）：
   ① 停 `chain_to_M15.sh`（PID 44472）→ ② 停 M10 `driver.sh` → ③ 杀 12 个卡死 MATLAB → ④ **后台任务方式**重启 M10 driver（前台 `nohup &` 会被工具调用结束连带杀掉）→ ⑤ 确认 M10 跑通后再启链式看门狗。
3. `chain_to_M15.sh` 的判据要求 MATLAB 进程数归零，**建议改为"数据 320 且 driver 出现 DRIVER_DONE"**，避免单点卡死永久阻塞链条。
4. M15 的 WFG3 冒烟进程（PID 46544）同样是卡死态，若按 ①③ 清理需一并处理。

---

*数据目录：`D:\REMOandDREMO测试集\10目标\n30\HES_EA_N100`、`D:\REMOandDREMO测试集\15目标\HES_EA_N100`*
*日志：`PlatEMO/Experiments/HES_EA_N100_M10/logs/{driver.log,p*_a1.log}`、`.../HES_EA_N100_M15/logs/{chain.log,smoke_p*.log}`*
