# 指标模式（PIEA 指标子系统）模块去留 — 决策建议

> 针对 `AdaMao实验表/消融实验/指标模式/` 下 5 个 xlsx（同一实验、不同控制算法）的重新分析
> 结合 PlatEMO 中 `REMO_new2_AdaMaO` 及其 5 个消融变体源文件
> 生成时间：2026-07-08（用于 7.8 组会）

---

## 0. 一句话结论

**不要现在就删，也不要整段保留；先补做 M=5 / M=15 + HV 再拍板（选项 c）。**
**当前 M=10 证据下的倾向性结论：直接走 `Lite`（彻底移除照搬的 PIEA 指标子系统）最干净；若一定要保留，只保留 `Full` 的「诊断触发的指标模式」，并明确标注为外部借用。绝对不要保留 `FixedIndicator`（always-on）或 `PIEAOnly`（standalone）。**

---

## 1. 先确认：5 个表到底是什么关系

我把 5 个 xlsx 用 openpyxl 全部解析，确认它们是**同一批实验的不同「对比视图」**：

- 每个算法在 16 个问题上的 **IGD 均值±标准差完全一致**（交叉核对 16/16 一致）；
- 唯一区别是 Excel 里 `+/-/= ` 显著性符号的**参照算法（control）不同**——每个文件把其中一个变体放到最后一列作 control。
- 因此**跨文件的符号不能直接比**，本文一律以一致的「均值±标准差」为准。

5 个变体对应的算法文件（均在 `PlatEMO/Algorithms/.../REMO_new2_AdaMaO/`）：

| 变体（表名） | 入口文件 | 指标模式的状态 |
|---|---|---|
| `完整版本` Full | `REMO_new2_AdaMaO.m` | 诊断触发：退化度 ≥ 0.45 才用指标重排 |
| `NoIndicator` | `..._NoIndicator.m` | 关闭 indicator 候选模式 |
| `Lite` | `..._Lite.m` | 整条 PIEA 指标子系统移除 |
| `PIEAOnly` | `..._PIEAOnlySelection.m` | 只用指标选择，**去掉自适应关系学习** |
| `FixedIndicator` | `..._FixedIndicatorAlways.m` | 始终启用 indicator（always-on） |

---

## 2. 数据给出的硬事实（M=10，IGD）

**事实 1：5 个变体在 IGD 上无统计显著差异。**
Friedman 检验 χ²=1.40，df=4，**p=0.8442 ≫ 0.05**。任何「某版本明显更强」的说法在此数据上都不成立。

**事实 2：Full ≃ Lite（M=10 下删掉指标子系统不丢性能）。**
平均秩 Full 3.06 ≈ Lite 3.06；Full-vs-Lite 逐问题近似 z 检验：Full 优 5、Lite 优 4、无差异 7。即「删掉指标子系统几乎无代价」。

**事实 3：指标模块在「连通、规则 PF」上帮，在「断开/退化 PF」上崩。**
这是最关键的规律，来自逐问题扫描：

| PF 类型 | 代表问题 | 指标模式表现 |
|---|---|---|
| 连通、规则 | DTLZ1–5、WFG2、WFG9 | PIEAonly / FixedInd 多为该问题**最佳**（绿） |
| 断开/退化 | **DTLZ7、WFG6** | **崩塌**：PIEAonly 在 DTLZ7 IGD=20.3，Full 仅 8.63（劣 2.35×）；FixedInd 在 DTLZ7 也 19.6 |

**事实 4：崩塌只发生在「脱离自适应关系学习」或「无触发 always-on」时。**
DTLZ7：Full 8.63 / NoInd 8.70 / Lite 8.80（都正常），但 PIEAonly 20.3、FixedInd 19.6（崩）。
说明：**AdaMaO 的核心（自适应关系学习 + 动态候选选择）能防止崩塌**；照搬的指标模块本身在退化 PF 上是危险的，只有 Full 的「退化度触发」能兜住。

**事实 5：所有变体都显著优于原始 REMO（10–14/16 问题 IGD 更低），且对 SOTA 有竞争力。**
说明**核心框架本身已足够强**，指标子系统不是竞争力来源。

---

## 3. 对「三个选项」的逐项回答

### (a) 保留整个指标模式模块？ → ❌ 不建议
- M=10 上无显著增益（p=0.84），且 standalone/always-on 形式在退化 PF 上崩塌；
- 对 **Swarm and Evolutionary Computation（一区）** 投稿，把 PIEA 的 SDE / ε+ / Minkowski 轮盘选择**原样照搬**当「本文贡献」，是明确的原创性/新颖性风险，审稿人极可能质疑；
- 论文贡献应干净收敛到「自适应关系对训练（conservative/curriculum/weighted 动态切换）+ 动态候选选择（conservative/explore）」。

### (b) 只保留部分？ → ✅ 可以，但仅在一种情况下
- **可以保留的部分 = `Full` 的「诊断触发式指标模式」**（退化度 ≥ 0.45 才触发），它不是 always-on，不会在退化 PF 上崩；
- 投稿中必须**明确标注该指标模块源自 PIEA（外部借用）**，不作为本文贡献，仅作为被证伪/被约束的消融对象；
- 更激进也更干净的做法是 **`Lite`：整段移除**。它与 Full 在 M=10 无差异，代码更短、更易复现、原创性最安全。

### (c) 先补做实验再决定？ → ✅ 这是流程上最正确的「门」
**为什么 M=10 单维度不足以定稿：**
- 指标模式的设计初衷是「种群退化（degeneracy ≥ 0.45）时」才启用。M=10 时退化度通常不够高，**触发条件很少满足 → 模块基本处于休眠态**，所以测不出增益；
- 指标/多样性机制的价值**恰恰在高维（M=15）才显现**——更高 M 下退化更普遍，Full 的触发会更频繁，那时指标模式**可能**才真正起作用；
- 因此「只在 M=10 测」会**系统性低估**这个模块，对它的去留下结论不公平。

**所以：在拿到 M=5 / M=15 + HV 之前，不下最终删除/保留结论。**

---

## 4. 建议答案（直接给你拍板的版本）

> 给组会的建议话术：

1. **流程上选 (c)**：本周先别定稿，补 M=5、M=15 与 HV 两个指标，再回头定夺。这是最站得住脚的学术表述。
2. **当前倾向（ provisional）选 (b) 的 Lite 化**：在 M=10 证据下，直接投 **`Lite`（去掉整段 PIEA 指标子系统）** 风险最低、原创性最安全；把「5 变体消融证明 borrowed 指标模块在本框架中无益甚至有害，故 deliberate 移除」作为方法学严谨性的卖点。
3. **保留的退路**：若 M=15 数据显示 Full 的退化度触发在断开/退化 PF 上**显著**改善（例如 DTLZ7/WFG6 高维崩塌被抑制），则改投 **`Full`（诊断触发式）**，并标注外部借用。
4. **红线**：无论怎么选，**不要**保留 `FixedIndicator`（always-on）与 `PIEAOnly`（standalone）——它们在退化 PF 上的崩塌是数据里最干净、最无歧义的结论。

---

## 5. 投稿前必须补齐的实验清单

| # | 实验 | 为什么必须做 |
|---|---|---|
| 1 | **目标维度数 M∈{5,10,15}** | 当前只有 M=10；指标模块价值最可能出现在高维，缺 M=5/15 结论会偏颇 |
| 2 | **第二个指标 HV（Hypervolume）** | 一区审稿人几乎必要求 IGD + HV 双指标；HV 对分布/多样性更敏感，可能暴露指标模块的价值或危害 |
| 3 | **逐次 Wilcoxon 符号秩检验** | 用 30 次原始运行（非均值±标准差近似 z）做配对检验，统一以**投稿算法为 control** 报 `+/-/=`，避免「每个文件 control 不同」的混乱 |
| 4 | **可复现声明** | 报告平台、种群规模 N、FE 预算、随机种子设置（确认 5 变体为同种子下 30 次独立运行） |
| 5 | **消融叙事** | 论文中把「移除 borrowed PIEA 模块」写成方法学严谨性证据，而非弱点 |

---

## 6. 方法与透明声明

- 仅基于 16 个 M=10 问题的 IGD 均值±标准差（非逐次原始数据）；变体间配对显著性为 n=30 正态近似 z（非严格 Wilcoxon）。
- 5 个 xlsx 的 `+/-/= ` 符号 control 随文件变化，已统一改用一致均值比较，未直接采信原表符号做跨变体判断。
- 数据已用 Python 独立复算（见 `analyze_ablation.py` / `make_heatmap.py` / `analyze_rank_all.py`），与既有报告数值一致。

---

## 7. 平均秩（Average Rank）怎么算、怎么读、为什么不能单独下结论

### 7.1 计算方法
把 16 个问题当 16 个「区块」。**每个问题内部**，算法按 IGD 从小到大排名（最小=秩 1，最大=秩 k；并列取平均秩）。把某算法在 16 题上的秩求和 ÷ 16 = 其平均秩（越低=越常排前=越稳）。
例（DTLZ7 IGD：Full 8.63 / NoInd 8.70 / Lite 8.80 / FixedInd 19.6 / PIEAonly 20.3）→ 秩：Full1 NoInd2 Lite3 FixedInd4 PIEAonly5。

### 7.2 三个坑（组会必答）
1. **只是序数，非量级**：秩差不表示"好多少"。
2. **会掩盖崩塌**：PIEAonly 5 变体平均秩最低(2.69)，全场也排第 2(4.88)，却在 DTLZ7 上 IGD=20.3（Full 的 2.35×）崩塌——靠"简单 PF 多拿第一"拉高平均分，对稳健性是假象。
3. **必须配显著性检验**：5 变体 Friedman p=0.84，那些 3.06 vs 2.69 的差异不显著，谈"谁秩低"无意义。

### 7.3 不能只内部比——全场 11 算法平均秩（16 题，越低越好）
| 名次 | 算法 | 平均秩 | 备注 |
|---:|---|---:|---|
| 1 | PIEA | 4.56 | borrowed 模块来源（外部 SOTA） |
| 2 | PIEAonly | 4.88 | 本算法变体（仅指标，崩 DTLZ7） |
| 3 | FixedInd | 5.19 | 本算法变体（always-on，崩 DTLZ7） |
| 4 | Full | 5.69 | 诊断触发 |
| 5 | Lite | 5.69 | 删指标子系统 |
| 6 | WFG10 | 5.94 | 基线 SOTA |
| 6 | R2AEA | 5.94 | 基线 SOTA |
| 8 | NoInd | 6.12 | 关指标候选模式 |
| 9 | REMO_new2 | 6.81 | |
| 10 | MCEAD | 7.38 | |
| 11 | REMO | 7.81 | |

**读法**：5 个变体稳居中上游（第 2–8/11），压过 REMO/REMO_new2/MCEAD、与 WFG10/R2AEA 同级 → 核心框架本身有竞争力，指标子系统非竞争力来源。讽刺点：带指标的 PIEAonly/FixedInd 平均秩比 Full/Lite 还高，却正是崩塌者——再次说明平均秩 alone 会误导。

### 7.4 组会口径
"两层级比较：① 5 变体内部 Friedman p=0.84 无差异，删指标(Lite)不丢性能；② 与 6 SOTA 同场排名稳居中上游，竞争力来自自适应关系学习而非 borrowed 指标。平均秩仅作排序摘要，因 p 不显著且 PIEAonly 在退化 PF 崩塌，不以它作去留依据，结合稳健性+原创性风险倾向 Lite。"
