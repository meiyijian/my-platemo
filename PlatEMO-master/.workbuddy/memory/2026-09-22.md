# 2026-09-22

## 08:32 检查：**FE500 十跑目标已达成**（最后一跑正在收尾）

23:52 启动的全局队列任务 `xBYL7y` 跑了一夜，08:32 时状态：

| 算法 | 文件数 | 期望 | DTLZ7 |
|---|---|---|---|
| SSDE | 320 | 320 | 20/10 ✅ |
| SAMOEATL2M | 319 | 318 | 19/10 ✅ |
| REMO | 310 | 310 | **10/10 ✅** |
| PACDIS | 310 | 310 | **10/10 ✅** |
| HES_EA | 184 | 160 | **9/10（run 10 正在跑）** |
| CSEA | 160 | 160 | 10/10 ✅ |
| PCSAEA | 160 | 160 | 10/10 ✅ |
| **合计** | **1763** | ~1738 | |

⇒ **16 题 × ≥10 跑，七个算法全部达标**；唯一未落盘的是 `HES_EA DTLZ7 run 10`
（`logs/HES_EA/p7.log` 里 run 9 于 08:21:59 完成，run 10 在跑，单跑 wall ≈ 740 s）。

### 数值体检
- **HES_EA 的 DTLZ7 单跑 wall ≈ 740 s（runtime ≈ 470 s）** ⇒ IGDp 开销约 270 s/跑。
- REMO 的 DTLZ7 片：`part 7 done: 10 runs (6 skipped) in 6511.5 s (1.8 h)` ⇒ 单跑 ≈ 1600 s。
- HES_EA 的非 DTLZ7 片：`part 10/11/12 done: 10 runs (6 skipped) in ~4260 s (1.2 h)` ⇒ 单跑 ≈ 1070 s。

### ⚠️ 夜间有 28 次 `SLICE_TIMEOUT` 触发（REMO 18、HES_EA 10）
`rc_part*.txt` 里共 28 条退出码 `124`，但**没有生成任何 poison**（说明没有同一片被杀两次）。
⇒ 这些多半**不是死锁、而是"合法的慢"**：DTLZ7 的 10 跑切片在慢算法上会超过 4 h。
被杀的片下一轮重驱即完成。**教训：`SLICE_TIMEOUT=4 h` 对"慢算法 + DTLZ7 + 10 跑"偏紧**，
以后这类切片应设 6–8 h，或把 DTLZ7 切片切到 5 跑以内（本次 `split_starved` 只在片数 < 16 时切，
夜里池子一直是满的所以没切）。

### 现场
`*.tmp.mat` 残留 0；数据目录干净。driver 仍在 `wait`（等最后一片），跑完会自动退出并执行验收。

## 09:27 出表：**七算法 × 10 跑 IGDp 对比表（锚点 PACDIS）**

覆盖核对：7 算法 × 16 题 × 10 跑 = **1120 格全齐**（无缺）。
脚本 `build_table_runs10_IGDp.py`（可复用，参数是跑数）；产物
`FE500_M20_runs10_IGDp.md` / `.csv`（在本实验目录下）。

### 符号口径
- 主用 **配对 Wilcoxon 符号秩**（种子配对，正确做法）；同时算了**非配对 Mann-Whitney
  （≈ 论文表的 ranksum）**做对照，两套 tally 都打印出来，避免"悄悄不一致"。
- 一致的部分：PCSAEA 0/15/1、SSDE 1/15/0 两套完全相同；REMO/CSEA/HES_EA/SAMOEATL2M 差 1–2 题。

### 结果（配对 Wilcoxon，16 题）
| 算法 | + 显著更优 | - 显著更差 | = 无差异 |
|---|---|---|---|
| REMO | 1 | 9 | 6 |
| PCSAEA | **0** | **15** | 1 |
| CSEA | 1 | 9 | 6 |
| **HES_EA** | **3** | 7 | 6 |
| SSDE | 1 | 15 | 0 |
| SAMOEATL2M | 1 | 14 | 1 |

### 关键结论
1. **PACDIS 总体仍第一**：对 PCSAEA / SSDE 是 15/16 完胜；对 SAMOEATL2M 14 胜。
2. 🔴 **HES_EA（原版）是最强对手**：3 胜 7 负 6 平，是唯一在多个题上压过 PACDIS 的基线。
   - **DTLZ7：HES_EA 2.025 vs PACDIS 16.51 —— 8 倍差距，PACDIS 被碾压**
   - **DTLZ5：HES_EA 0.0744 vs PACDIS 0.176**（2.4 倍）
   - **WFG8：HES_EA 15.54 vs PACDIS 16.23**（小幅）
   - 且 HES_EA 在 WFG4/5/6/9 上均值也低于 PACDIS（未达显著）。
   ⇒ 与工作机那份"HES_EA_N100 反超 PACDIS 8/3/5"的结论**方向一致**，只是这里 PACDIS 战绩更好
   （原版 HES_EA 有 `NI=329` 的预算劣势，且这里 10 跑、含 DTLZ7）。
   **论文必须正面处理 HES_EA，不能宣称 PACDIS 全面领先。**
3. 其它反例：**WFG3** 上 REMO/CSEA/SAMOEATL2M 三家都显著优于 PACDIS（SAMOEATL2M 1.653 最好）；
   **WFG4** 上 SSDE 显著更优（15.28 vs 17.89）。
4. 每行最低均值归属：**HES_EA 6 题**（DTLZ5/7、WFG5/6/8/9）、**PACDIS 5 题**（DTLZ2/4/6、WFG2/7）、
   REMO 3 题（DTLZ1/3、WFG1）、SAMOEATL2M 1 题（WFG3）、SSDE 1 题（WFG4）。

### caveat（读表时必须带上）
- 10 跑（非论文主表的 20 跑）⇒ 统计功效低一档，`=` 里可能藏着真实差异。
- **HES_EA / PCSAEA / SAMOEATL2M 用原版类**，`NI = 11*D-1 = 329` 吃掉 500 里的 66%，
  只剩 ~171 给搜索；PACDIS/REMO 是 100 初始 + ~400 搜索。**HES_EA 在如此劣势下还能赢 DTLZ5/7，更要警惕。**
- CSEA 的初始规模是 `min(11D-1,109)=109`；SSDE 是 N=100。
- 本表是**工作表的中间产物**，不是论文最终 xlsx（那个按项目规范在工位电脑出，Times New Roman 等）。

## 下一步（待用户决定）
1. 跑 `verify_FE500_M20` 做完整性校验（文件数 + 末次 FE 落在 [500,600] + IGD/IGDp 齐全有限）。
2. ✅ 七算法 × 10 跑 IGDp 对比表**已出**（见上一节）。
3. 若要进论文主表：需在**工位电脑**按项目 xlsx 规范出（Times New Roman 11pt、蓝标、`=-+` 符号），
   且要考虑用 20 跑（当前只有 SSDE/部分算法有 20 跑；HES_EA/CSEA/PCSAEA 只有 10 跑）。
