# 2026-09-22

## 08:32 检查：**FE500 十跑目标已达成**（最后一跑正在收尾）

23:52 启动的全局队列任务 `xBYL7y` 跑了一夜，08:32 时状态：

| 算法 | 文件数 | 期望 | DTLZ7 |
|---|---|---|---|
| SSDE | 320 | 320 | 20/10 ✅ |
| SAMOEATL2M | 319 | 318 | 19/10 ✅ |
| REMO | 310 | 310 | **10/10 ✅** |
| PACDIS | 310 | 310 | **10/10 ✅** |
| HES_EA | 184 | 160 | **9/10（run 10 正在跑）** |
| CSEA | 160 | 160 | 10/10 ✅ |
| PCSAEA | 160 | 160 | 10/10 ✅ |
| **合计** | **1763** | ~1738 | |

⇒ **16 题 × ≥10 跑，七个算法全部达标**；唯一未落盘的是 `HES_EA DTLZ7 run 10`
（`logs/HES_EA/p7.log` 里 run 9 于 08:21:59 完成，run 10 在跑，单跑 wall ≈ 740 s）。

### 数值体检
- **HES_EA 的 DTLZ7 单跑 wall ≈ 740 s（runtime ≈ 470 s）** ⇒ IGDp 开销约 270 s/跑。
- REMO 的 DTLZ7 片：`part 7 done: 10 runs (6 skipped) in 6511.5 s (1.8 h)` ⇒ 单跑 ≈ 1600 s。
- HES_EA 的非 DTLZ7 片：`part 10/11/12 done: 10 runs (6 skipped) in ~4260 s (1.2 h)` ⇒ 单跑 ≈ 1070 s。

### ⚠️ 夜间有 28 次 `SLICE_TIMEOUT` 触发（REMO 18、HES_EA 10）
`rc_part*.txt` 里共 28 条退出码 `124`，但**没有生成任何 poison**（说明没有同一片被杀两次）。
⇒ 这些多半**不是死锁、而是"合法的慢"**：DTLZ7 的 10 跑切片在慢算法上会超过 4 h。
被杀的片下一轮重驱即完成。**教训：`SLICE_TIMEOUT=4 h` 对"慢算法 + DTLZ7 + 10 跑"偏紧**，
以后这类切片应设 6–8 h，或把 DTLZ7 切片切到 5 跑以内（本次 `split_starved` 只在片数 < 16 时切，
夜里池子一直是满的所以没切）。

### 现场
`*.tmp.mat` 残留 0；数据目录干净。driver 仍在 `wait`（等最后一片），跑完会自动退出并执行验收。

## 10:39 全局队列任务 `xBYL7y` 跑完（10h47m）→ **实验目标 100% 达成 + 验收通过**

### 数据（runs 1-10 × 16 题 × 7 算法 = 1120 格，**零缺口**）
| 算法 | 文件数 |
|---|---|
| SSDE | 320 |
| SAMOEATL2M | 319 |
| REMO | 310 |
| PACDIS | 310 |
| HES_EA | 185 |
| CSEA | 160 |
| PCSAEA | 160 |
| **合计** | **1764** |

### 完整性校验：**1120/1120 全过**（Python 校验，见下）
`result + metric{runtime, IGD, IGDp}` 齐全、长度一致、全有限、
**末次 FE 落在 500..547**（在 `[500, 600]` 容差内）、快照数 13–30。
`*.tmp.mat` 残留 0；MATLAB 进程 0。

### 🔴 踩坑：`verify_FE500_M20.m`（MATLAB 版验收）**在本机会崩**
driver 收尾的验收步骤里 MATLAB 抛 **access violation `0xc0000005`**，
栈是 `libxml2.dll` → `addons_registry_core.dll` → `libmwflhttpclient_core.dll`
⇒ **MATLAB 插件注册表的后台 HTTP 线程**（在启动时去抓插件 XML），**与我们的代码无关**，
是 MATLAB R2021b 在这台机器上的环境问题。试验本身早已跑完（`DRIVER_DONE` 正常打印）。
**对策：验收改用新增的 `verify_runs.py`**（Python 版，逻辑与 `.m` 版逐条对应：
文件存在 / `result+metric{runtime,IGD,IGDp}` / 长度一致 / 全有限 / 末次 FE ∈ [500,600]），
**不含 MATLAB，不会被那个线程带崩**。用法 `python verify_runs.py [跑数] [--all]`；
`--all` 会检查目录里所有 `.mat`（不限 runs 1..N）。退出码 0=全过。

## 09:27 出表：**七算法 × 10 跑 IGDp 对比表（锚点 PACDIS）**

覆盖核对：7 算法 × 16 题 × 10 跑 = **1120 格全齐**（无缺）。
脚本 `build_table_runs10_IGDp.py`（可复用，参数是跑数）；产物
`FE500_M20_runs10_IGDp.md` / `.csv`（在本实验目录下）。

### 符号口径
- 主用 **配对 Wilcoxon 符号秩**（种子配对，正确做法）；同时算了**非配对 Mann-Whitney
  （≈ 论文表的 ranksum）**做对照，两套 tally 都打印出来，避免"悄悄不一致"。
- 一致的部分：PCSAEA 0/15/1、SSDE 1/15/0 两套完全相同；REMO/CSEA/HES_EA/SAMOEATL2M 差 1–2 题。

### 结果（配对 Wilcoxon，16 题）
| 算法 | + 显著更优 | - 显著更差 | = 无差异 |
|---|---|---|---|
| REMO | 1 | 9 | 6 |
| PCSAEA | **0** | **15** | 1 |
| CSEA | 1 | 9 | 6 |
| **HES_EA** | **3** | 7 | 6 |
| SSDE | 1 | 15 | 0 |
| SAMOEATL2M | 1 | 14 | 1 |

### 关键结论
1. **PACDIS 总体仍第一**：对 PCSAEA / SSDE 是 15/16 完胜；对 SAMOEATL2M 14 胜。
2. 🔴 **HES_EA（原版）是最强对手**：3 胜 7 负 6 平，是唯一在多个题上压过 PACDIS 的基线。
   - **DTLZ7：HES_EA 2.025 vs PACDIS 16.51 —— 8 倍差距，PACDIS 被碾压**
   - **DTLZ5：HES_EA 0.0744 vs PACDIS 0.176**（2.4 倍）
   - **WFG8：HES_EA 15.54 vs PACDIS 16.23**（小幅）
   - 且 HES_EA 在 WFG4/5/6/9 上均值也低于 PACDIS（未达显著）。
   ⇒ 与工作机那份"HES_EA_N100 反超 PACDIS 8/3/5"的结论**方向一致**，只是这里 PACDIS 战绩更好
   （原版 HES_EA 有 `NI=329` 的预算劣势，且这里 10 跑、含 DTLZ7）。
   **论文必须正面处理 HES_EA，不能宣称 PACDIS 全面领先。**
3. 其它反例：**WFG3** 上 REMO/CSEA/SAMOEATL2M 三家都显著优于 PACDIS（SAMOEATL2M 1.653 最好）；
   **WFG4** 上 SSDE 显著更优（15.28 vs 17.89）。
4. 每行最低均值归属：**HES_EA 6 题**（DTLZ5/7、WFG5/6/8/9）、**PACDIS 5 题**（DTLZ2/4/6、WFG2/7）、
   REMO 3 题（DTLZ1/3、WFG1）、SAMOEATL2M 1 题（WFG3）、SSDE 1 题（WFG4）。

### caveat（读表时必须带上）
- 10 跑（非论文主表的 20 跑）⇒ 统计功效低一档，`=` 里可能藏着真实差异。
- **HES_EA / PCSAEA / SAMOEATL2M 用原版类**，`NI = 11*D-1 = 329` 吃掉 500 里的 66%，
  只剩 ~171 给搜索；PACDIS/REMO 是 100 初始 + ~400 搜索。**HES_EA 在如此劣势下还能赢 DTLZ5/7，更要警惕。**
- CSEA 的初始规模是 `min(11D-1,109)=109`；SSDE 是 N=100。
- 本表是**工作表的中间产物**，不是论文最终 xlsx（那个按项目规范在工位电脑出，Times New Roman 等）。

## 14:32 用户要求**补到每题 20 跑**（第九次重启，任务 `WvORRg`）

10:39 那轮跑到 10 跑目标达成（1120 格零缺口）后，用户要求继续补满 20 跑。

### 现状与剩余队列
| 算法 | 已有 | 缺口 |
|---|---|---|
| SSDE | 320 / 320 | 0 |
| SAMOEATL2M | 319 / 320 | 2 |
| REMO | 310 / 320 | 10（DTLZ7 的 run 11–20） |
| PACDIS | 310 / 320 | 10（同上） |
| HES_EA | 185 / 320 | 135 |
| CSEA | 160 / 320 | 160 |
| PCSAEA | 160 / 320 | 160 |

干跑：`RUNS=1-20 CHUNK=10` ⇒ **53 片 / 477 跑**（CHUNK=5 则是 101 片 / 477 跑）。
DTLZ7 占 8 片；`collect_slices` 的**按算法** `split_starved` 把 REMO/PACDIS 的 10 跑 DTLZ7 片
自动切成 **2×5 跑**（因为它们各自只有 1 片 < 16），所以不会撞超时。

### 启动参数（两处为本次特意调整）
```
ALGS="REMO PACDIS HES_EA CSEA PCSAEA SAMOEA SSDE" MAXJOBS=16 THREADS=1
RUNS=1-20 CHUNK=10 ROUNDS=8 SLICE_TIMEOUT=21600 MAX_SLICE_FAILS=2
SKIP_VERIFY=1 LAUNCH_GAP=20 COLD_STARTS=4 LAUNCH_GAP_WARM=5 MIN_FREE_MB=0
```
- **`SLICE_TIMEOUT` 14400 → 21600（6 h）**：吸收上一轮的教训——慢算法的 DTLZ7 切片会超 4 h，
  被误杀两次还会被**投毒永久丢数据**。现在即使 HES_EA 的 10 跑 DTLZ7 片（实测 2.1 h）
  在争抢下再慢一倍也还在 6 h 内。
- **`SKIP_VERIFY=1`**：driver 收尾的 MATLAB 验收在本机会崩（插件注册表 HTTP 线程，
  见上一节）；改为跑完后手动执行 `verify_runs.py`。

### 预估
477 跑，主体是 HES_EA 135 跑（~1000 s/跑 ⇒ 2.3 h）+ CSEA/PCSAEA 各 160 跑（成本未知，
按 300–500 s/跑估共 1.5–2.5 h）+ REMO/PACDIS 的 DTLZ7 20 跑（~1600 s/跑 ⇒ 0.6 h）
⇒ **约 5–6 h**，约今晚 20:00–21:00 收尾。

### 现场
14:33 起，`attempt 1: 53 slices`，从 `[REMO] part 7 runs [11..15]` 起跑，4 进程爬向 16，空闲内存 17.9 GB。
**巡检自动化 `852747ad-…` 的 validUntil 是今天 10:00，已过期**——本轮没有自动巡检，需要的话让用户确认后再延长。
## 16:02 进度查询：16 路跑满（CPU 100%），实测各算法单跑 wall

**实测平均单跑 wall（从 harness 的 `logs/<KEY>_part*_r*.log` 的 `[done]` 行统计，含 16 路争抢）**

| 算法 | 平均 wall | 样本 | 备注 |
|---|---|---|---|
| **SSDE** | **11 s** | n=320 | 最快 |
| **PCSAEA** | **153 s** | n=160 | FNN，轻 |
| **CSEA** | **370 s** | n=160 | 分类器 |
| **SAMOEATL2M** | ~200 s | — | 日志被覆盖，按之前估计 |
| **HES_EA** | **2059 s（≈34 min）** | n=208 | **本轮实测已升到 ~2300 s**（16 路争抢下比早先 1070 s 慢一倍） |
| **REMO / PACDIS** | ~1600 s | — | DTLZ7 更慢 |

⇒ **HES_EA 是绝对瓶颈**：它一个算法就占剩余工作量的 ~71%。

**剩余工作量与 ETA（14:32 起跑）**：HES_EA 112 跑×2300 + CSEA 160×370 + PCSAEA 160×153
+ REMO/PACDIS DTLZ7 14×1600 ≈ 363,900 s / 16 ≈ **6.3 h** ⇒ 约 **今晚 21:00** 收尾。

### 本轮进度（16:02，共 1.5 h）
`SSDE 320` ✅、`SAMOEATL2M 319`、`PACDIS 314`、`REMO 312`、`HES_EA 208`、`CSEA 160`、`PCSAEA 160` ⇒ **1793/2240**。
CPU **100%**、MATLAB **16** 个、空闲内存 9.8 GB。**无 poison、无 tmp 残留、无新超时**。

⚠️ **CSEA / PCSAEA 还没动是正常的**：队列顺序是 REMO→PACDIS→HES_EA→CSEA→PCSAEA，
HES_EA 的 16 片先占满池子，CSEA 的片在槽位释放后才启动。
全局队列下**总墙钟与顺序无关**（池子不空转），只是数据"成批"到达。

### 踩坑：`p<N>.log` 会被覆盖，`[done]` 只剩 harness 自己的日志
`driver.sh` 用 `-logfile logs/<KEY>/p<N>.log`，而 **MATLAB 的 `-logfile` 是覆盖写**：
同一个切片被重驱（尤其是"全部 skip"那种）时，**上一次的 `[done]` 行会被冲掉**。
⇒ **要统计单跑成本，必须读 harness 自己追加的 `logs/<KEY>_part<NN>_r<a>-<b>.log`**，
不能读 `p<N>.log`。
1. 补跑完成后跑 `verify_runs.py --all`（**不要**用会崩的 MATLAB 版 `verify_FE500_M20`）
   做完整性校验。
2. ✅ 七算法 × 10 跑 IGDp 对比表**已出**；补满 20 跑后可用 `build_table_runs10_IGDp.py 20` 出 20 跑版。
3. 若要进论文主表：需在**工位电脑**按项目 xlsx 规范出（Times New Roman 11pt、蓝标、`=-+` 符号）。
4. 巡检自动化 `852747ad-…` 已过期；若还要夜间自动巡检需用户确认后延长 validUntil。

## 22:16 进度查询：**2230 / 2240**，只差 10 个跑（全部是 DTLZ7）

| 算法 | 进度 |
|---|---|
| SSDE | 320 ✅ |
| SAMOEATL2M | 320 ✅ |
| PACDIS | **320 ✅** |
| REMO | 318（缺 DTLZ7 run 15,20） |
| CSEA | 318（缺 DTLZ7 run 19,20） |
| PCSAEA | 317（缺 DTLZ7 run 18,19,20） |
| HES_EA | 317（缺 DTLZ7 run 18,19,20） |
| **合计** | **2230 / 2240** |

- 正在跑的 2 个 MATLAB = `PCSAEA p7`（DTLZ7，run 17 于 22:15:49 完成）与 `CSEA p7`（DTLZ7，run 18 于 22:10:59 完成），
  它们在顺序推进剩下的 run。跑完这一轮 attempt，driver 会再查一次并启动 REMO/HES_EA 剩的 5 个 DTLZ7 跑。
- 无 poison、无 `*.tmp`、CPU 32%（只剩 2 片，符合预期）。**ETA 再 30–60 min。**
- 值得注意：**10 个缺口全是 DTLZ7** —— 再次印证 DTLZ7（IGDp 参考集 2¹⁹ 点）是绝对的长杆。

### ⚠️ 本轮新增 3 次超时（REMO +2、HES_EA +1），但**未被投毒、未丢数据**
累计 rc=124：REMO 20、HES_EA 11。原因：**`SLICE_TIMEOUT=21600`（6 h）仍不够 HES_EA 的 10 跑切片**——
实测 HES_EA 单跑已涨到 ~2300 s，10 跑 = 6.4 h > 6 h。
**为什么没被投毒**：被杀时已完成的 run 都已原子落盘，重驱时该片的 run 列表变短（例如只剩 3 跑），
`<runs>|rc` 的签名因此不同，不满足"同一签名被杀 ≥2 次"，所以没触发投毒。
⇒ 代价只是"最后那一个在跑的 run 白费"，不是整片报废。
**教训（下次设参数直接用）**：按算法的单跑 wall 反推——
`CHUNK × 单跑wall < SLICE_TIMEOUT`。慢算法（REMO/PACDIS/HES_EA，单跑 1600–2300 s）应使 **`CHUNK=5`**
（5×2300 = 3.2 h，6 h 有余量）；快算法（SSDE/PCSAEA/CSEA）可以用 CHUNK=10 甚至 20。
1. 补跑（目标 20 跑）完成后跑 `verify_runs.py --all`（**不要**用会崩的 MATLAB 版 verify）。
2. 之后可用 `build_table_runs10_IGDp.py 20` 出 **20 跑版**七算法 IGDp 对比表。
3. 若要进论文主表：需在**工位电脑**按项目 xlsx 规范出（Times New Roman 11pt、蓝标、`=-+` 符号）。
4. 巡检自动化 `852747ad-…` 已过期（validUntil 09-22 10:00）；本轮无自动巡检，需用户确认才能延长。

## 下一步（待用户决定）
1. 补跑（20 跑目标）完成后跑 `verify_runs.py --all`（**不要**用会崩的 MATLAB 版 verify）。
2. 之后用 `build_table_runs10_IGDp.py 20` 出 **20 跑版**七算法 IGDp 对比表。
3. 若要进论文主表：在**工位电脑**按项目 xlsx 规范出（Times New Roman 11pt、蓝标、`=-+` 符号）。
4. 巡检自动化 `852747ad-…` 已过期；需用户确认才能再延长。

---

## 22:20 用户要求暂停并出**最新版（20 跑）**表格

暂停在 **2231/2240**（只差 9 个跑，全是 DTLZ7：REMO 2、CSEA 1、PCSAEA 3、HES_EA 3）。
MATLAB 0、无 tmp、无 poison。脚本改名 `build_table_IGDp.py`（原 `..._runs10_...`，名字带死默认值不合适），
带跑数参数；产物 `FE500_M20_runs20_IGDp.md` / `.csv`。

### 🔴 20 跑版结论**比 10 跑版强硬得多**——HES_EA（原版）是真正的大麻烦

| 算法 | + 显著更优 | − 显著更差 | = 无差异 |
|---|---|---|---|
| REMO | 2 | 9 | 5 |
| PCSAEA | **0** | **15** | 1 |
| CSEA | 2 | 9 | 5 |
| **HES_EA** | **5** | 8 | **3** |
| SSDE | 1 | 15 | 0 |
| SAMOEATL2M | 1 | 15 | 0 |

**HES_EA 的 5 个胜场**：DTLZ5（0.0763 vs PACDIS 0.1830）、**DTLZ7（2.015 vs 16.368，8 倍）**、
WFG5（16.32 vs 18.12）、WFG8（15.50 vs 16.08）、WFG9（18.04 vs 19.22）。
（对比 10 跑版是 3 胜：DTLZ5/DTLZ7/WFG8 ⇒ **加大到 20 跑后 WFG5、WFG9 也转为显著**，
样本量上去后"=`"里藏的差异显形了，正如预期。）

**行内最低均值归属（20 跑）**：**HES_EA 7 题**（DTLZ2/5/7、WFG5/6/8/9）、
REMO 4 题（DTLZ1/3、WFG1/3）、PACDIS 4 题（DTLZ4/6、WFG2/7）、SSDE 1 题（WFG4）。
⇒ **HES_EA 拿到的"最好"比 PACDIS 还多（7 vs 4）**。

其它反例：DTLZ3 输给 REMO(135.3)/CSEA(152.7)；WFG3 输给 REMO/CSEA/SAMOEATL2M（三家都赢）；
WFG4 输给 SSDE(15.42)。**PACDIS 只在 DTLZ4/DTLZ6、WFG2/WFG7 四题上是最优。**

### caveat
- 4 个 DTLZ7 单元格 n<20：REMO 18、CSEA 19、PCSAEA 17、HES_EA 17（暂停导致）。DTLZ7 上 HES_EA 领先 8 倍，
  这点缺口不改变结论。
- HES_EA / PCSAEA / SAMOEATL2M 是**原版类**，`NI=11*D-1=329` 吃掉 66% 预算——
  **HES_EA 在预算劣势下仍赢 5 题、拿 7 个最优，这是最需要正视的一条。**
- 非配对 Mann-Whitney 口径 tally 基本一致（见产物文件）。
