# 2026-09-24

## NBGGP 完成 + M15 REMO 补齐（chain 自动衔接，06:12 收尾）

### NBGGP（NoBatchDist Good-group Precision）
- **160/160 全部完成**，用时 **126.5 min（2.1 h）**，rc=0。
- `finish_NBGGP` PASS：`160/160 validated; 8/8 configurations complete; FE=300; runs=20.`
- 完成标记 `results\FORMAL_COMPLETE.txt` 已写；分析产物 5 个 CSV 在 `results\analysis\formal\`：
  `NBGGP_CheckpointMetrics.csv`、`NBGGP_Coverage.csv`、`NBGGP_LabelDynNative.csv`、
  **`NBGGP_PairedComparisons.csv`**、`NBGGP_PerRunStage.csv`。
- **并行 wrapper 的实际收益**：串行 9.4 h → 并行 **2.1 h**（省 78%）。
  但仍比最初估的 40–50 min 慢：16 个子集分给 12 个 worker，子集内部仍串行 10 个 job，
  争抢下单跑从 215 s 涨到约 300 s。**下次同类任务应按「子集数 ≤ worker 数」来切**，
  即把 160 个 job 切成 12 份而不是 16 份，让每个 worker 只跟一个子集。

### M15 补齐（chain 自动执行）
`finish_NBGGP` → `RunSevenAlgsM15FE500('run',12)`（补 132 个 REMO）→ `MergeAllFE500IGDp(15,12)`
- **M15 现在 7 个算法全部 320/320**（20 跑 × 16 題），IGDp 完整（REMO `written 132, reused 188`）。
- chain END **06:12:32**。

### 三张表最终版本（IGDp，runs 1–20，锚点 = PACDIS，`+/-/=`）
| 算法 | M=10 | M=15 | M=20 |
|---|---|---|---|
| REMO | 1/9/6 | 2/10/4 | 3/9/4 |
| SSDE | 1/15/0 | 1/15/0 | 1/15/0 |
| PC-SAEA | 0/16/0 | 0/16/0 | 0/15/1 |
| SAMOEA-TL2M | 2/14/0 | 1/14/1 | 1/15/0 |
| CSEA | 0/14/2 | 2/11/3 | 2/10/4 |
| HES-EA | **6/8/2** | **5/8/3** | **5/7/4** |

- **M15 的 REMO 补齐后从 `2/9/5` 变成 `2/10/4`**（DTLZ5 由「不显著」变为「显著更差」）——
  说明样本缺口确实会影响结论，补齐是必要的。
- ⇒ **三批口径现已完全统一**（7 算法 × 16 題 × 20 跑，全部含 IGDp），可以直接并列引用。
- xlsx 三张已重建并校验：A1:J18、每行恰好一个蓝标、Times New Roman 11 居中细边框。

## pMix 灵敏度表（当前 NoBatchDist / PACDIS 配置）

### 数据坐标（工位机）
- `REMOandDREMO测试集\10目标\n30\` 与 `\20目标\` 下的
  `REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist[_PMix{000,025,075,100}]`
- **`PMix050` ≡ 目录 `..._NoBatchDist` 本体**（默认配置，全 16 题 × 20 跑 = 320 .mat）
- 四个扫描档各 **120 .mat = 6 题（DTLZ2/4/7、WFG1/3/8）× 20 跑**，M=10 与 M=20 对称
- 目录里**没有** `_PMix050` 目录，别去找

### 数据语义（本日新确认，重要）
- `metric.IGD` / `metric.IGDp` 是**一条 30 点的评价轨迹**（不是 30 次运行），
  M=10 与 M=20 都是 30 点；**末值 = 最终性能**，出表取 `arr[-1]`
- 配对设计佐证：同一 runId 下各 pMix 档轨迹前 2 点完全相同（初始样本同种子），之后才分化
- 对照：`REMO_new2_AdaMaO` 的 M=10 文件只有 18 点、M=20 为 30 点 →
  **跨目录比较前必须先核对轨迹点数**，不同批次的检查点粒度不一致

### 出表脚本与产物（2026-09-24）
- 脚本：`.workbuddy/run_scripts/build_pmix_tables.py`（配 `check_pmix_coverage.py` 查覆盖）
- 论文表：`论文写作\experiments\pmix_current_igdplus_table.tex`（主，IGD⁺）
  与 `pmix_current_igd_table.tex`（IGD，供对照）；**未 \input 进主文，等用户决定是否替换
  `pmix_historical_table`**
- 归档：`AdaMao实验表\lambdat030版本\pMix灵敏度_NoBatchDist\`（xlsx + 两份 md + 编译预览 PDF）
- 显著性：**配对 Wilcoxon 符号秩检验**（同种子 20 对，α=0.05），锚点 p_mix=0.50；
  与历史表「PlatEMO 自带 export 标注」不是同一来源，脚注已写明
- ⚠️ 新表数值**不能与历史表（batch-distance 版、IGD）直接比对**——批次/配置/指标口径均不同

### 结论（IGD⁺，5 档）
- 默认 p_mix=0.50 在两类目标数下平均秩都最好（M=10: 2.00；M=20: 2.17；
  合并 2.08），p_mix=0（纯歧义模式）最差（合并 4.17）
- DTLZ7 是最敏感的问题：p_mix 越大越差（M=10 从 1.03e+1 单调升到 1.91e+1），
  p_mix=1.0 显著恶化；DTLZ2/DTLZ4/WFG8 在 p_mix→1.0 时也显著变差
- WFG1/WFG3 基本无差异（多数 = ）；M=20 整体比 M=10 更不敏感（多数 = ）
