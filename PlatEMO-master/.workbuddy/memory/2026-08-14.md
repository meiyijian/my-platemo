# 2026-08-14 工作日志

## Git 提交与推送：UniformMix LabelValidation 实验代码

- 需求：把 `PlatEMO/Experiments/REMO_new2_AdaMaO_UniformMix_LabelValidation` 除 `results/` 外全部上传并推送。
- 关键发现：git 仓库根目录是 `D:/PlatEMO-master`（不是 `D:/PlatEMO-master/PlatEMO-master`）。
  `.gitignore` 第 8 行 `PlatEMO-master/PlatEMO/Experiments/` 忽略了整个 Experiments 目录，导致实验代码默认被忽略，
  需要 `git add -f` 强制添加。
- 操作：`git add -f` 整个实验目录 → `git reset -- results` 撤销 results 暂存 → 提交。
  - 提交 `6d1173d`：52 个新文件（20 个顶层 .m + algorithms/ + private/ + tests/ + 代码与Plan对照审查报告.md），7423 行新增。
  - `results/`（约 1.1G，含 .mat 数据）已正确排除，未入库。
  - 另有 5 个 Plan .md 之前已跟踪，合计远程该目录 57 个文件。
- 推送：最终确认已成功推送到 `origin/master`（`git ls-remote` 返回 `6d1173d`，本地与远程完全同步）。
  - 过程小插曲：首次 push 返回 exit 1 + 空输出、二次 GIT_TRACE 显示 credential 提示失败，
    但事后验证远程已含该提交，实际推送成功（推测 GCM 有缓存凭据，首次已静默推成功）。
  - 环境备忘：git 仍配置代理 `http://127.0.0.1:7897`；HTTPS 到 github.com:443 当前可通（TLS 正常）。
