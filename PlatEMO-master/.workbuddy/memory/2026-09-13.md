# 2026-09-13 工作日志

## 排查「后台 MATLAB 任务显示已挂起」

- 用户报告后台 MATLAB 任务显示「已挂起」，要求继续运行。
- 排查结论：**无任何 MATLAB 进程在运行，也没有任务真正挂起** —— WorkBuddy 后台任务面板显示的是 8 月 30 日会话遗留的陈旧条目（会话结束后面板条目不会自动消失）。
- 实际状态：CDR formal 实验（400 jobs = 4 arms × 5 问题 × 20 run，M=10）已于 8-30 当天由 3 路并行 MATLAB 全部跑完（WFG2 16:38 / DTLZ6 18:31 / WFG7 18:56 完成），results 下共 404 个 .mat，最终分析也已在 8-30 20:04-20:06 完成（report_final_ladder / mechanism / threshold.txt + 最终结论_哪部分可写.md）。
- 顺带发现：自动化列表里有 3 个陈旧任务——两个 8-30 的一次性 CDR 巡检（早已过期但仍显示 ACTIVE）和已暂停的 Stage1 每小时巡检（Stage1 已完成，已无用），已向用户说明可清理。
- 环境问题备注：本会话 Bash/PowerShell shim 一度 PATH 异常（`dirname`/`ls` not found），用 PortableGit 完整路径（/c/Users/lsx/.workbuddy/binaries/PortableGit/versions/1.2.0/usr/bin/*.exe）绕过；PowerShell 工具返回空输出，Bash + 全路径可用；PowerShell 输出可用 Out-File 到 %TEMP% 再 Read 绕过。

## 20:33-20:45 排查「任务管理器三个 MATLAB 一个显示已挂起」

- 真相：`RunPaperWeighted30('run',2)` 实验**正在运行**（9-12 23:05 启动，非挂起）。进程树：cmd.exe(19148) → matlab.exe launcher(41648, -wait -batch) → MATLAB.exe 引擎(41532) → 2 个 parpool Processes worker(30312/40416)；另有 9-11 打开的交互式桌面 MATLAB(19528) 空闲中。
- 实测活性（6 秒 CPU 增量）：两 worker +1.42s/+1.22s（在算），launcher/引擎 0（纯等待属正常设计），无任何进程全部线程挂起；两 worker 累计 CPU 29290s vs 29261s（差 0.1%）证明全程无实质挂起。
- 结论：任务管理器「已挂起」是 Win11 对完全空闲后台进程（-wait launcher / fetchNext 引擎）的节能冻结显示，属于无害标签；不可结束任何 MATLAB 进程。
- 进度：日志 `diagnostics/paper_30runs/run_20260912_230537_869.log`，892/2208（40%），速率约 41 job/h，预计还需 ~32h（约 9-15 早晨完成）。结果原子写入（temp→validate→movefile），RUNNING.lock 防双开，重跑 `RunPaperWeighted30('run',2)` 可断点续填。
- 实验规格：7 算法(REMO/PIEA/CSEA/PCSAEA_N100/KRVEA_100/MCEAD/Weighted) × 16 题 × M=10/15/20 × 30 runs=10080，本次补缺 2208，数据写 C:/Users/lsx/Desktop/REMOandDREMO测试集/{10目标n30,15目标,20目标}。
