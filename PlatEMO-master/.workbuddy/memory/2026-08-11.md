# 2026-08-11 工作日志

## 尝试拉取最新仓库代码（失败，网络问题）

- 用户要求 `git pull origin master`
- 现状：当前沙箱环境无法与 GitHub 建立 TLS 连接
  - 走 Clash 代理 127.0.0.1:7897：TCP 能建连（Connection established），但 TLS 握手失败（unexpected eof / schannel failed to receive handshake）
  - 直连：同样 TLS 握手失败
  - 判定：Clash 节点疑似失效或被墙，无法连通 GitHub；非命令/仓库问题
- 本地状态（好消息）：工作区干净，`git log origin/master..HEAD` 为空，显示 "up to date with origin/master"
  - 说明 7/30 强制添加的 CONCLUSIONS.md 提交已成功推送（本地不再领先），无数据丢失
  - 但无法 fetch 远程可能的新提交（网络不通）
- 待办：需用户在能联网的终端（确认 Clash 节点可访问 GitHub 后）手动 `git pull origin master`

## Stage1 UniformMix LabelValidation 实验（晚间，21:25-23:42+）

- 实验目录：`PlatEMO/Experiments/REMO_new2_AdaMaO_UniformMix_LabelValidation/`
- 目标：审计冻结算法 `REMO_new2_AdaMaO_SDEOnly_UniformMix_Original` 的标签机制（HybridPBI_Classification 的 good/bad 划分）是否真实驱动方向选择
- **已建成完整管线**：协议(LabelValidationProtocol) / schema / 稳定种子 / 审计算法(LVUniformMixAuditBase + Hybrid/AnchorNative 两行为入口) / runner(可断点续跑+原子写) / validator / analyzer / 测试套件
- **等价性验证通过**：审计类与冻结类逐值一致（决策/目标/IGD/IGDp 全 0 差异），两个关键修复：
  1. patternnet `train()` 首次调用额外消耗 RNG → 会话开始时 warm-up 训练一次
  2. 冻结入口第 31 行 `Lp = 1;` 初始化被遗漏 → 导致 IndicatorSelectorSDEOnly 路径分叉
- smoke（2 作业）通过，决策 PASS_TO_STAGE2；pilot（4 作业）数据质量优秀（M10 下 68/68 snapshot 全部 ND_KMEANS 方向）
- **screening 100 作业**（5 问题×M10/20×5 runs×2 behaviors，maxFE=500, gmax=3000，单作业约 8.4 分钟）已按 (problem,M) 拆成 10 个并行 MATLAB 进程（-singleCompThread）后台运行，预计 1.5-2.5 小时
- 定时任务已创建：`automation-1786463086351`（每小时检查 screening 进度，用 check_Stage1Progress.m）
- 结果归档目标：`C:\Users\lsx\Desktop\AdaMao实验表\Stage1_UniformMix_LabelValidation\`（文件夹已建）
- 经验：并行跑 runner 时 analyzer 直接从磁盘扫描 run_*.mat，manifest 覆盖无碍；`matlab -batch` 输出被管道缓冲时无法实时看进度
