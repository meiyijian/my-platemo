# 2026-08-13 工作日志

## 代码 vs Plan 对照审查（REMO_new2_AdaMaO_UniformMix_LabelValidation）

对照 5 个 Stage Plan（01–05）审查了实验代码，结论：Stage 1–3 已实现且大体忠实，Stage 4/5 未实现。审查报告已写入实验目录 `代码与Plan对照审查报告.md`。

主要发现（严重问题集中在 Stage 3）：
- ComputeExternalLabelMetrics.m:38 `nNonTie = nnz(U ~= U(1))` 非「有效非并列对数」判定，属 bug（应统计 pairs 中 dy≠0 对数）
- Stage 3 Holm 校正未实现（analyze_IndependentUtilityValidation.m:320 `hpv=pv` 占位）
- Stage 3 决策只用 median(delta) 符号，未纳入效应量/CI（违反 plan §10）
- run_IndependentUtilityValidation.m:60 gate 允许列表混入 4 个 Stage3 决策代码（应为仅 PASS_TO_STAGE3）
- checkpoint 选择偏离 plan（应标 UNAVAILABLE_CHECKPOINT，代码退而求其次）+ targetRatios(c) 索引错位 bug
- BuildLabelUtilityReferenceSet 统一用 GetOptimum(16384) 取前缀，plan §4.1 要求非 DTLZ7 用 4096/8192 分别生成 → 实锤：screening 已出 INSUFFICIENT_REFERENCE_STABILITY（DTLZ7_M10）
- Stage 3 L6 未算 rank 指标；H2 打乱门槛 median>0.50 过宽松
- Stage 1 runner 缺前置 which 检查；Stage 2 INSUFFICIENT_DATA 粒度不一致；overlap 对称性检查退化

当前运行状态：Stage1 screening PASS_TO_STAGE2（100 valid）；Stage2 screening PASS_TO_STAGE3（WarningFlags=SCHEDULE_REDUNDANT）；Stage3 screening INSUFFICIENT_REFERENCE_STABILITY（DTLZ7_M10）。
