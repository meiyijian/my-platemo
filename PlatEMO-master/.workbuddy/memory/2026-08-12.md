# 2026-08-12 工作日志

## Stage1 screening 实验进度检查（定时任务 automation-1786463086351，00:40）

- 首次运行进度检查：`check_Stage1Progress('screening')`
- 结果：Expected=100，Completed(valid)=31，Invalid=0，Pending=69（31%）
- allDone=false，未运行分析器，仅简要报告
- 速率估算：10 个并行进程下约 18 分钟/作业/进程，预计约 02:30-03:00 全部完成

## Stage1 screening 实验完成 + 分析器运行（定时任务，02:37）

- 进度检查：**100/100 完成且有效**（Invalid=0, Pending=0）→ allDone=true
- 运行 `run_LabelMechanismSnapshotAudit('screening')` + `analyze_LabelMechanismSnapshotAudit('screening', equivalenceFile=...)`
- **修复 bug**：`analyze_LabelMechanismSnapshotAudit.m` 第 110 行 `dirRows(end+1,:)=row` 尺寸不匹配（1×8 vs 1×10）——不同组合 FallbackReason 种类数不同。改为 cell 收集 + 统一补宽 + 动态表头（reasonN/countN）
- 分析结果：**Decision = PASS_TO_STAGE2**（100 valid、等价性 PASS、自适应覆盖≥50%）
  - 方向来源：KMEANS 自适应主导（18/20 组合 100%；DTLZ4 M20 回退 OBJECTIVE_RANGE_LT_1E12；DTLZ7 M10 Hybrid 回退 FRONT1_LT_THRESHOLD）
  - 候选模式：fallback=0，indicator≈32 / explore≈35 代，每代 ~5.97 评估
  - 分支重叠：ScoreV-LabelDyn 相关 DTLZ 强 WFG 弱；ScoreV TopQ 与 Margin TopQ 重叠低（0.06-0.34）
- 产出：`results/stage1/screening/analysis/` 下 7 个文件 + `Stage1_screening_summary.md` 汇总报告
- 待用户操作：可归档实验到桌面 AdaMao实验表 文件夹；Stage1 全部通过后可启动 Stage2

## Stage1 全部完成 + 归档到桌面（08:00）

- 用户询问"实验结果在哪里，为什么文件夹什么都没有"——因为结果一直在项目目录，尚未归档
- 确认 10 个并行进程全部完成：**100/100 run_*.mat 有效**，无 tmp 残留
- 重跑 analyzer（约 40s，加载 100 文件）：Decision = **PASS_TO_STAGE2**（与 02:37 定时任务跑的结果一致）
- **归档完成**：`C:\Users\lsx\Desktop\AdaMao实验表\Stage1_UniformMix_LabelValidation\`（131 文件，495MB）

## Stage2 因果消融管线实现与验证（09:30-13:10）

- 计划：`02_Stage2_LabelCausalAblation_Plan.md` — 纯离线因果消融，不产生任何新 FE
- 已实现 11 个文件：LVTopQDeterministic / ComputeLabelAblationVariants（L0-L8 九变体）/ ComputeUniformDirectionScore(L7) / ComputeReducedNDDirectionScore(L8) / ComputeLabelOverlapMetrics / ComputeLabelPerturbationStability / ComputeShuffleEnvelope(L3vsL6) / run_LabelCausalAblation / ValidateLabelCausalAblationFile / analyze_LabelCausalAblation / tests/test_LabelCausalAblation + private/LVHashString, LVStageBin, LVVariantTable
- 单元测试 10/10 通过；期间修 3 个测试自身 bug（TopQ 并列、PBI 方向语义、struct 广播）
- **关键经验**：
  1. `LVHashString` 初版不支持 struct（metadataHash 传入 struct 报错）→ 已改为通用 MD5（数值用 %.17g 规范化）
  2. smoke 首代种群宽度=initialFE(32)≠problemN(20)，validator 正例数检查须用实际 PopulationWidth
  3. L6 shuffle 用 offlineSeed=snapshotSeed+6*100+replicate，调用前后恢复全局 RNG（已验证）
- 正在验证 smoke → pilot → screening（100 作业）
- **smoke 排错中**：
  1. `LVHashString` 初版不支持 struct（metadataHash 传入 struct 报错）→ 已改为通用 MD5（数值用 %.17g 规范化）
  2. `ComputeLabelOverlapMetrics` 行字段（无 Replicate）与 `ComputeShuffleEnvelope` 行字段（有 Replicate）不一致 → 追加到同一 overlapRowsAll 触发 MATLAB:heterogeneousStrucAssignment；已给 overlap 行统一加 Replicate=0 字段
  3. smoke Stage1 metadata 无 theta 字段（theta 在 snapshot 里）→ runner metadata 改为从 snapIn(1).Theta 取
  4. analyzer 的 runRec 用 struct 数组 → 嵌套字段长度随 run 变化触发 heterogeneousStrucAssignment；改为 **cell 数组**（runRec{i}）
  5. **文件格式**：Stage2 MAT 用 -v7.3（HDF5）时单文件 636MB（100 作业=64GB）；改 **-v7 仅 4.6MB**（99% 节省）——小字段 struct 数组用 v7 即可
- **Stage2 smoke 完成（14:55）**：2 作业全 completed（各 1 snapshot），Decision=INSUFFICIENT_DATA（DTLZ2 仅 1 paired run < 4，smoke 预期行为）；9 个分析文件生成
- **screening 测速（15:26）**：单作业（67 snapshots）**211 秒**，v7 文件 4.6MB
- **归档**：smoke 已复制到 `C:\Users\lsx\Desktop\AdaMao实验表\Stage2_LabelCausalAblation\smoke\`（11 文件，18MB，含 README_归档说明.md）
- **定时任务已创建**：`automation-1786519651245`（每小时检查 Stage2 screening 进度，只读，不干预用户运行）
- **用户将自行运行 screening**：已提供手动命令；screening 100 作业 10 并行约 35-40 分钟，串行约 5.9 小时

## Stage2 screening 并行模式（15:30-15:43）

- **给 runner 增加 `'Parallel', true` 参数**（内部 parfor + parpool，6 workers）——一条命令自动并行，无需手动开 10 终端
- 重构：主循环改为 `runOneJob(job,stage1Root,stage2Root,profile)`（parfor-safe，无共享状态），manifest 按 job 顺序重排
- 验证：DTLZ2 M10 runs1-2 × 2 behaviors = 4 作业并行，parpool 启动 6 workers，**241 秒**（含 parpool 启动 ~40s），全部 67 snapshots completed
- 用户手动运行命令：`run_LabelCausalAblation('screening','Parallel',true);`（100 作业 6 workers 预计 ~60 分钟）

## Stage2 screening 进度检查（定时任务 automation-1786519651245，16:23）

- `check_Stage2Progress('screening')`：**Expected=100 | Completed(valid)=39 | Invalid=0 | Pending=61**（39%）
- allDone=false，未运行分析器
- 实验活跃推进中：最近 1 小时新增约 40 个 run_*.mat，最新文件时间戳 16:26（约 4-6 个并行实例）
- 预计剩余：按当前速率约 1 小时；10 并行理论约 25-30 分钟

## Stage2 analyzer 增加 archiveDir 自动归档（15:45）

- **analyze_LabelCausalAblation 增加 `'archiveDir', path` 参数**：分析完成后自动 copyfile 整个 profile 结果树（analysis + 原始 MAT）到指定目录（如桌面 AdaMao实验表\Stage2_LabelCausalAblation）
- 用户最终命令：`run_LabelCausalAblation('screening','Parallel',true); analyze_LabelCausalAblation('screening','archiveDir','C:\Users\lsx\Desktop\AdaMao实验表\Stage2_LabelCausalAblation');`
- 默认输出仍在项目目录 `results/stage2/<profile>/analysis/`，archiveDir 仅为可选自动归档

## Stage2 screening 进度检查（定时任务 automation-1786519651245，17:29）

- `check_Stage2Progress('screening')`：**Expected=100 | Completed(valid)=92 | Invalid=0 | Pending=8**（92%）
- allDone=false，未运行分析器
- 1 小时内新增 53 个 valid（39→92）；最新 run 文件写入于 17:12，实验仍活跃（AnchorNative/Hybrid 各 46 个）
- 预计：按当前速率约 8-10 分钟完成全部；下次定时检查（约 18:30）大概率 allDone=true，届时运行分析器 + archiveDir 归档提示

## Stage2 WFG7 M20 8 个 failed 作业诊断与修复（19:25-19:45）

- **现象**：screening 卡在 92/100，剩 8 个作业全部 failed（WFG7 M20 run002-005，Hybrid+AnchorNative 各 4）
- **根因**：`ValidateLabelCausalAblationFile.m` 对 L0 变体强制正例率 ∈ [0.3,0.7] 带检查；但 L0 是 Stage 1 历史基线（Catalog==LabelDyn），30-70% 只是 Stage 1 自适应 delta 的**目标带**（见 `LVUniformMixAudit_AnchorNative.m` 注释 "target 0.30-0.70"），非协议硬约束。WFG7 M20 早期快照 LabelDyn 正例率低至 0.22，被误杀
- **证据链**：issue 数（1/1/1/3、1/3/1/3）与 L0<0.3 越界快照数逐一吻合；Stage1 validator 对 LabelDyn 比例无任何约束（8 个输入文件均 valid）；方案 §4.2 明确 L0 忠实复现 LabelDyn，不用于单因素证明
- **修复**：`ValidateLabelCausalAblationFile.m` 第 150-156 行改为合法比例 sanity 检查（仅排除 pct<=0 或 pct>1）
- **重跑**：`run_LabelCausalAblation('screening')` 串行后台重跑全部 100（92 个 valid 将 skip，8 个 failed 重算）——保证 manifest 完整；已备份旧 manifest 为 .bak
- **重跑完成**：100/100 全部 valid（8 个 failed 修复通过），耗时约 48 分钟
- **发现 analyzer 潜伏 bug**：`analyze_LabelCausalAblation.m` `computeDecision` 的 fracF 用逆序 pair 键 `'L3|L1'/'L3|L2'`，但 `ComputeLabelOverlapMetrics` 生成的是字典序键 `'L1|L3'/'L2|L3'`；因之前 hasFailedJob 在第 0 步提前 return 从未执行到，bug 潜伏至今才暴露 → 已修复键顺序（第 399-400 行）
- **最终分析（20:53）**：**Decision = PASS_TO_STAGE3**，WarningFlags = **SCHEDULE_REDUNDANT**（100 valid, 0 invalid）
  - L3 vs L1 Jaccard run-median **0.389**（min 0.136/max 0.724）；L3 vs L2 **0.613**（min 0.19/max 0.85）→ 非平凡分歧存在
  - **L3 vs L4 = 1.0000，L3 vs L5 = 1.0000**（100 个 run 全部完全一致）→ 动态调度无独立作用 → SCHEDULE_REDUNDANT
  - L2 vs L7 0.266（min 0.02）；L2 vs L8 0.389；L7 vs L8 0.282
  - 分歧集合（对称差中位数）：L1|L2 31.0、L1|L3 21.9、L2|L3 13.2
  - L3 vs L6 shuffle envelope：EARLY 0.376 / MIDDLE 0.357 / LATE 0.370（P025≈0.26，P975≈0.51-0.58）→ L3 落在 envelope 内部，与打乱基线无明显差异
- **待用户操作**：结果已就绪，可归档到 `C:\Users\lsx\Desktop\AdaMao实验表\Stage2_LabelCausalAblation`（analyzer 支持 `archiveDir` 参数自动归档）

## Stage2 screening 归档完成（21:04）

- 按用户指令执行归档：复制最新 screening 结果树（476M）到 `C:\Users\lsx\Desktop\AdaMao实验表\Stage2_LabelCausalAblation\screening`（覆盖 17:31 旧归档）
- 验证：DecisionCode=PASS_TO_STAGE3、WarningFlags=SCHEDULE_REDUNDANT；run_*.mat 100 个齐全；WFG7 M20 AnchorNative/Hybrid 各 5 个
- Stage2 全部完成，待用户启动 Stage3（02_Stage2 plan 交接要求：需同时提供 Stage1/Stage2 MAT、双阶段 manifest 和 decision CSV）

## Stage2 进度检查（定时任务 automation-1786519651245，20:28）—— 确认完成

- `check_Stage2Progress('screening')` → **100/100 valid**（allDone=true）——用户已补跑 WFG7 M20 8 个 failed 作业（19:39-20:16）
- 本任务首次运行分析器失败：错误 "此容器中不存在指定的键"，源于 `computeDecision` 的 `fracF` 用逆序键（`'L3|L1'` 等）而数据是字典序（`'L1|L3'`）——与用户 19:45 修复的 bug 同一处；运行期间（20:34-20:38）用户恰好正在改文件，重跑（当前版本）成功
- 重跑结果与用户 20:53 分析一致：**PASS_TO_STAGE3 / SCHEDULE_REDUNDANT**；诊断脚本（diag_pairkeys/allkeys/repro/wrapper/iskey）已清理
- 新观察：用户已开始搭建 Stage3（`analyze_IndependentUtilityValidation.m` 21:20、`ValidateIndependentUtilityFile.m` 21:30 出现）

## Stage3 独立效用验证实现 + screening 完成 + 归档（21:11-22:35）

- **从零实现 Stage3**（方案 03_Stage3_IndependentUtilityValidation_Plan.md，11 文件 + 测试）：
  - 基础：LVIGDPlus（确定性 IGD+）、BuildLabelUtilityReferenceSet（嵌套参考集 R4096⊂R8192⊂R16384 + DTLZ7 专用 Latin 构造）、ComputeGreedyIGDPlusOracle、ComputeLeaveOneOutIGDPlus
  - 评价：ReconstructFutureLabelOutcomes（H1/H3/FINAL 未来重建）、ComputeExternalLabelMetrics（Precision/Recall/Jaccard/NDCG/Kendall/AUC + L0 native + L6 分布）、ComputeDisagreementUtility（L2vsL1/L3vsL1/L3vsL2 分歧效用）
  - 管线：run_IndependentUtilityValidation（Parallel 参数 + refSize 参数 + resume/block）、ValidateIndependentUtilityFile、analyze_IndependentUtilityValidation（run-cluster bootstrap 10000 + Holm + 决策）
- **测试**：单元测试 8/8 通过（修 3 处：IGD+ 方向理解错误、greedy 冗余解用例、future censor 断言）；smoke→pilot→screening 逐步验证
- **关键经验**：
  1. **参考集必须嵌套前缀**（Rmaster(1:4096)/Rmaster(1:8192)），独立 GetOptimum 采样会致敏感性失败（DTLZ7 M10 Jaccard 0.884<0.90）
  2. **R16384 实际行数因问题而异**（DTLZ2 M10=13442、M20=10395；UniformPoint 返回行数≠请求），analyzer 判定"已升级"须用 `~=4096` 而非 `==16384`
  3. analyzer 的 max12 比较须走 runJacOf（支持 max(L1,L2)），直接 variant 索引会 0 runs
  4. Stage3 单作业比 Stage2 快很多（100 作业 6 workers 约 3-4 分钟，只算 5 检查点非 67 快照）
- **screening 100/100 valid**（0 failed）；**Decision = INSUFFICIENT_REFERENCE_STABILITY**
  - 唯一不稳定单元 **DTLZ7_M10**（R16384 升级后 AN Spearman=0.945<0.95，DTLZ7 PF 4 段不连续致参考点敏感）；其余 9/10 单元敏感性通过
  - 主分析（稳定单元）：L2 vs L1 DTLZ +0.027/WFG **-0.066**（负）；L3 vs max(L1,L2) DTLZ -0.008/WFG **-0.052**（L3 未优于单支路）；L3 vs L4 ≈0；L3 vs L5 ≈0；L2 vs L7 DTLZ -0.020/WFG **-0.075**（均匀方向不差于非支配）；L2 vs L8 负
  - **L3 shuffle percentile mean=0.164**（500 行）→ L3 的 Oracle Jaccard 落在打乱分布低分位，**未超过打乱对照**——与 Stage2 的 envelope 结论一致，融合标签无外部效用增益
- **归档完成**：`C:\Users\lsx\Desktop\AdaMao实验表\Stage3_IndependentUtilityValidation\screening\`（59M，9 分析文件 + 100 MAT）
- **自动化已建**：automation-1786542181288（每小时查 Stage3 进度；平台 RRULE 不支持 MINUTELY，无法做 30 分钟粒度）
- **关键发现（供论文）**：Stage3 未发现"当前融合标签"的外部效用证据——L3 未超过 L1/L2 单支路或 L6 打乱，且均匀方向(L7)不弱于非支配方向(L2)。按方案 §10 应为 **NO_EXTERNAL_LABEL_EVIDENCE** 倾向（但被 DTLZ7_M10 敏感性 gate 挡住），建议停止"更好标签"主张或重审 Stage2 PASS 结论
