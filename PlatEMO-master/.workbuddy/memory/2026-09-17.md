# 2026-09-17

## 中文对照稿同步至最新英文稿（`论文写作/HPDC-MaOEA_中文版.md`）

- 触发：用户要求「翻译为中文版 md，并覆盖之前的，同时提交推送」→ 走 skill `paper-cn-mirror`。
- 源：`HPDC-MaOEA.tex` @ 2026-09-17 10:42，SHA-256 `05BBCD42F2BA1191FC925471AF41E1F73FE1A353BF2BFD98C1FF1870F30D7347`，HEAD `387f7d8`（tex 工作区干净，无未提交改动）。
- 编号现状（用脚本按源码顺序核过，勿凭记忆）：式 1–18、算法 1–3、图 1–3（新增收敛图）、表 1–4（新增消融表与 p_mix 表）、命题 1、参考文献 [1]–[10]。
- 式号关键位：`eq:prefilter-exp`=10、`eq:ambiguity`=11、`eq:expscore`=12、`eq:greedy`=13（旧版探索分数是式 10，改动后极易错位）。
- 相对上一版中文稿（对应 09-15 15:01 英文稿）的变化：§3.4.1 恢复 λ=0.30 固定模糊度奖励；§4.3 由因子设计稿改为四配置实测消融（16 题 × M=10/20）；§4.5 由占位改为 p_mix 灵敏度（新表 4）；§4.6 由占位改为收敛结果（新图 3）；删除旧 §4.4「代表解数量的对照」整节。
- 工具链：新增 `.workbuddy/run_scripts/tex_ablation_pmix_to_md.py`（`ablation` / `pmix` 两模式），配合既有 `tex_table_to_md.py` + `fill_table_placeholders.py`。
  - 坑 1：tex 中 pMix 表的 `\label{tab:exp:pmix}` 在 `\begin{tabular*}` **之前**，定位必须向后找 begin。
  - 坑 2：ablation 表用 `\mathbf{}` 而非 `\textbf{}`，主表脚本的加粗/剥离逻辑不通用。
  - 坑 3：分组横幅行后面不要再输出 `|---|` 分隔行，否则一张表被切成两张。
- 提交：`26d19fb`（md + 新脚本，2 files / +273 −70），已推送 origin/master；`git ls-remote origin master` == 本地 HEAD（`git status -sb` 的 ahead 209 仍是已知假象）。提交信息经 python `decode('utf-8')` 验证合法。
- 未改 tex → 未触发编译，符合 `论文写作/AGENTS.md` 的触发条件。
- 复核发现（按 skill 要求不擅改，只点名）：英文稿 §4.2 称「MCEA/D has the lowest mean on DTLZ3 at 15 and 20 objectives」，但表 1 中 M=10 的 DTLZ3 最低均值也是 MCEA/D（5.4540e+2）。该口误上一版中文稿即存在，本轮照抄英文稿并在交付说明里点名。

## GoodGroupPrecision 跨算法版本结论对拍

- 触发：用户问「工作区里不同算法版本的 goodgrouprecision 实验结论是否类似」。
- 三臂定位（都跑同一冻结协议：N=100、D=30、maxFE=500、25 跑、种子 `problemIndex*10000+M*100+run`、3 视图 × 6 真值 × 4 阶段、top25 配额）：
  - A `Experiments\REMO_new2_AdaMaO_GoodGroupPrecision`（10 题 × M10/20，500 跑全齐；qKeep=0.80、lambda0=0.35）
  - B `Experiments\REMO_UniformMix_Pruned_Weighted_Lambdat030_GoodGroupPrecision`（DTLZ2/4/5 × M10/20，150 跑全齐；qKeep=0.70、λ_t=0.30）
  - C PWGGP（λ_t=0.50）**原始目录已不在**，只剩 B 的 `results\analysis\crossArm\LTGGP_vs_PWGGP_Paired.csv`（40 行，无逐 run 值 → 无反推 p 值能力）
  - 已实测 A/B 在 DTLZ2-M10-run1 的 Seed 都是 11001 → 逐跑种子对齐，B 是「同问题同种子换算法版本」的复现。
- 结论：**共有 3 问题上高度一致**。864 个 hybrid 对比单元符号一致 87.9%、Pearson 0.941；hybrid vs `population_final` 的 Precision 0.3496(A) vs 0.3535(B)、excess +4.11 vs +4.06 pp、Lift 1.430 vs 1.421；逐阶段 hybrid−score_v 优势 S1 最强后单调衰减，两臂同形（+3.41/+2.19/+2.47/+1.57 vs +3.47/+2.25/+2.00/+1.41）。
- 三个必须交代的差异：①B 只覆盖 DTLZ2/4/5，正落在 A 的正面区域 —— A 在其余 7 问题 excess −3.81 pp（WFG 四题 −10.46 pp），B 无法检验；②唯一逐阶段符号分叉在 S4 的 hybrid−anchor（A +1.70 pp / B −0.27 pp）；③跨臂不同会话，只能作确认性证据。
- 另一条结构性事实（两臂共有，写论文必须交代）：hybrid 相对 score_v 的优势**只在 `population_*` 真值上为正**（+1.06/+0.97 pp），在 `front_*` 真值上是负的（−0.32/−0.36 pp）；聚合 144 格看胜格率只有 42–43%，与主口径逐阶段表看似矛盾。
- λ 维度：λ0.30 的 IGD 更好但 hybrid Precision 全面更低 → 「标签质量」与「最终 IGD」反向，与 A 审计里「视图排序质量与最终 IGD+ 无显著关联」同源。
- 产出：归档 `C:\Users\lsx\Desktop\AdaMao实验表\GoodGroupPrecision_跨版本对拍\GoodGroupPrecision_跨版本结论对拍.md`；脚本 `.workbuddy\run_scripts\ggp_cross_version_compare{,2}.py`；中间表 `.workbuddy\_ggp_compare{,2}.md`。
- 环境：本机 managed python 无 numpy/scipy → Wilcoxon 用正态近似 + 并列校正手写；正式显著性一律引两侧 CSV 自带 `PValueHolm`。

## §4.4 GGP 段改写示范（PAQC 贡献向）

- 触发：用户问「Lambdat030 GGP 结论怎么写进小论文才能体现 PAQC 的贡献」→ 只做示范文本，**未改 `HPDC-MaOEA.tex`**。
- 论文现状：§4.4 `\label{sec:exp:ggp}` 在 tex 第 986–1015 行，仍是设计骨架 + `\TODO{...}` 占位（要补 outcome/horizon/checkpoint/tie rule/coverage/run-level 聚合 + 结果与不确定性）。§3.2 已有 PAQC 三小节，式 `eq:score-cont`(S)、`eq:label-bin`(L)、`eq:fuse`(H, α_t=1−t)、`eq:group`(C_1)。§4.3 消融已给"PAQC 有贡献"（Full vs Full-PAQC 相对 REMO 8/1/7、10/1/5；去掉 PAQC 在 DTLZ5 两目标数都最差）。
- 三视图 ↔ 论文记号（改写时必须显式写清，否则审稿人问"融合的是 L 为何拿 A 比"）：`score_v`=S；`anchor_margin`=A=1−归一化 g，是 L 阈值前的连续裕量、**为等基数比较构造的参照分数**；`score_hybrid`=H。
- 核心贡献论证（LTGGP 实测，Precision@25%、144 格/对比、Holm）：H vs A 52:25（保留类 36:6、front_* 16:19）；H vs S 15:15，拆开为保留类 15:3（+0.97pp）、front_* 0:12（−0.36pp）。绝对水平 H/A/S：Precision 0.3535/0.3432/0.3307、Chance 0.3129、excess +4.06/+3.03/+1.78pp、Lift 1.421/1.292/1.214、AUC 0.609/0.600/0.540。
- ★最专属的一条：**S 与 A 按预算区间分工**——S−A 逐阶段 +3.369(19:2)/+1.439(17:6)/+0.230(8:6)/−0.692(2:9)，与 `eq:fuse` 的 α_t=1−t 同向；且 H−A ≥ S−A 在前三阶段（保留类 +2.249/1.300、+2.199/0.923、+1.426/0.419），S4 时 |H−A|=0.122 仅约 |S−A|=0.774 的六分之一 → 融合跟随当期占优分支。价值主张＝"把两个各自只在部分预算区间有效的信号拼成全预算可用的排序规则"，而非"加权平均总更好"。
- 反例（必须同写）：DTLZ4-M10 相对 S +6.93pp 显著但相对 A −1.84pp；DTLZ2-M20 相对 S −0.27pp；DTLZ5-M20 H=33.67% 略低于 Chance 33.77%；聚合后 front_* 族相对 S 为负。
- 最好的举例单元：DTLZ4-M20-S2 —— `population_final` H 22.19% / S 18.37% / A 18.45%（+3.83、+3.75pp，Holm p=0.0017、0.0343）；`population_h3` 94.73%/90.23%/91.22%（p=1.5e-4、1.6e-4）。全表仅 3 个"同时显著优于两成分"的单元，全在该设置。
- 仍缺、投稿前要补：①excess 无正式配对 p 值（只有 74.7% 为正的描述比例）；②AUC 有 22 个单元有效配对数<25，列 AUC 需脚注；③**没有"动态融合 vs 固定 α"对照臂**，这是 PAQC 故事唯一缺口、也最易被审稿人问。
- 产出：`C:\Users\lsx\Desktop\AdaMao实验表\GoodGroupPrecision_跨版本对拍\§4.4_PAQC_GGP_改写示范稿.md`（含中文示范稿 + 英文 LaTeX 就绪版 + 4 张表 + 交接曲线图建议）；脚本 `.workbuddy\run_scripts\ltggp_paper_numbers.py`、`ltggp_complementarity.py`。
