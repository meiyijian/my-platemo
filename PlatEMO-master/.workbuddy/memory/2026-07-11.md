# 2026-07-11 工作日志

## REMO_new2_AdaMaO 教学笔记 — 模块 3 运行时诊断

创建了 `笔记/03_运行时诊断.md`，这是 AdaMaO 教学系列的第 4 篇（大纲+3 个模块）。

### 进度概览
- 已讲：模块 0（总览）、模块 1（初始化与主循环）、模块 2（HybridPBI 混合分类）→ 模块 3（运行时诊断）
- 下一讲：模块 4（动态关系对训练模式）

### 本讲重点
- RuntimeDiagnostics 是 AdaMaO **唯一完全自创的模块**（REMO 和 PIEA 都没有运行时种群诊断）
- 两个诊断量：coverage（覆盖率，均匀参考向量查漏）和 degeneracy（退化度，SVD 分析种群维度）
- 与 HPC 混合 PBI 的关系：HPC 的参考向量是"评分尺子"（自适应贴前沿），诊断的参考向量是"体检网格"（均匀查漏）——同源但用途正交
- 两个诊断量驱动两个开关：coverage → relation_mode + candidate_mode；degeneracy → candidate_mode（indicator 触发条件）
- 图中完整串联了 HPC → RuntimeDiagnostics 的数据流

## 重新生成模块 2 (HybridPBI 混合分类) 笔记

创建了 `笔记/03_HybridPBI混合分类.md`，取代旧的 `HPC混合PBI模块讲解.md`。本次重写聚焦：

### 自适应参考向量机制（核心增量）
- 完整拆解 K-means 聚类非支配解 → 簇中心 → 单位向量的 4 步 pipeline
- 对比了 K-means vs DBSCAN/层次聚类/高斯混合，解释为什么选 K-means
- 列出 4 层退回均匀向量的兜底条件（M≤3、前沿稀疏、维度退化、异常）
- 图示对比均匀向量（覆盖空白）vs 自适应向量（贴合前沿）

### 两套 PBI 公式对比（之前没讲透的）
- 信号 A (score_v)：d1 + 5·d2，不归一化，连续分 [0,1]
- 信号 B (label_dyn)：(d1 + delt·d2) / ||Ref-Zmin||，除以参考解距离，硬标签 {0,1}
- 证明两者是同一数学骨架的不同变体，并非两套无关公式

### 其他增量
- delt 二分搜索 [-20,20] 的完整流程 + 直觉（旋钮调节好比例）
- 融合权重 α=1-ratio 的设计直觉（早期信全局，后期信局部）
- 8 处已知缺陷按严重程度排列（PBI 量化不对等、α 太死板、N/4 硬切、置信度假自信等）
- confidence 的下游完整消费路径（加权训练 + 课程学习）
