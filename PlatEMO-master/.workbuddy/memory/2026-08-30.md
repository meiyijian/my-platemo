# 2026-08-30 工作日志

## CDR 实验 phase0 → formal 自动化衔接

- 背景：`REMO_new2_AdaMaO_CandidateDiscardRecovery`（候选丢弃回收实验）phase0（100 job，A0 only，约 6.3h）正在运行，需在 4-5 小时后自动接续 formal 档。
- 产出：实验目录下新增 `postphase0_pipeline.m`（无头流水线脚本）：
  - 自动检查 phase0 完成度（枚举 CDRProtocol("phase0").Jobs 逐文件 CDRValidateRunFile 校验，与 analyze 同口径）；
  - 未完成则每 5 分钟轮询、最多等 4 小时（兜住"可能没跑完"）；
  - 完成 → `analyze_CandidateDiscardRecovery("phase0")` 读 GATE VERDICT；
  - GATE 任一格 FALSIFIED → 停止（遵守文档"不要跑 treatment arms"）；PASS/MARGINAL → 继续；
  - `run_CandidateDiscardRecovery("formal")`（自动跳过 phase0 已完成的 100 个）→ `analyze_CandidateDiscardRecovery("formal")`。
- 验证：`matlab -batch "...postphase0_pipeline('check')"` 实测通过（MATLAB 路径 `D:\mathlab2023a\bin\matlab.exe`，注意目录拼写是 **mathlab** 不是 matlab）；检查时 phase0 进度 56/100 valid、0 invalid。
- 自动化：一次性任务「CDR phase0 后处理流水线」id=82884a76-85aa-4d50-94fb-72466ff5d75f，scheduledAt=2026-08-30T05:30（5 小时后），触发后用 PowerShell 以 `Start-Process` 分离启动 MATLAB `-batch` 后台跑该脚本，日志重定向到实验目录 `postphase0_pipeline.log` / `_err.log`，不阻塞等待（formal 档串行约 25h）。
- 经验：MATLAB `-batch` 模式可用于无头跑该实验流水线；用 `-RedirectStandardOutput/-RedirectStandardError` 分文件重定向避免 Start-Process 报错。
