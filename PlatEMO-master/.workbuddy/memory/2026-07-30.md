# 2026-07-30 工作日志

## Git 操作：强制添加被忽略的实验文件夹

- 用户要求将 `PlatEMO/Experiments/REMO_new2_AdaMaO_ConfidenceProbe/` 整个文件夹上传 git，但不修改 .gitignore
- 原因：git 仓库根目录 `D:/PlatEMO-master/.gitignore` 第 8 行 `PlatEMO-master/PlatEMO/Experiments/` 整体忽略了 Experiments 目录
- 解决：用 `git add -f` 强制添加。该目录 9 个文件中 7 个已被跟踪，新增 2 个（CONCLUSIONS.md、analyze_ConfidenceProbe_SDE.m）
- 已 commit（5755260），但 push 失败：沙箱无 TTY，Git Credential Manager（credential.helper=manager）无法弹窗认证
- 待办：用户需在自己的终端执行 `git -c http.proxy= -c https.proxy= push origin master` 完成推送
