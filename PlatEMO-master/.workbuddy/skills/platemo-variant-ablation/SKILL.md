---
name: platemo-variant-ablation
description: 在 PlatEMO 里从已有算法目录派生一个常数或超参变体（lambda、qKeep、nMax、pMix 等），做小规模两臂配对对照或全系列长跑（多目标数×全问题集×20–30 跑、非配对 ranksum、IGD/IGD+ 对比表 xlsx），含显著性检验与结论表格。当用户说"实现一个 X=Y 的算法变体、小范围跑一下、给结论表格"，或"对这两个消融臂跑一遍全系列并出对比表"时使用。
---

# PlatEMO 变体消融：派生 → 配对跑 → 结论表

## 1. 先读源目录，锁定唯一改动点

不要凭记忆改。读源算法主类 + 被调用的选择函数，把要改的常数/分支找出来，确认**只改这一处**，
其余（PAQC、候选生成、贪心项、参数默认值）逐字保留。改动点写进注释和 README。

## 2. 复制目录 + 重名隔离（必须）

- 主类文件 `classdef < ALGORITHM` 留**顶层**（GUI 靠它发现算法）。
- **其余所有 .m 一律进 `private/`**，包括 `ResolveUniformMixMode.m`、`PrunedIndicatorSelection.m`
  这类名字在很多算法目录都存在的通用名文件。private 优先级最高，解析确定。
- 验收（两步，缺一不可）：
  1. **静态**：新目录顶层除主类外没有 `.m` ⇒ 全局路径重名 `.m` = 0
     （列全部 `.m` 按名分组、排除 `\private$`，再逐个查顶层文件是否上榜）。
  2. **动态（唯一有效证据）**：在算法目录里放一个**临时探针函数**，函数体内用 `functions(@Name)` 取解析路径：
     ```matlab
     function p = ProbeX(); s = functions(@ResolveUniformMixMode); p = s.file; end
     ```
     断言返回路径含自己的目录名。**`which('ResolveUniformMixMode')` 不能当验收证据**：它从基础工作区
     查不到 private，返回的是别的目录的副本，根本不是本算法实际调用的那个（2026-09-15 修正）。
     验完把探针 **`Move-Item` / `movefile` 搬到 `.workbuddy/_movecheck/`**（沙箱 `Remove-Item` 被拦）。
- 副本身份：与来源目录**逐文件 SHA-256 比对**，确认是"按字节复制"而不是"看着像"。
- 反例（2026-09-15 实测）：`ResolveUniformMixMode.m` 在不同目录有 **3 个不同版本**，全局解析
  哪个纯看 addpath 顺序。两臂若各自解析到不同版本，配对就废了。同类高危还有 `Shape_Estimate.m`
  （曾有一个 4 参数的抢占版，被抢后 `IndicatorSelectorSDEOnly` 的 try/catch 会**静默降级**成 `Lp_prev`，
  不报错、只变差——这种失败最难查）。

## 2b. 模块移植型变体（框架 A + 模块 B）

用户说"在 X 的基础上用 Y 的 Z 模块"时，做法与上面的常数变体不同：

1. **先找同类先例**。这种组合臂常已有人做过一次（本次先例 `REMO_UniformMixCandidate` = 原版 REMO 框架 +
   旧版候选解模块），照它的骨架与命名习惯改最省事，也最贴用户对该族的预期。
2. **划清边界再动手**：把"框架函数清单"和"模块函数清单"分别列出来对照来源目录；判断模块内部的
   中间产物（指标模型、模式随机流）算不算模块的一部分，依据是**模块的定义性特征**——CDIS 的定义是
   "每轮在两种完整准则之间选一种"，砍掉指标模型就只剩探索分支，那不是 CDIS ⇒ 指标模型必须随模块搬。
3. **只写入口文件**：框架照抄原算法主循环，模块函数按字节复制（`Copy-Item`，不要重打字）。
   新写的入口文件保持**纯 ASCII**；复制来的旧文件保持原字节（中文注释照旧，别顺手重写）。
4. **主循环保护沿用模块来源方**：模块来源那版主循环带空候选补齐 / 剩余 FE 截断，就照抄，
   别退回框架原版的裸循环——裸循环在候选池为空时会死循环，且容易超预算。
5. 入口文件头部写清「框架逐行照搬 / 唯一改动是 X / 与来源方差异 / 参数表」；README 写
   「来源表 + 差异表 + 隔离说明 + 验证表 + 未做事项」。
6. **想复用现成臂当对照前，必须做代码级核验**：同名文件哈希不同**不代表**行为不同——
   `ResolveUniformMixMode` 的多个"版本"里至少两个只差注释。做法是**剥掉注释按行比代码**
   （PowerShell 读行、剔除空行/纯注释行、按第一个 `%` 截断行尾注释，再 `Compare-Object`），
   然后逐条列出「输入 / 分支 / 默认值 / 输出」的差异。
   只看哈希会误判；**只看注释会被过时文档误导**（实测 `REMO_UniformMixCandidate.m` 头部写"三模式
   conservative/explore/indicator"，实际 `ResolveUniformMixMode` 只返回两模式，第三分支是死代码）。
   复用前还要确认：k 的实际取值（class 的 `ParameterSet` 默认可能和目录名不符）、
   主循环有没有空候选补齐 / 剩余 FE 截断（没有的话会超预算）。


## 3. 静态检查

`checkcode(f,'-string')`。注意它返回 **char 不是 cell**，用 `fprintf('%s\n',r)` 打印；
`char(checkcode(f))` 会报错。

## 4. runner：两臂同池同种子

- **绝不用历史数据当配对臂**：同源码同种子在不同线程数/不同会话下结果不同。
- 两臂在**同一个 parpool、同一次 parfeval 批次**里跑，`rng(seed,'twister')` 在 Solve 前设置。
- 每臂 `addpath(genpath(platform))` 后再 `addpath(algFolder)`，让各臂优先解析自己目录。
- **runId 避开数据目录里已存在的**（先数 `.mat` 的文件名尾号，别覆盖）。
- 种子沿用项目公式：`20260912 + M*1e5 + 题号X1000 + runId`。
- 只写 `.mat`（`result`/`metric`/`metadata`），不写 manifest / 日志 / 逐轮 CSV 到数据目录。
- runner 放 `.workbuddy/run_scripts/`，**绝不放进算法目录**（会破坏运行前 SHA-256 源码校验）。

## 5. 跑前审计 + 资源

- 先跑 `'check'` 动作：确认待跑数、`which` 解析、问题规格（D/M/N/maxFE）都对再启动。
- **worker 数看上一步实测的剩余内存**，不要照抄旧命令：6 workers 需要 ~18GB 余量。余量不足就降
  （本次 13GB 余量用 5 workers，稳定 7GB 富余）。真跑起来用 `-batch` + `run_in_background=true`。
- 进度**看产物 .mat 个数**，`-batch` 的 stdout 是块缓冲，不实时。

## 6. 统计与报告

- **跨臂比较一律先统一 runId 集合**：各臂存量跑数常常不等（如 18/20/30）。若图省事用"每臂全部可用
  跑数"，会算出**不同的结论**（实测：某个 −19% 从"显著更差"变成"边缘不显著"、另一个 +2% 从显著变不显著）。
  先取交集（如 `runIds = 1:18`）再算，并在报告里写明"各臂统一到 runId 1–18，n=18"。
- 每题配对 `signrank(A,B)`（Statistics Toolbox 自带，不必装 scipy）+ Holm 校正。
- **变体还要和外部基线比**（如 REMO）。基线多半是历史数据 → 只能非配对 `ranksum`，
  取与变体**同一 runId 段**保持种子范围对齐，另用基线全量做敏感性。报告里区分
  「优于基线的显著项数」和「劣于基线的显著项数」——只看总数会把劣势项算成成果。
- 基线若有预算超支（如 REMO 末次 FE 300–307），必须核对并写明它对基线有利。
- Holm 递推必须写显式 `if`：`x=max(prev, p*(m-i+1))`。**不要用辅助函数假装短路**
  （`iif(i>1,holm(i-1),0)` 会先求值 `holm(0)` → "数组索引必须为正整数"）。
- **标签向量要和 p 值向量同形**：`problems` 是 1×4 行 cell 时 `strcat(problems,'|050')` 仍是 1×4，
  `[A;B]` 得到 2×4，线性索引变成**列优先**，与 8×1 的 p 值错位 → 判决标签张冠李戴。
  先 `problemsCol = problems(:)` 再 strcat，并 `assert(numel(labels)==numel(allp))`。
- 报告里必须点明：**合并均值在题目量纲差异大时（如 IGD 0.7 vs 10）是被大量纲题目主导的，不可引用**；
  给逐题 delta 和符号计数，别只给 pooled p。
- 结论表列：`Problem | n | mean_A | sd_A | mean_B | sd_B | delta% | p | 判定`，
  delta% = `100*(mean_B-mean_A)/mean_B`，并**明确标注正负号含义**（IGD 越小越好）。

## 7. 收尾

数据目录只留 `.mat`；删掉 `_tmp_*.txt`、日志等中间产物（用 `[System.IO.File]::Delete()`
绕过 safe-delete 拦截）。**算法目录里不许留 runner / 探针 / 日志**（会破坏运行前的 SHA-256 源码校验）；
沙箱删不掉就用 `Move-Item` / `movefile` 搬到 `.workbuddy/_movecheck/`，临时文件一律只放 `.workbuddy/` 下。
改动结论写进 `.workbuddy/memory/<日期>.md`。

## 8. 全系列长跑（多目标数 × 全问题集 × 20–30 跑）

给"已有的一对消融臂"跑全系列（如 DTLZ1–7+WFG1–9 × M=10/20 × 20 跑 = 640–1280 跑）时，第 4–6 节
的配对设计不适用 —— 对照臂是历史数据、只能**非配对 ranksum**。以下是实测补丁。

### 8.1 先找现成协议文档，别自己发明口径
项目里通常已有 `论文写作/experiments/*_audit/README.md` 这类文件写死了：跑数（20 vs 30）、
种子基数（M10 `21260912` / M15 `21760912` / M20 `22260912`）、线程 1、`k_eff=min(N,max(6,ceil(1.5M)))`、
哪个目录当基线。**先读它**，跑数与基线选择属"改变统计效力"的决策，要跟用户确认再动手。

### 8.2 调度脚本可能已丢，但协议可以复现验证
`PlatEMO/Experiments/` 常被 `.gitignore` 排除 → 里面的 runner 从未入库、可能已被删。
重建 runner 后做**一次协议复现验证**：拿一个配置已知的臂（如 full）跑任一 run，
比对存量 `.mat` 的**第 1 个快照**（FE=100 = 初始 LHS 种群）的 IGD。
- 命中（浮点舍入级）⇒ 种子公式与「造 Problem **之前** `rng(seed,'twister')`」的接线正确。
- 试 3–4 种接线（造 Problem 前 / 后 / 前后都设 / 题号 0 基 / 只用 base+run），只有一种会命中。
- **第 2 个快照起轨迹发散是正常的**（项目已承认「同 seed 在不同执行环境轨迹仍不一致」），
  不代表协议错；统计继续用非配对检验。
- 便宜实现：用 `maxFE=106` 只跑一步（约 10 s）比全跑 5 min 快得多；FE=100 的快照无论预算多大
  都存在 index 1。
- 坑：做这种小规模验证时**不要改问题列表的长度**（把 16 题缩成 1 题会让题号变成 1、种子整体偏移，
  看起来像"协议不对"）。要过滤就加一个 `onlyProbs` 字段、**保留全局题号**。
- **前提：存量与新协议的 SaveCount 必须一致，否则这套验证法无效。**
  实测（HES_EA_N100）：存量是 SaveCount=18 的残批（FE 序列 100,105,…,135,150,165,180,200,…,300），
  同种子重跑得到 30 快照、IGD(end) 60.55 vs 96.68 —— 看起来像"协议错"，其实只是 SaveCount 不同。
  先查存量 `result` 的快照数与 FE 序列，再下结论。
- **移植型算法的参数千万别传 house 向量**：HES_EA_N100 的类默认是 `{wmax=20,WN=190,KMeans=4}`，
  若照搬本项目的五元参数会**静默**变成 wmax=3000/WN=0.5/KMeans=0.25（`ParameterSet` 按位置赋值，
  不报错）。外部算法一律 `'Parameters', {}` 让类默认生效。

### 8.3 ETA：用实测跑数反推，别用存量 runtime 均值
存量 `.mat` 的 `metric.runtime` 含当时机器的争用，会把 ETA 估高一倍。
**正确做法**：跑 15 分钟后用「已完成跑数 ÷ 已耗时」反推，并注意不同问题代价差 ~35%
（DTLZ 贵、WFG6–9 便宜），早期窗口会低估速率。
实测参考（i5-12400 6 核 / 32 GB / 5 workers，M=10 全系列）：**≈76 跑/h**；
单臂均摊 noCDIS 312 s/跑、noPAQC 372 s/跑。
用存量均值反推只会得到 ≈42 跑/h，据此会把 8.4 h 说成 27 h。

### 8.4 长跑是可中断的：逐文件落盘 + 开跑前查文件
每个 job 写完先 `.tmp` 再 `movefile`，且进入 job 后第一件事是 `exist(outFile)` 就跳过。
⇒ 想改跑量/顺序（例如"只跑十目标"）**直接 `Stop-Process -Force` 杀掉 MATLAB 再按新参数重启**，
只丢在跑的 5 个 job（≈6 min 墙钟）。`RunNBD_AblationAll(workers, Ms)` 这种把 Ms 做成参数的写法
让"先跑完某一档目标数"变成一行调用。

### 8.5 IGD+ 的成本分布与串行边界
- 除 **DTLZ7/M=20 外**每快照 0.015–0.09 s，30 快照 <3 s/跑，**可以随跑随算**。
- **DTLZ7/M=20 参考集 524288 点 / 80 MB**：官方 `IGDp.m` 每快照 7–9 s，且**在 parpool 里必崩**
  （本机历史六次）。⇒ 该题只在优化阶段进池、IGDp 留到收尾时**串行**用分块版算（2.12 s/快照）。
- 历史基线常**只有 IGD、没有 IGDp**（实测 `REMO_k15`/`REMO_k` 480+498 个文件全缺）。
  补算后 `save(f,'metric','-append')` 合并进原 `.mat` 并**回读校验**（沿用项目既有
  `ComputeAndMergeIGDp_SingleAlg.m` 的做法）；只有 `metric` 被写，`result` 不动。

### 8.6 ⚠️ MATLAB → JSON 传二维 cell 必须先 strjoin（隐蔽 bug）
`jsonencode` 把 N×K 的 cell of char **按列主序压平成 1×N·K 的字符串数组**，不是嵌套数组。
下游按 `cells[p][a]` 取值就变成"取字符串第 a 个字符" ⇒ 表格**数据行被写成单个字符**
（`F2='2'`、`G2='.'`…），而**表头与计数行完全正常**，非常难发现。
```matlab
% 行字符串数组：每行一个字符串，字段间用 '|'
T.cells = arrayfun(@(p)strjoin(cellsT(p,:),'|'),1:nP,'UniformOutput',false);
```
Python 侧 `row.split('|')` 还原。**另一坑**：改了结构体字段的表示形式后，所有引用点
（包括 MATLAB 里打印预览用的 `fprintf ... cells{p,pos}`）都要一起改，否则立刻"索引超出数组边界"。

### 8.7 出表后必查两条

1. 每一行的**最小值是否落在正确列**（脚本化比对"蓝字单元格列号 == 数值最小列号"，0 处错位才算过）。
   注意：按单元格 5 位显示精度比大小会在并列时因列序产生**假阳性**，真实均值要到 double 精度比。
2. `+/-/=` 计数行是否等于逐格符号的实际统计（脚本化复算，别手数）。
3. 符号列存成整列 char 时，`sprintf('...%s',signs{a})` 会把**所有题号**的符号拼进每个单元格；
   必须写 `signs{a}(p)`，且 anchor 的空符号要单独走 `sprintf('%.4e (%.2e)')`（空 char 索引报"索引不能超过 0"）。

锚点列（末列）不带符号、计数行锚点格留空 —— 这是项目 xlsx 模板的既定口径。

## 9. 给第三方/官方移植型基线做"提速改造"的正确顺序

2026-09-20 实测（CI-EMO，M=20）：差点把一整批**改过数值**的基线数据当干净数据交付。流程如下。

1. **先 profiler，再动手**。`profile on; Alg.Solve(P); profile off; st=profile('info')`，
   按 `TotalTime` 排序列出热点（缩短预算到几十轮即可，几分钟出结果）。
   **调用次数最多的函数不一定是热点**：本次 `predictor` 115 万次只占 37%，
   而只被调 17 万次的 `dacefit>objfunc` 占 58%。凭调用次数猜热点 = 白干。
2. **改造必须做独立副本**（复制目录 + 主类改名 + helper 全部移进 `private/`，原目录一行不动），
   与第 2 节的隔离铁律一致。
3. **同级 A/B 等价性验证是硬门槛**：同种子、同 maxFE，跑原版与被改版，比对
   `Algorithm.result(:,1)` 的 FE 网格与 `metric.IGD` 轨迹。判定标准：
   - 网格一致 + `maxRelDiff < 1e-12` ⇒ 可视为等价，放行；
   - **`maxRelDiff` 到 1e-3 以上就说明选点被翻盘了** ⇒ 不可用，回退原版。
   本次向量化只快 **1.26×** 且末快照 IGD 差 **6%**（从第 2 个快照即分岔）⇒ 直接废弃。
4. **算清"提速上限"再决定值不值得改**：本次预测部分只占总时 37%，即使完美向量化也就 1.6×，
   而 58% 的开销落在 30 维超参拟合的 `dacefit` 内部，改不动 ⇒ 根本不该动这个优化。
   先估算上限，能省下大量无用功。
5. **结论要向用户明说**：慢就是慢（CI-EMO/DISK 在 M=20 都是 60–75 min/跑），
   不要用"改过的基线"换速度——审稿人会问可复现性。

