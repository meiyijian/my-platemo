# Plan

> 状态：叙事方向经用户纠正（2026-08-12）。沿用已确认的 `full-report · 80%`、`tufte`、`wide + TOC`、`user-assets` 与极简框式封面；正文必须是一份完整算法汇报，科学审校仅作为表述边界。

## Brief

- 目标读者：已经听过 REMO、PIEA 和上一版 AdaMaO 的导师与组会成员。
- 目标语言：中文。
- 文章类型：`full-report`，保留约 80% 决策相关信息。
- 汇报目的：完整讲清 UniformMix-OriginalRelation 的研究故事——为什么从 REMO 出发、当前版本具体改了什么、算法每代怎样运行、已有诊断怎样改变设计、哪些结论已经成立、下一步怎样验证。
- 中心命题：保留 REMO 的普通无权关系学习，把改动集中在关系学习的前后两端：前端用 HybridPBI 构造高维伪标签，后端用 explore / indicator 两种候选视角分配稀缺真实评价；UniformMix 以固定概率进行中性路由，并把尚未验证的部分显式交给五阶段证据链。
- 必须保留：
  - REMO 的关系学习范式和主循环。
  - 当前入口的 7 个参数、初始化、独立路由随机流、预算截断与回退。
  - HybridPBI 的 `k_eff`、数据依赖方向场、代表解标签、阶段融合与 Top-`rGood` 硬化。
  - 原始 `GetRelationPairs + DataProcess + patternnet` 普通无权关系训练。
  - explore / indicator 两条候选路径及 `pMix=0.5` 的准确身份。
  - ConfidenceProbe 与 Cascade pilot 的关键数字、H1/H2/H4 判决和 STOP 结论。
  - 当前证据边界、三项实现风险、五阶段验证和六周执行顺序。
  - 上一版性能表，但只能作为历史背景，不能证明当前简化版性能。
- 可压缩：逐页讲稿与页面内容的重复、宣传性措辞、重复数字、未进入当前算法的 confidence/degeneracy 回退设想。
- 语气：完整、清楚、克制。先讲算法故事，再讲证据边界；不把研究原型包装成已被正式性能证明的最终算法。
- 版式：`wide` + TOC；桌面为左目录与宽正文，窄屏自动回落。
- 配图：使用现有混合 PBI 机制图；其余流程、证据和时间线用 Tufte 风格 Raw 图解。
- 封面：模板 E 极简框，但主视觉改为“标签生成 → 关系学习 → 双路径候选 → 真实评价”的算法信息流，不再以审校或 STOP 为封面主体。

## Outline

- Hero：`UniformMix-OriginalRelation`；副标题“在 REMO 关系学习框架中，重构高维伪标签与候选选择”。
- Lead：从昂贵评价预算出发，说明本工作保留关系学习内核，只改标签入口与候选出口。
- Summary：四个故事节点——继承、标签重构、候选双路径、证据门槛。
- Raw：一张“整场故事地图”，把十章挂到同一条因果链上。

### Sections

1. **01 研究起点：REMO 把目标值预测改写为关系学习**
   - REMO 的价值、主循环、在 many-objective 场景下重新提出的两个问题。
   - Raw：从昂贵真实评价到关系学习、再到本文两处改造的故事路径。
2. **02 当前版本：一次有意的减法**
   - 当前入口、初始化和每代循环；相对 REMO 的七处代码级差异；相对上一版 AdaMaO 删除的机制。
   - Table + Raw 主循环。
3. **03 标签层：HybridPBI 如何生成 Catalog**
   - `k_eff`、方向场、代表解、PBI、阶段融合、Top-`rGood`。
   - Formula + 本地混合 PBI 机制图；明确最终仍是硬 Catalog。
4. **04 关系层：保留原始普通无权训练**
   - 四类关系对、`0/+1/-1`、3/4–1/4 按对划分、patternnet、`p_err` 的含义与限制。
   - Table 展示数据流和信息损失。
5. **05 候选层：explore 与 indicator 两种搜索视角**
   - GA 候选池、关系得分、预测模糊度、决策空间分散、SDE 风格 SVR、粗筛与重排、回退。
   - Raw 双路径图；参数表。
6. **06 为什么要改：两个诊断把问题具体化**
   - 支配型外部真值在 M≥10 稀疏；当前 indicator 级联存在覆盖缺口。
   - 数据表 + 诊断边界；不把两者写成 HybridPBI 已有效的证明。
7. **07 负结果如何改变算法故事**
   - H2/H4 与救援净增益失败；停止 indicator-rescue 主张。
   - `p=0.5` 定位为待对照验证的中性混合，不声称理论最优。
8. **08 当前证据账本：实现、诊断、历史结果与空白**
   - OriginalRelation 实现验证、现有 pilot、上一版性能背景、当前版缺少正式端到端结果。
   - 证据梯级与版本标记。
9. **09 下一步：五阶段验证与六周执行**
   - S1–S5 门控、100 作业算式、停止条件、三项实现风险、六周时间线。
10. **10 结论：这项工作现在处于什么位置**
    - 汇总贡献边界和 go/no-go 决策：先证标签机制，再决定保留或删除候选层。
11. **附录 参数、数字速查与高风险问答**
    - 7 参数；历史性能表；5 个高风险问答；代码与计划路径。

- 结尾方式：`Conclusion` 给出研究主线和下一次组会需要回答的三个问题。

## Theme

- 主题：`tufte`。
- 理由：算法机制、数据判决和证据边界需要以正文、表格、公式与细线图为主，避免幻灯片式卡片和装饰。
- 约束：暖纸底、衬线正文、青灰结构色、暖红只表示停止/风险；所有 Raw 只用 `--ra-*` token。

## Assets

- 外部图片模式：`user-assets`。
- 使用：`article/assets/hybrid-pbi-cycle1.png`，放在 Section 03，解释真实标签数据流。
- Raw 计划：故事地图、主循环、双候选路径、诊断判决链、证据梯级、五阶段门控和六周时间线。
- 禁止：装饰性 AI 架构图、编辑器界面截图、把未实现的 confidence/degeneracy 回退画成当前算法。

## Plan 自查

1. Brief 与 Outline 对齐：通过；每项必须保留信息均有唯一承载章节。
2. 信息取舍有据：通过；只压缩重复讲稿和无证据宣传语言。
3. 正文为主体：通过；Raw 只承担流程、状态和依赖关系。
4. 图片与 Raw 均有论证目的：通过。
5. 章节编号连续：通过；正文 01–10，附录 A。
