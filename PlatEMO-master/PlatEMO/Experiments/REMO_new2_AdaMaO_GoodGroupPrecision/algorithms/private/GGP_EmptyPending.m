function pending = GGP_EmptyPending()
% GGP_EmptyPending - 0x1 struct array of checkpoints awaiting scoring.
%
% Fields must match exactly what REMO_GGP appends, otherwise MATLAB throws
% on the first pending(end+1) = struct(...) assignment.

    pending = struct( ...
        'iter',      {}, ...
        'scoreIter', {}, ...
        'FE',        {}, ...
        'ratio',     {}, ...
        'snapObj',   {}, ...
        'groups',    {});
end
