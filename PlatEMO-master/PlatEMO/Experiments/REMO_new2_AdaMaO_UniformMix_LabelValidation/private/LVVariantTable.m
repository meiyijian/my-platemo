function [codes, names] = LVVariantTable()
%LVVariantTable Frozen variant-code / variant-name mapping (Stage 2 §4).
%   [codes, names] = LVVariantTable() returns the integer variantCode for
%   each L0..L8 label variant. codes is 9x1, names is 9x1 cellstr. The
%   variantCode is used inside the offlineSeed formula
%   (offlineSeed = snapshotSeed + variantCode*100 + replicate).

    codes = (0:8)';
    names = { ...
        'L0_ANCHOR_BINARY_NATIVE'; ...
        'L1_ANCHOR_MARGIN_Q25';    ...
        'L2_ND_SCORE_Q25';         ...
        'L3_HYBRID_CURRENT_Q25';   ...
        'L4_HYBRID_FIXED_Q25';     ...
        'L5_HYBRID_REVERSE_Q25';   ...
        'L6_HYBRID_SHUFFLED_Q25';  ...
        'L7_UNIFORM_SCORE_Q25';    ...
        'L8_ND_SCORE_KEFF_Q25'};
end
