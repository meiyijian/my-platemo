# Only the reproducible artefacts of this experiment are versioned.
# Raw per-run audit MAT files (150 files, 770 MB) stay on disk only; the
# committed analysis CSV can be regenerated from them with
#   run_Lambdat030GoodGroupPrecision / analyze_Lambdat030GoodGroupPrecision
raw/

# Long-format checkpoint dump (47 MB) is regenerated by the analysis script.
analysis/formal/LTGGP_CheckpointMetrics.csv
analysis/smoke/LTGGP_CheckpointMetrics.csv

# Transient run logs.
logs/
