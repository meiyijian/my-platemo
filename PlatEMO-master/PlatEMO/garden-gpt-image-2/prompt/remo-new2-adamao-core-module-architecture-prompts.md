# GPT-image prompts for REMO_new2_AdaMaO core modules

## Shared visual constraints for every figure

Create a publication-ready academic algorithm architecture figure for a many-objective evolutionary optimization paper. Pure white background, flat vector-clean appearance, precise geometry, strict alignment, generous whitespace, thin dark-slate outlines, muted deep blue + teal + low-saturation amber only, no gradients, no shadows, no 3D, no decorative textures, no cartoon icons, no photorealism. Use one consistent sans-serif typeface such as Helvetica, Inter, or Arial. All labels must be English. Use short labels, mathematically correct notation, clean arrowheads, and a small legend only when necessary. The result must remain understandable in grayscale and suitable for a two-column IEEE paper. Do not invent performance values, curves, equations, tensor sizes, or experimental data. Aspect ratio 16:9, high resolution, sharp readable text.

## Figure 1 — Overall AdaMaO optimization pipeline

Draw a left-to-right academic method overview titled “REMO_new2_AdaMaO: Adaptive Relation-Learning Many-Objective Optimization”. Show seven aligned stages connected by solid arrows: (1) “Latin Hypercube Initialization” with decision vectors and expensive true evaluation, producing “Population + Archive”; (2) “Hybrid PBI Classification” producing “good/bad labels, confidence, reference solutions”; (3) “Runtime Diagnostics” producing “coverage” and “degeneracy”; (4) a two-lane adaptive learning stage: upper lane “Relation Mode Router → conservative / curriculum / weighted → Relation-Prediction Network”, lower lane “PIEA Indicator Roulette → SDE / I-epsilon+ / Minkowski → SVR Indicator Model”; (5) “Candidate Mode Router → conservative / explore / indicator”; (6) “Surrogate-Assisted GA and AdaMaOSelection” producing a small candidate batch; (7) “Expensive True Evaluation → Archive Update → Radar-Grid RefSelect”. Add a curved feedback arrow from stage 7 back to stage 2 labelled “next generation”, and a thinner feedback arrow from true-evaluation outcome to the indicator roulette labelled “0/1/2 feedback”. Highlight the two routers and the feedback loop as the novel adaptive components. Avoid showing invented numerical results.

## Figure 2 — Hybrid PBI classification module

Draw a compact dual-view mechanism diagram titled “Hybrid PBI Classification”. Input on the left: normalized objective vectors of the current population. Split into two parallel branches. Upper branch, “Global Reference-Vector View”: adaptive reference vectors, orthogonal projection, longitudinal distance d1, perpendicular distance d2, then “PBI = d1 + theta d2” and “score_v = 1 / (1 + PBI)”. Lower branch, “Dynamic Reference-Solution View”: radar-grid RefSelect chooses k_eff reference solutions, then GetOutput_PBI produces a local binary label. Merge both branches in a central fusion block labelled “Adaptive fusion controlled by evolution ratio”, producing three outputs on the right: “Catalog: good / rest”, “confidence”, and “Ref”. Use simple objective-space glyphs: dots, rays, projections, and reference points. Clearly distinguish global direction information from local reference-solution information. Do not draw a real Pareto front or fabricated coordinates.

## Figure 3 — Runtime diagnostics and adaptive relation-mode router

Draw a two-part architecture figure titled “State-Aware Relation Learning”. Left half: “Runtime Diagnostics”. Show normalized population objectives feeding two diagnostic computations: “Reference-direction assignment → coverage” and “Centered objective matrix → SVD energy → rank90 → degeneracy”. Also show inputs “previous model error prev_p_err” and “mean classification confidence”. Right half: a clean decision router with three mutually exclusive outputs. Route to “Curriculum” when “prev_p_err > tau_err”; annotate “retain the most confident samples within good/rest groups”. Route to “Weighted” when “prev_p_err <= tau_err AND mean_conf >= 0.55 AND coverage < 0.60”; annotate “confidence-weighted relation pairs”. Otherwise route to “Conservative”; annotate “all standard relation pairs, equal weight”. The three branches rejoin at “pairwise training set (XXs, YYs, optional WWs)”. Use amber only for the currently adaptive decision router, blue for data, teal for selected training samples. Make the logical conditions legible and exact.

## Figure 4 — Relation-prediction neural network

Draw a publication-style neural architecture diagram titled “Pairwise Relation-Prediction Network”. On the left show two decision vectors xi and xj concatenated into a 2D-dimensional pair vector [xi, xj]. Show preprocessing “mapminmax to [-1, 1]”. Then show a three-hidden-layer feed-forward pattern network with widths labelled symbolically, not numerically: “ceil(1.5 x xDim)”, “xDim”, and “ceil(0.5 x xDim)”, followed by a three-class softmax output. Label the output classes exactly “-1: worse”, “0: equivalent”, “+1: better”. Beneath the network show two training paths: “equal-weight training” for conservative/curriculum modes, and “sample-weighted training” for weighted mode, where normalized weights are clipped below by w_min. Add a small testing branch that reuses the same normalization parameters and outputs classification error “p_err”. Do not invent activation functions, optimizer names, epochs, tensor batches, or accuracy values not specified by the implementation.

## Figure 5 — Adaptive PIEA indicator roulette and feedback

Draw a circular closed-loop academic mechanism diagram titled “Adaptive Indicator Roulette”. At the top place a probability wheel with three sectors labelled “SDE”, “I-epsilon+”, and “Minkowski”, initially conceptually balanced but do not print fabricated percentages. The selected indicator receives the current population and produces “Fitness”. For the Minkowski branch, include a small side block “Shape_Estimate → adaptive Lp”. Fitness and population decision vectors feed an “RBF-SVR Indicator Model”, which later ranks candidate solutions. On the right show expensive evaluation feedback scored by two filters: “NDSort first front” and “NDSort_SDR first front”, yielding score 0, 1, or 2. At the bottom show a sliding-window update block with “Choose_record”, “Win_record”, and “Pw proportional to wins / choices”, then a curved arrow returns to the roulette. Emphasize online adaptation and closed-loop credit assignment. Keep formulas qualitative except for the stated update relationship; no fake probability values.

## Figure 6 — AdaMaO adaptive candidate-selection module

Draw a three-lane comparison architecture titled “AdaMaOSelection: State-Dependent Candidate Evaluation”. At the left, show surrogate-assisted GA repeatedly generating candidates from current population and reference solutions, with relation-model scoring against good-class and rest-class reference samples. Then split into three horizontal lanes. Lane 1 “Conservative”: relation score → descending rank → select n_min. Lane 2 “Explore”: normalized relation score + lambda_t times uncertainty, where “lambda_t = lambda0 (1 - ratio) max(0, 1 - p_err / 0.45)” → q_keep quantile filter → greedy max-min decision-space diversity → select between n_min and n_max. Lane 3 “Indicator”: relation score coarse filter, retain top 30 percent with at least 20 candidates → SVR indicator prediction → 0.70 quantile filter → ranked batch between n_min and n_max. At the top, show the exact router: choose “indicator” when indicator model exists, p_err <= tau_err, and degeneracy >= 0.45; else choose “explore” when p_err <= tau_err and coverage < 0.60; otherwise “conservative”. Merge the three lanes into “Next candidates for true evaluation”. Use consistent colors by function: blue for relation learning, teal for exploration/diversity, amber for indicator guidance.

## Figure 7 — Expensive evaluation, feedback, and radar-grid environmental selection

Draw an iterative end-stage diagram titled “Archive Feedback and Environmental Selection”. Input: a small batch “Next” from AdaMaOSelection. Stage 1: enforce remaining evaluation budget and perform “Expensive True Evaluation”. Stage 2: append new solutions to the cumulative “Archive”. From the new solutions create two feedback branches. Upper branch: standard NDSort and NDSort_SDR produce indicator feedback score 0/1/2, which updates indicator probabilities. Lower branch: “RefSelect” first preserves nondominated solutions, then handles overflow with radar-grid mapping of M-dimensional objective vectors to 2D angular-radius cells, prioritizing sparse cells and representative solutions, producing the next “Population”. Draw a large curved arrow from the next population back to Hybrid PBI Classification to indicate the next generation. Include a fallback note in a small dashed box: “if surrogate selection returns empty, generate candidates with OperatorGA”. Keep the archive visibly cumulative and the population fixed-size concept visually distinct; do not invent archive sizes or objective values.

## Optional negative prompt to append

Avoid: glossy AI infographic, business dashboard, generic flowchart template, colorful PPT style, 3D blocks, isometric scene, robot imagery, human characters, DNA icons, light bulbs, gears as decoration, neon colors, gradients, shadows, fake charts, fake numerical results, illegible tiny text, mixed Chinese and English, crossing arrows, inconsistent block sizes, unexplained acronyms, arbitrary formulas, or a photorealistic background.
