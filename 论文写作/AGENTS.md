# 论文修改后的提交约定

用户要求：每次修改本目录的 `HPDC-MaOEA.tex` 后，完成必要检查，自动提交并推送本次相关改动，并向用户说明改动部分，方便回退。

- 提交前检查差异并编译论文；修改交叉引用后至少编译两遍。
- 只提交本次任务涉及的论文、配套图件、说明和必要源码改动，不夹带其他未提交工作。
- 将相互依赖的论文术语和源码名称放在同一次提交中。
- 沿用当前任务确认的远程与分支；正常推送，不强制覆盖远程历史。
- 完成后报告提交编号、推送结果、改动摘要、验证结果和对应的回退方式。
- 发现原有占位或引用问题时如实说明；不要为通过检查而擅自补写实验结论。
