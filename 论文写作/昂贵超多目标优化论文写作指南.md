# 昂贵超多目标优化论文写作指南

> 基于七篇文献的范式拆解 · 目标层次：SCI 一区（IEEE TEVC / Information Sciences / Swarm and Evolutionary Computation）
>
> 生成日期：2026-09-02

---

## 目录

1. [七篇文献坐标系](#一七篇文献坐标系)
2. [一区论文的骨架](#二一区论文的骨架)
3. [摘要怎么写](#三摘要怎么写)
4. [引言的四段式](#四引言的四段式)
5. [相关工作的分类学](#五相关工作的分类学)
6. [贡献列表的句式](#六贡献列表的句式)
7. [方法章节的组织](#七方法章节的组织)
8. [复杂度分析模板](#八复杂度分析模板)
9. [实验章节的七件套](#九实验章节的七件套)
10. [结论与局限性](#十结论与局限性)
11. [中英术语对照表](#十一中英术语对照表)
12. [可复用英文句式库](#十二可复用英文句式库)
13. [选题建议：五个可做的切口](#十三选题建议五个可做的切口)
14. [八周执行计划](#十四八周执行计划)
15. [投稿前检查表](#十五投稿前检查表)
16. [消融实验的完整拆解](#十六消融实验的完整拆解)
17. [HPDC-MaOEA 投稿 SWEVO 的实验决策记录](#十七hpdc-maoea-投稿-swevo-的实验决策记录)

---

## 一、七篇文献坐标系

先建立坐标系。这七篇不是随机的七篇，它们在两个维度上分布：**代理模型的输出形式**（回归 → 分类 → 关系 → 指标）和**问题规模**（目标数 m、决策维数 D）。

| 简称 | 全称关键词 | 刊物/年份 | m 范围 | D 范围 | FEmax | 核心创新 |
|---|---|---|---|---|---|---|
| **RSEA** | Radial Space division based EA | ASOC 2017 | 3,5,10,15 | M−1+k | 按代数（500–800 代，非昂贵） | 目标空间径向投影降到 2 维，自适应网格维持多样性 |
| **PCSAEA** | Pairwise Comparison based SAEA | SWEVO 2023 | 2,3 | 15–21 | 2000 | 代理做成对比较而非预测适应度，可靠性驱动的三态使用 |
| **PIEA** | Performance Indicator-based EA | Inf. Sci. 2024 | 3,7,11(+30,50,100) | 100,200,300(+500) | 300 | 只训一个 SVM 逼近**性能指标**，从根上消除误差累积 |
| **R2AEA** | Regression & Relation-Assisted EA | SWEVO 2025 | 3,6,10 | 50,100,200(+500) | 1000 | 回归（早期）+ 关系（后期）两阶段切换，预测熵做不确定性 |
| **MP-SAMaOEA** | Multi-Perspective infill SAMaOEA | SWEVO 2026 | 6,10,20 | 10–21 | 300 / 400 | 填充准则按"作为训练样本的价值"选解（角度+欧氏距离双视角） |
| **KHM-EF** | Kriging + Hybrid-Metric + Elite Front | SWEVO 2026 | 3,5,10 | M+4 / M+9 | 500 | 支配概率 + Voronoi 体积构成双目标填充准则 |
| **DACKEA** | Distance-Aware Cooperative Kriging EA | SWEVO 2026 | 3,5,10 | 10 | 300 | 全局-局部 Kriging 按空间距离自适应加权融合 |

**读这张表要看出三件事：**

第一，**技术演进的主线是"降低代理模型的学习难度"**。回归要学 f(x) 的精确值，最难；分类只要学"好/坏"标签；关系只要学"谁比谁好"；PIEA 更进一步，把 m 个目标压成 1 个标量指标去学。每一次输出形式的简化，都换来一篇顶刊。这条主线还没走完。

第二，**m 和 D 存在明显的分工，几乎没人同时做大**。PIEA 把 D 做到 300 但 m 只到 11；MP-SAMaOEA 把 m 做到 20 但 D 只有 10–21；R2AEA 的 D 到 200 而 m 到 10。**m ≥ 20 且 D ≥ 100 的区域基本是空白**，这是你最值得占的位置。

第三，**FEmax 越小越难，也越可信**。DACKEA 和 PIEA 用 300 次真实评价，MP-SAMaOEA 用 300（20 目标时放宽到 400），KHM-EF 用 500，而 PCSAEA 用 2000。审稿人会认为 300 的预算比 2000 更贴近真实昂贵场景。**你的预算不要超过 500**，超了会被质疑"这还算昂贵优化吗"。

---

## 二、一区论文的骨架

七篇的章节结构高度一致，这不是巧合，是这个领域审稿人的期待模式。照着搭，不要创新章节结构。

```
1. Introduction                          （2–2.5 页，最重要）
2. Preliminaries / Related Work          （1.5–2 页）
   2.1 问题定义（MOP/MaOP 的 Eq.(1)）
   2.2 代理模型基础（Kriging / GP / SVM 的公式）
   2.3 Motivation                        ← 强烈建议单独立节
3. The Proposed Algorithm                （4–6 页，核心）
   3.1 Framework / Overall procedure     ← 必配 Algorithm 1 伪代码
   3.2 创新模块 A
   3.3 创新模块 B
   3.4 Computational complexity analysis ← 一区必须有
   3.5 Discussion                        ← 自辩 novelty，强烈建议
4. Experimental Studies                  （8–12 页）
   4.1 Experimental settings
   4.2 Comparison with state-of-the-art
   4.3 Ablation studies
   4.4 Parameter sensitivity analysis
   4.5 Runtime analysis
   4.6 Real-world application
5. Conclusion                            （0.5 页）
```

**几个结构层面的经验：**

**§2.3 Motivation 要单独立节。** DACKEA 的做法值得抄：引言第 9 段已经指认过缺陷，到 §2.3 又用近乎重写的方式把缺陷升级为"two major challenges"。这种"双重铺垫"看似冗余，实际是给审稿人两次机会理解你的动机。引言里读漏了，到 Motivation 还能补上。

**§3.5 Discussion 是回应审稿意见的预埋。** 审稿人几乎必问"你和 [某篇] 有什么区别"。DACKEA 提前在 §3.4 Discussion 里把四篇最接近的工作逐一切割，原句：

> "The main novelty therefore lies **not in** weighted ensemble prediction itself, **but in** the distance-aware reliability modeling of local Kriging models."

这个 "not in A, but in B" 句式直接可用。写论文时就把这段写好，能省掉一轮 major revision。

**伪代码必须有，而且要标注输入输出。** MP-SAMaOEA 的 Algorithm 1 把所有符号列在 Input 里（FEmax、FE、Database_TS、D、ω、ω_max、NP），这样读者不用回头翻正文找符号定义。

---

## 三、摘要怎么写

摘要是四句话的事，但每句都有固定功能。以 MP-SAMaOEA 为例拆解：

**第 1 句 — 领域定调 + 术语抢占：**
> "Surrogate-assisted many-objective evolutionary algorithms (SAMaOEAs) have become a pivotal method for solving expensive many-objective optimization problems (EMaOPs), where the cost of objective evaluations is computationally prohibitive."

注意 SAMaOEAs 和 EMaOPs 两个缩写在第一句就定义完。摘要里的缩写要在摘要内自足。

**第 2 句 — 承认直觉做法有效（这一步不能省）：**
> "Training a surrogate model for each objective function is an intuitive approach, as it effectively approximates the landscape of that objective function."

**第 3 句 — 指认机理级缺陷（转折点）：**
> "**However, this approach tends to suffer from error accumulation as the number of objective functions increases.**"

**第 4 句 — 说明"显然的解法"为什么行不通（这是拉开差距的关键）：**
> "Due to the heterogeneous characteristics of different objective functions, identifying and adding informative training samples requires a substantial number of objective evaluations."

**第 5 句 — 提出方案 + 缩写：**
> "**Therefore, we propose** a surrogate-assisted evolutionary algorithm with multi-perspective infill sampling (MP-SAMaOEA), in which only a small number of solutions are selected for objective evaluations to enhance the predictive ability of the surrogate model."

**第 6–7 句 — 模块展开**，用 "A ... is first presented"、"Specifically, ..."、"Additionally, ..." 串起来。

**最后一句 — 实验结论 + 落点：**
> "Experimental results on the WFG and DTLZ benchmark suites, as well as a real-world application, demonstrate that MP-SAMaOEA outperforms the comparative algorithms, **particularly in solving expensive super-many-objective optimization problems**."

结尾的 "particularly in ..." 是划定优势区间，比笼统说 "outperforms" 可信得多。

**一个反面参照：** KHM-EF 的摘要缺陷句只有一句 "However, it remains challenging to effectively balance convergence and diversity of the solutions."——这是"经典难题式"定位，不如 MP-SAMaOEA 的"精确缝隙式"锋利。收敛与多样性的平衡是二十年的老问题，说它"仍然有挑战"等于没说。**你要找的是那种"这件事没人考虑过"的缺陷，而不是"这件事很难"的缺陷。**

---

## 四、引言的四段式

引言是决定送审与否的部分。七篇的引言都是同一个骨架，我按功能拆成四块（实际会展开成 8–10 段）。

### 第一块：背景与问题定义（1–2 段）

给出 MOP/MaOP 的形式化定义 Eq.(1)，界定 m > 3 为 many-objective。**如果你做超多目标，必须在这里抢占定义**，MP-SAMaOEA 的做法：

> "In this paper, MaOPs with **more than 10 objective functions** are regarded as super-many-objective optimization problems."

这一句让整篇论文的 m ∈ {6,10,20} 实验设置变得合法。你也要写一句类似的。

紧接着定义 PF / PS：

> "The set of solutions is collectively referred to as the Pareto-optimal front (𝑃𝐹) in the objective space and the Pareto-optimal set (𝑃𝑆) in the decision space, respectively."

### 第二块：昂贵性的现实依据（1 段）

**必须给具体的工程例子和数量级对比**，不能空说"评价很贵"。几篇的例子可以参考：

- PCSAEA：drug design、aerodynamic design，"hundreds to thousands" 次评价 vs 传统 EA 需要 "tens of thousands to millions"
- MP-SAMaOEA：深度神经网络架构搜索需数小时到数天
- KHM-EF：重载铁路空车调度、翼型设计、卡车驾驶室设计、高炉优化

句式：
> "However, for many real-world MOPs and MaOPs, **a single function evaluation can be prohibitively expensive**, e.g., ..."

结论句是"所以传统 MOEA 用不了"：
> "As a result, they may not be applicable to certain practical problems, where objective evaluations of these practical MaOPs require significant resources, such as time, computational power, or physical experiments."

### 第三块：相关工作分类 + 内嵌批判（3–5 段）

这是引言的主体，见下一节详述。核心技巧是**每一类结尾自带一句批判**，不要把所有批评堆到最后。

### 第四块：缺陷指认 → 转折 → 贡献（2 段）

这是全篇最关键的十行字。标准三步走（PCSAEA 的模板）：

**步骤 1 — 先肯定（不能省，省了显得不专业）：**
> "Based on the above analysis, the existing SAMaOEAs have demonstrated significant competitiveness in solving EMaOPs."

**步骤 2 — 定位机理级缺陷。** 这里给四种力度递增的写法，按推荐度排序：

| 类型 | 例句 | 适用 |
|---|---|---|
| **盲点型**（最推荐） | "most existing infill criteria select solutions ... and **these criteria fail to consider how solutions perform as training samples**" | 你发现了一个没人想过的角度 |
| **占位型**（最有力） | "most existing research **remains restricted to** low-/medium-dimensional problems, with **very little attention paid to** ... more than 100 dimensions" | 有量化证据支撑（配一张统计表） |
| **机理型** | "this approach tends to suffer from **error accumulation** as the number of objective functions increases" | 能推导或实证的固有缺陷 |
| **泛化性型** | "such refinements often come at the cost of general applicability, resulting in degraded performance on other types of problems" | 相对较弱，慎用 |

**PIEA 的占位型论证是全部七篇里最高效的 gap 论证**，值得单独学：它用 **Table 1** 把 30 多篇主流 SAMOEA 按最高决策维度分箱（2–15 / 16–30 / 31–50 / 51–100 / 101–150 / 151–200），一眼看出 100 维以上只有 MCEAD 和 ADSAPSO 两篇。然后正文只需一句：

> "It is apparent that the prevailing research has focused on low-to-medium dimensional problems, with a notable scarcity of studies addressing high-dimensional problems, particularly those exceeding 100 dimensions."

**把"空白"从主观判断变成可视事实，这是投一区最划算的一页篇幅。强烈建议你也做一张这样的表。**

**步骤 3 — 转折句。** 三种写法：
> "**To address these problems / To address the aforementioned challenges**, this paper proposes ..."
>
> "**Motivated by** the parameter-free adaptive reference-point strategy from AR-MOEA [40], this paper proposes ..., **termed** KHM-EF."（把创新锚定在可追溯的谱系上，降低"凭空想象"的质疑）

### 进阶技巧：制造"交叉难点"

PIEA 的引言第 8 段列了三条 challenge，第三条是精髓：

> "(1) 高维本身的困难 (2) 多目标本身的困难 (3) **High-dimensional and multi-/many-objective characteristics interact with one other. Inaccurate models exacerbate cumulative errors, and ineffective infill sampling strategies fail to improve the model accuracy.**"

然后收口：
> "**Hence, the intertwined nature of high-dimensional and multi-/many-objective characteristics amplifies the complexity.**"

第三条制造了"1+1 > 2"的效果——单独解决高维有人做过，单独解决多目标有人做过，但**两者耦合产生的新困难没人做过**。这是把增量工作包装成必要工作的标准手法，也是你做"m 大 + D 大"这个空白区时最该用的立论方式。

### 进阶技巧：抢先自我否定

PCSAEA 在提出成对比较代理后，立刻承认已有类似工作，再指出其不足：

> "**However, these models do not consider the reliability of models, and are unable to achieve significantly better performance than regression and classification models as presented in the experiments [29].**"

主动把最接近的竞品摆出来打，比等审稿人指出要好得多。

---

## 五、相关工作的分类学

这个领域的相关工作有两种成熟的分类维度，选一个用，不要自创。

### 分类维度 A：按代理模型的角色（推荐）

DACKEA 和 KHM-EF 都用三分法：

1. **Regression-based** — 代理直接预测目标值。代表：K-RVEA、KTA2、EDN-ARMOEA、HeE-MOEA、TS-SAEA
2. **Classification-based** — 代理输出好坏标签或支配关系。代表：CSEA、CPS-MOEA、REMO、PC-SAEA、θ-DEA-DP、MCEAD
3. **Indicator-based** — 代理逼近性能指标。代表：PIEA、EMMOEA、DirHV-EI

MP-SAMaOEA 用二分法（每目标建代理 vs 建关系/指标代理），也成立，更简洁。

### 分类维度 B：按填充准则

PoI（probability of improvement）/ EI（expected improvement）/ LCB（lower confidence bound）/ 混合准则。这个维度适合做填充准则创新的论文。

### 组织技巧一：Table 压缩

KHM-EF 用一张四栏表（Category / Algorithm / Surrogate model / Features）把十几篇工作压缩成一页，正文只作提要式点评。**超多目标这种文献密集方向，这样能省 1–1.5 页篇幅。**

R2AEA 的三栏表更聪明——它只列**对比算法**：Surrogate Type / Model / Strategy for Large D。这张表同时完成了两件事：交代相关工作，以及说明"为什么选这几个做对比"。

### 组织技巧二：每类结尾内嵌批判

MP-SAMaOEA 的做法，第二类结尾：

> "In methods belonging to this category, **although** training a surrogate for a relation or performance indicator can reduce the errors introduced by the surrogate, **its effectiveness largely depends on whether** the relation or performance indicator can accurately capture the underlying relationships or performance characteristics."

回归类的标准批判句（PIEA）：
> "**However**, ... requires the training of one or multiple models for each objective. **Moreover**, for high-dimensional problems, building an accurate regression model is hindered by an insufficient number of training samples."

分类类的标准批判句（PIEA）：
> "**Nevertheless**, the screening capability is relatively poor, owing to its **less informative output**."

### 组织技巧三：逐篇拆解最接近的对手

PIEA 在 §2 末尾对每个中高维方法点名批评，这段是"精准打击"的范本：

- HeE-MOEA / EDN-ARMOEA：重视建模但 "insufficient attention has been paid to mitigating the impact of cumulative errors"
- ADSAPSO：降维 "overly reliant on predefined parameters and the quality of the current population"
- MCEAD：预设参考向量导致 "certain ineffective subproblems are still considered"，且 "employs significantly imbalanced labels, resulting in a decrease in the classification accuracy"
- REMO：成对标签 "provides ample training samples but **doubles the data dimensions**"
- ESBCEO / ADDHDEMO / GP-iGNG：Kriging 开销大且 **要求 d 不超过样本数**

最后这条对 Kriging 高维硬约束的打击最致命。**如果你做高维，这句话是你排除 Kriging 系方法的理由。**

单篇工作的引用句式统一为：
> "For instance, X et al. [n] + 动词（proposed / employed / designed）+ 机制描述 + **thereby** + 收益"

---

## 六、贡献列表的句式

贡献 2–4 条都能中，**关键不是条数，而是每条都必须能对应到一个独立的消融实验表**。这是硬规则：如果你列了 3 条贡献但只有 1 个消融表，审稿人会认为另 2 条是凑数的。

### 三段式（最常见，推荐）

R2AEA 的结构：**框架 / 关键部件 / 实验**

> 1. "A two-stage surrogate-assisted evolutionary framework is proposed, including regression-assisted weight optimization (RWO) and relation-assisted multi-objective optimization (RMO). RWO aims to ..., while RMO focuses on ..."
> 2. "A new infill criterion is proposed based on the voting scoring strategy in the RMO stage. It considers both the uncertainty and the performance, where the uncertainty is determined by the predicted entropy."
> 3. "To validate the effectiveness of the proposed algorithm, comprehensive experiments have been conducted on benchmark problems with 50 to 200 decision variables and some real-world problems."

DACKEA 的结构：**模块 1 / 模块 2 / 整体算法 + 实验**，第三条的连接句很实用：

> "**Building upon the two core components outlined above, we propose** the DACKEA algorithm..."

### 单条贡献的内部句式

标准模板：

> **A** [模块名] **is proposed to** [直接目的]**, aiming to** [更高层收益]**.** [机制展开 1–3 句]**. Furthermore,** ... **The** ... **is then conducted based on** ...

带对比的强化版（KHM-EF）：

> "A reference-vector–constrained elite front selection strategy is proposed. **Instead of treating all non-dominated solutions as elites,** the proposed strategy identifies elite front solutions based on their angular proximity to reference vectors. These representative solutions are incorporated into both the evolutionary search and infill sampling stages, **effectively alleviating** the issue of poor diversity caused by population crowding, **while also** accelerating the runtime of the algorithm by reducing the population size."

注意 "Instead of ... , the proposed ..." 和结尾的**双重收益**（性能 + 效率）。每条贡献都给两个收益，显得更饱满。

### 加分写法：概念可迁移性

PIEA 的第 4 条贡献：
> "**Integrating the indicator-based concept into a classification-based framework** further demonstrated its performance."

它把自己的 idea 移植到另一个范式（做出 PIEA_c 变体）并独立成 §6。这证明的是**概念的可迁移性而非单个算法的有效性**，档次明显更高。如果你的创新点是一个"思想"而不是一个"技巧"，一定要做这个实验。

### 加分写法：无参数

RSEA 的第 3 条：
> "In the proposed RSEA, **all the parameters involved are adaptively adjusted**, which enhances the practicability of RSEA for solving real-world application problems."

配一句正文强调：
> "It should be highlighted that **no specific parameter is involved in RSEA**."

"无参数"在这个领域是很有杀伤力的卖点，因为审稿人普遍反感需要精调的算法。

### 加分写法：验证代理本身而非只验证算法

PCSAEA 的第 1 条贡献末尾：
> "**While existing work rarely compared the learning performance of surrogate models, this work performs experiments to verify that the proposed surrogate model has higher accuracies than existing models.**"

它单独做了一组实验对比代理模型自身的 Recall / Precision / Accuracy / F1（在 DTLZ2、DTLZ3、WFG7、MaF4 上）。**大多数论文只报最终 IGD，不报代理精度，这是一个容易做出差异化的地方。**

### 命名技巧

| 算法 | 命名逻辑 | 可学之处 |
|---|---|---|
| MP-SAMaOEA | 策略首字母 + 领域既有缩写后缀 | 后缀沿用 SAMaOEA，读者一眼知道领域 |
| R²AEA | Regression + Relation 双 R → R 平方 | 视觉上有巧思，好记 |
| PIEA | 以**建模对象**命名 | 比堆形容词更能传达核心思想 |
| KHM-EF | Kriging + Hybrid-Metric + Elite Front | **去掉 EF 正好是消融变体 KHM**，命名与消融设计咬合 |
| DACKEA | 形容词 + 机制 + assisted + EA | 领域最常见的构词法，安全 |

KHM-EF 的做法最值得抄：**起名时就想好消融变体的名字**。你的算法叫 X-AB，消融时就有 X-A、X-B、X 三个天然变体，表格标题自解释。

---

## 七、方法章节的组织

### 每个模块必须回答"为什么这样设计"

这是区分一区和三区的关键。三区论文写"我们用了 X"，一区论文写"**X 的合理性在于 Y**"。

DACKEA 的句式（**背下来**）：
> "**The rationale behind the proposed** distance-aware strategy **is that** the reliability of a local Kriging model is closely related to the spatial support provided by its neighboring training samples."

再看几个"论证机理"的例子，注意它们都不是空喊：

**PCSAEA 论证成对比较优于回归**，用四条并列的机理论证：

1. *可量化性*：
   > "it is difficult to know whether a specific RMSE value indicates that the model is accurate or not. **By contrast, it is easy to quantify how accurate a binary classifier is by checking whether its accuracy is significantly higher than 0.5.**"
2. *可反用*：
   > "classification models and pairwise comparison based models can be reversely used ... **while regression models cannot**."
3. *样本量与平衡*：
   > "there will be 𝑁 training samples for a classification model and **𝑁(𝑁−1) training samples** for a pairwise comparison based model."
4. *突破已评价上界*：分类器 "cannot identify candidate solutions that are better than all the evaluated solutions"，而成对模型可以。

**R2AEA 论证两阶段的必要性**（全文核心论点）：
> "It is noticed that **regression models perform well in early stage** by providing comprehensive information that enables efficient convergence through dimensionality reduction. In contrast, **relation models prove effective in later stages**, offering robust performance despite their limited information, particularly when regression models become unreliable. **Hence, the combination of regression and relation models have great potential to solve HEMOPs efficiently.**"

**R2AEA 用一句话否掉"直接上高维回归"这个显然的替代方案**：
> "However, training a reliable regression model in 100 dimensions would require prohibitive samples (> 1e3), **which contradicts the premise of expensive optimization**."

这种"承认对手优点后一击致命"的句式非常有用——你必须主动排除掉读者会想到的简单方案。

**KHM-EF 用反例论证为什么用几何均值**：三维情况下支配概率 90%/90%/10%，算术均值虚高到 63%，几何均值则不会。**给一个具体数字的反例，比一段文字论证有力得多。**

**RSEA 给出距离保持的代数推演**（E₂ = √(L(W₁W₁ᵀ + W₂W₂ᵀ)Lᵀ)，R = W₁W₁ᵀ+W₂W₂ᵀ），把"降维可视化"合法化为"可用于选择"。如果你要借用降维思想，**这一步的理论论证不能省**。

### 自适应参数的写法

领域内有一个反复出现的模式：**用进化进度驱动收敛/多样性的权衡**。

- RSEA：`r = 1 − (t/t_max)²`，早期偏收敛、后期偏多样性
- PCSAEA：`r = FE/FEmax`（方向相反，同源思想）
- KHM-EF：τ 随评价次数消耗动态递减
- R2AEA：`tr = 0.5` 固定阈值切换阶段

配套说明句：
> "the adaptive penalty parameter r **enhances the convergence maintenance at the early stage** of the evolution **and enhances the diversity maintenance when** the solutions are well converged."

**这里有一个明显的改进空间**：以上全是**开环**（时间驱动）调度。R2AEA 自己的敏感性分析已经暗示最优 tr 依问题而变（"Variants with intermediate thresholds (e.g. 0.25 to 0.75) outperform extreme settings"）。**改成闭环（用验证集准确率或 ECE 校准误差触发切换）是一个工作量小、故事完整、审稿人容易认可的增量。** 见第十三节切口 3。

### 主动承认设计代价

PIEA 的环境选择用 one-to-one comparison 而非留一半，作者明确承认这削弱开采能力，然后给补偿方案：

> "**Although this may weaken the exploitation capability**, the number of generations in the evolutionary process **can be increased to compensate for this**."

这种写法不会削弱论文，反而增加可信度。**每个设计选择如果有明显代价，主动说出来并给补偿，比装作没有代价好。**

---

## 八、复杂度分析模板

一区必须有这一节。PIEA §4.5 是标准模板，四步走，直接套：

**第 1 步 — 符号约定：**
> "Suppose 𝑁 denotes the number of individuals under expensive evaluation, 𝑚 and 𝑑 denote the number of objective functions and decision variables, respectively, 𝑛 denotes the population size, 𝑤 denotes the maximum cycle times in surrogate-assisted optimization, 𝜂 denotes the preselection survivor number in infill sampling."

**第 2 步 — 逐模块给界：**
> "The time complexity of shift-based density estimation, additive indicator, and Minkowski distance is 𝑂(𝑚𝑁²), 𝑂(𝑚𝑁²), and 𝑂(𝑚𝑁), respectively. The time complexity of constructing regression SVM is 𝑂(𝑑𝑁³). The time complexity of surrogate-assisted optimization and infill sampling is 𝑂(𝑛𝑑𝑤) and 𝑂(𝜂𝑑𝑁), respectively. The time complexity of Pareto dominance and strengthened dominance is 𝑂(𝑀𝑁) and 𝑂(𝑛²), respectively."

**第 3 步 — 量级比较并取主导项：**
> "**Considering that 𝜂 << 𝑛 < 𝑁, according to the operation rules of symbol 𝑂**, the time complexity of the proposed PIEA **can be simplified as** 𝑂(𝑑𝑁³)."

**第 4 步 — 配实测运行时间**（见实验章节）。

**参考量级：** Kriging 建模是 O(n³)（这是它在高维和大样本下的死穴），SVM 回归 O(dN³)，非支配排序 O(mN²)，k-means O(nkt)。

**七篇里六篇都有这一节**（MP-SAMaOEA §3.4 给到 O(|Arc|³)，KHM-EF §3.4 给到 O(M·A_t³) 量级并指认 Kriging 构建为主导瓶颈），**只有 DACKEA 没有**——它只给了实测运行时间。这是 DACKEA 的短板，也说明这一节是审稿人的默认期待，不能省。

---

## 九、实验章节的七件套

这是决定录用的部分。七件套齐全才是一区完整形态。**七篇里没有一篇是全的**，这正是你的机会。

| # | 项目 | 一区基线 | 七篇的完成情况 |
|---|---|---|---|
| 1 | 基准套件对比 | DTLZ + WFG + **MaF** | 三套齐全的只有 **DACKEA**（MaF1–7）；PCSAEA 用 MaF1–5，RSEA 用 MaF2–5；MP-SAMaOEA、KHM-EF、PIEA、R2AEA 都只有 DTLZ+WFG |
| 2 | 尺度扩展实验 | 更大 m 或更高 D 的补充实验 | PIEA 做到 m=100/D=500，R2AEA 做到 D=500 |
| 3 | 消融实验 | 7–9 个变体，逐模块拆 | PCSAEA 4 组、DACKEA 9 个变体、PIEA 4 组。**完整拆解见第十六节** |
| 4 | 参数敏感性 | 覆盖全部或大部分实例 | PIEA 在 **144 个实例**上做九组参数网格 |
| 5 | 运行时分析 | 组件级分解 + 横向秒数对比 | KHM-EF 最完整；MP-SAMaOEA 缺 |
| 6 | 真实工程案例 | 1–3 个，PF 未知用 HV | MP-SAMaOEA / DACKEA / PCSAEA / R2AEA 有；**PIEA / KHM-EF / RSEA 没有** |
| 7 | 代理精度本身的对比 | Recall / Precision / F1 或 RMSE | 只有 **PCSAEA** 做了 |

### 具体参数基线（照抄这一列）

| 项 | 建议值 | 依据 |
|---|---|---|
| 测试套件 | DTLZ1–7 + WFG1–9 + MaF1–7 | 三套齐全是一区标配（DACKEA 的做法），MaF 在超多目标场景更被认可 |
| 目标数 m | **5, 10, 15, 20, 25** | 覆盖真超多目标区间；MP-SAMaOEA 只到 20，KHM-EF 只到 10 |
| 决策维 D | **50, 100, 200**（如做高维）或 M+9 | PIEA 到 300，R2AEA 到 200 |
| FEmax | **300–500** | 超过 500 会被质疑不够"昂贵" |
| 初始样本量 | **11×D − 1**（LHS） | 领域惯例，MP-SAMaOEA、PCSAEA 都用 |
| 种群规模 N | 100（或按参考向量数定） | — |
| 每轮填充数 | 5 | MP-SAMaOEA、DACKEA 都用 5 |
| 对比算法 | **7 个起** | PIEA 7 个、DACKEA 7 个、PCSAEA 7 个 |
| 性能指标 | **IGD⁺ + HV**；真实问题只用 HV | IGD⁺ 是 Pareto compliant，比 IGD 更严谨 |
| 参考点数 | 约 10000 | PCSAEA、PIEA 都用这个量级 |
| 独立运行 | **30 次** | KHM-EF、PCSAEA、R2AEA、RSEA 是 30；MP-SAMaOEA、DACKEA 只有 20 |
| 统计检验 | Wilcoxon rank-sum，α = 0.05，记 +/−/≈ | 全部七篇都用 |
| 跨问题排名 | **Friedman test + average ranking** | **七篇里只有 PIEA 和 R2AEA 做了排名，全都没做 Friedman，这是加分点** |
| 平台 | PlatEMO + MATLAB | 领域标准，用别的要解释 |
| 硬件 | **写明 CPU 型号、主频、内存** | PIEA 写了（i7-9800X 3.80GHz / 16GB），DACKEA 没写 |
| 遗传算子 | SBX + 多项式变异，pc=1，pm=1/D，分布指数 20 | 全领域统一，照抄 |

### 对比算法候选清单

按类别选，每类至少 2 个：

- **回归类**：K-RVEA、KTA2、EDN-ARMOEA、HeE-MOEA、AB-SAEA、SFA-DE
- **分类/关系类**：CSEA、CPS-MOEA、REMO、PC-SAEA、MCEAD、θ-DEA-DP
- **指标类**：PIEA、DirHV-EI、EMMOEA
- **高维专用**：SR-SAEA、ADSAPSO、AVG-SAEA
- **超多目标专用**：PA-EMaOEA、MP-SAMaOEA、KHM-EF、DACKEA、SSDE、LDSAF

**技巧**：R2AEA 用 Table 1 列出每个对手的 Surrogate Type / Model / Strategy for Large D，同时完成"介绍对手"和"说明选择理由"。另外它直接沿用 REMO 的参数设置（6 个参考解、500 次代理评价）来保证公平——**站在对手肩膀上是标准操作，一定要写明"为公平起见我们沿用了 [n] 的设置"**。

### 报数句式

三数并列（推荐）：
> "MP-SAMaOEA obtains **21, 25, 19, 27, and 24 better** IGD⁺ values, **1, 0, 1, 0, and 0 worse**, and **5, 2, 7, 0, and 3 similar** IGD⁺ values compared to ..., respectively."

胜负统计：
> "PIEA won on **44 of 63 instances**, tied on 4, and lost on 15, demonstrating the superiority of the adopted approximation method."

**报完数必须归因，不能只报数：**
> "**The superior performance of MP-SAMaOEA can be attributed to the following.** ... **Consequently**, SAMaOEAs with higher surrogate model accuracy perform better."

### 划定优势边界与为不完全胜利辩护

**主动说明在哪类问题上不占优，反而增信。** KHM-EF：
> "Specially, KHM-EF demonstrates even more pronounced superiority when the problem dimension is high, **whereas it is less effective for linear hyper-plane and degenerate front problems**."

然后给机理解释（退化前沿上局部 Voronoi 密度估计失准导致冗余采样），并点名在 DTLZ6 上输给 PIEA 和 DirHV-EI。

万能辩护句：
> "**According to the no-free-lunch theorem, it is unrealistic to expect a single algorithm to achieve uniformly superior performance across all problem classes.**"

PCSAEA 更彻底，主动暴露最强对手：
> "It is worth noting that **REMO obtains the most results that are significantly better than PC-SAEA**, where REMO also uses a pairwise comparison based model."

### 两个符号约定的坑

1. **`+/−/≈` 的方向必须在表注里写死。** 七篇的方向其实是一致的（'+' 都表示所提算法优于对手），但表述方式不同，容易读错。RSEA 从对手视角写："Symbol '+' indicates that **the compared algorithm is significantly outperformed by RSEA**"；MP-SAMaOEA 从自己视角写："the symbols '+', '−', and '≈' represent that **the performance of our method is better than, worse than, or comparable with** that of the other comparative algorithms"。**建议用后一种（自己视角），歧义更小。**
2. **PCSAEA 正文写 "six representative" 但 §4.1 列了 7 个算法**，这是已发表论文里的笔误。**自查时把所有"数量词"和实际列表核对一遍。**

### 模拟昂贵场景的巧妙设计

R2AEA 在运行时实验里**把单次真实评价人为设为 1 分钟**来模拟真实昂贵场景，这样代理模型的训练开销占比才有意义。这个设计很聪明，可以借用。

---

## 十、结论与局限性

结论段的五步模板：**复述算法名 → 复述模块 → 复述实验结论 → However 承认局限 → 未来工作**。

局限性承认的标准写法（DACKEA）：
> "**Although** DACKEA shows competitive performance, **several issues remain. First**, ... **This may limit its scalability when** the number of decision variables or training samples increases further. **To address this, we plan to** ... **Second**, ... **Therefore, we also plan to** ..."

其他可用的局限性句式：
> "However, MP-SAMaOEA **lacks strategies to handle problems with multi-modal or irregular PFs**. Therefore, further research is warranted to enhance the optimization process and develop strategies for such challenges."
>
> "Despite its strong performance, KHM-EF shows **limited success on problems with degenerate Pareto fronts**, where it struggles to maintain both convergence and diversity. ... In future work, we plan to explore more shape-sensitive methods to more effectively handle degenerate Pareto fronts."
>
> "the **high time complexity of FNN training** limits the efficiency of R²AEA to some extent."（R2AEA 直接承认自己的效率短板）

展望的加分写法（PIEA）：
> "**In the future, we aim to extend** the indicator-based concept **to** another classification-based framework, **to exploit its potential further**."

**局限性一定要写，而且要写真实的局限。** 审稿人比你更清楚你的算法哪里弱，自己说出来是掌握主动权。常见可写的局限：退化/不规则 PF、多模态问题、决策变量进一步增大时的扩展性、代理训练本身的时间开销、参数虽自适应但仍有阈值。

---

## 十一、中英术语对照表

### 问题与场景

| 中文 | English | 缩写 | 备注 |
|---|---|---|---|
| 多目标优化问题 | multi-objective optimization problem | MOP | m = 2, 3 |
| 超多目标优化问题 | many-objective optimization problem | MaOP | m > 3 |
| 特超多目标优化问题 | super-many-objective optimization problem | — | m > 10，MP-SAMaOEA 的定义 |
| 昂贵多目标优化问题 | expensive multi-objective optimization problem | EMOP | — |
| 昂贵超多目标优化问题 | expensive many-objective optimization problem | EMaOP | — |
| 高维昂贵多目标优化问题 | high-dimensional expensive MOP | HEMOP | D ≥ 50 |
| 帕累托最优前沿 | Pareto-optimal front | PF | 目标空间 |
| 帕累托最优解集 | Pareto-optimal set | PS | 决策空间 |
| 决策空间 / 目标空间 | decision space / objective space | — | — |
| 真实评价次数 | (actual/true) function evaluations | FEs | FEmax 为预算上限 |
| 不规则前沿 | irregular PF | — | 退化 degenerate、不连续 discontinuous、凸 convex |
| 黑箱优化 | black-box optimization | — | — |

### 算法与模型

| 中文 | English | 缩写 |
|---|---|---|
| 代理辅助进化算法 | surrogate-assisted evolutionary algorithm | SAEA |
| 代理辅助超多目标进化算法 | surrogate-assisted many-objective EA | SAMaOEA |
| 克里金模型 / 高斯过程 | Kriging model / Gaussian process | GP |
| 径向基函数网络 | radial basis function network | RBF |
| 支持向量机（回归） | support vector machine (regression) | SVM / fitrsvm |
| 概率神经网络 | probabilistic neural network | PNN |
| 前馈神经网络 | feedforward neural network | FNN |
| 集成代理 | ensemble surrogate | — |
| 全局/局部代理 | global / local surrogate model | — |
| 回归代理 | regression-based surrogate | — |
| 分类代理 | classification-based surrogate | — |
| 关系模型 | relation model | — |
| 成对比较代理 | pairwise comparison based surrogate | — |
| 指标代理 | indicator-based surrogate | — |
| 拉丁超立方抽样 | Latin hypercube sampling | LHS |
| 模型管理 | model management | — |

### 填充准则与选择

| 中文 | English | 缩写 |
|---|---|---|
| 填充采样准则 | infill sampling criterion | — |
| 期望改进 | expected improvement | EI |
| 改进概率 | probability of improvement | PoI |
| 置信下界 | lower confidence bound | LCB |
| 自适应置信下界 | adaptive lower confidence bound | ALCB |
| 支配概率估计 | probabilistic non-dominance estimation | PND |
| 预测熵 | predictive entropy | — |
| 期望校准误差 | expected calibration error | ECE |
| 可靠性度量 | reliability measurement | — |
| 参考向量 / 参考点 | reference vector / reference point | — |
| 非支配排序 | non-dominated sorting | — |
| 环境选择 | environmental selection | — |
| 预筛选 | preselection | — |
| 基于偏移的密度估计 | shift-based density estimation | SDE |
| 强化支配关系 | strengthened dominance relation | SDR |
| 惩罚型边界交叉 | penalty-based boundary intersection | PBI |
| 拥挤度 | crowding degree / crowding distance | — |
| 自适应网格划分 | adaptive grid division | — |
| 精英前沿 | elite front | — |
| 维诺图 / 维诺体积 | Voronoi diagram / Voronoi volume | — |
| 径向投影 | radial projection | RadViz |
| 问题变换 / 重构 | problem transformation / reformulation | — |
| 变量关联 | variable association | — |

### 性能评价

| 中文 | English | 缩写 |
|---|---|---|
| 反世代距离 | inverted generational distance | IGD |
| 改进反世代距离 | IGD⁺（Pareto compliant） | IGD⁺ |
| 超体积 | hypervolume | HV |
| 间距指标 | Spacing | — |
| 最劣点 / 理想点 | nadir point / ideal point | — |
| 收敛性与多样性 | convergence and diversity | — |
| 开采与探索 | exploitation and exploration | — |
| 误差累积 | error accumulation / cumulative errors | — |
| 维度敏感性 | dimensionality sensitivity | — |
| 选择压力丧失 | loss of selection pressure | — |
| 支配抗性 / 支配稀疏 | dominance resistance / dominance sparsity | — |
| 秩和检验 | Wilcoxon rank-sum test | WRS |
| 弗里德曼检验 | Friedman test | — |
| 消融实验 | ablation study | — |
| 参数敏感性分析 | parameter sensitivity analysis | — |
| 无免费午餐定理 | no-free-lunch theorem | NFL |

### 高频动词与连接词

写作时反复用到的，避免重复：

- **提出类**：propose / present / develop / design / devise / introduce / employ / adopt / utilize / incorporate
- **改进类**：enhance / improve / boost / strengthen / refine / facilitate / accelerate
- **缓解类**：mitigate / alleviate / eliminate / reduce / avoid / circumvent / address / tackle
- **缺陷类**：suffer from / fail to / struggle to / be hindered by / deteriorate / degrade / be restricted to / fall short in / obscure
- **平衡类**：balance / trade off between / strike a balance
- **转折类**：However / Nevertheless / Nonetheless / Whereas / By contrast / In contrast / Despite / Although
- **递进类**：Moreover / Furthermore / Additionally / In addition / Besides / Specifically / More specifically
- **因果类**：thereby / thus / hence / consequently / as a result / owing to / due to / attributed to
- **强调类**：It is worth noting that / It should be highlighted that / Note that / Obviously / It is apparent that

---

## 十二、可复用英文句式库

全部摘自七篇原文，可直接改写使用。

### 摘要开头（领域定调）

1. "Surrogate-assisted many-objective evolutionary algorithms (SAMaOEAs) have become **a pivotal method for** solving expensive many-objective optimization problems (EMaOPs), **where** the cost of objective evaluations is computationally prohibitive."
2. "Multi-objective optimization problems in many real-world applications **are characterized by** computationally or economically expensive objectives, **which cannot provide sufficient function evaluations for** evolutionary algorithms to converge."

### 缺陷指认

3. "However, most existing research **remains restricted to** low-/medium-dimensional problems, with **very little attention paid to** addressing problems involving decision variables with more than 100 dimensions."〔占位型，最有力〕
4. "Furthermore, most existing infill criteria select solutions to be re-evaluated based on their performance in the current solution set, and **these criteria fail to consider how solutions perform as training samples**."〔盲点型，最推荐〕
5. "**Despite the wealth of research on** X, most existing studies have focused on ..., **whereas few studies have explored** ..."
6. "**Although** SAEAs have achieved noticeable progress on ..., **limitations still persist in** A **and in** B. **Specifically**, most existing SAEAs still have difficulty **simultaneously** maintaining ..."
7. "**While** a global model can capture the overall trend of the objective function, **its inherent smoothness makes it difficult to** capture local details in regions with abrupt changes, high nonlinearity, boundaries, or sparse data."
8. "**Given this fact, some studies attempt to** utilize the advantages of ... **However, they lack adaptability in** adjusting ... **This limitation can result in** ..."
9. "the performance of most SAEAs deteriorates dramatically when tackling high-dimensional optimization problems, **let alone** HEMOPs."
10. "**inaccurate models could mislead the search directions**."
11. "their efficiency degenerated seriously on problems with more than three objectives, **which mainly due to the loss of selection pressure**."
12. "These strategies are capable of selecting a set of evenly distributed solutions in the objective space but the convergence is not considered, **which may result in a set of evenly distributed but poorly converged solutions**."
13. "they fail to maintain diversity on problems with irregular PFs (degenerated, discontinuous, and convex), **since they have predefined the reference information ... without any prior knowledge about the PFs of the problems**."
14. "**In summary, current SAEAs face two major challenges. In** model construction, they often experience ... **In** infill sampling criteria, they struggle to ..."

### 相关工作的内嵌批判

15. "In methods belonging to this category, **although** training a surrogate for a relation or performance indicator can reduce the errors, **its effectiveness largely depends on whether** the relation can accurately capture the underlying characteristics."
16. "**Nevertheless**, the screening capability is relatively poor, **owing to its less informative output**."
17. "**However**, ... requires the training of one or multiple models for each objective. **Moreover**, for high-dimensional problems, building an accurate regression model **is hindered by an insufficient number of training samples**."
18. "the rough fitness functions and model management strategies **obscure the advantages of** pairwise comparison based models in expensive multi-objective optimization."
19. "Known diversity maintenance strategies can be **roughly divided into four types. The first type** ... **The second type** ... **The third type** ... **The fourth type** ..."

### 方法引入与转折

20. "**To address the aforementioned challenges / To address these problems**, this paper proposes ..., **called / termed** XXX."
21. "**Motivated by** the parameter-free adaptive reference-point strategy from AR-MOEA [40], this paper proposes ..."
22. "**Based on the above analysis**, the existing SAMaOEAs have demonstrated significant competitiveness. **Note that** the quality of the surrogate models has a significant impact."
23. "**Obviously**, this process requires a substantial number of actual evaluations, **which are prohibitively costly for** EMaOPs."
24. "**Hence, the intertwined nature of** high-dimensional and many-objective characteristics **amplifies the complexity**."
25. "**Building upon the two core components outlined above, we propose** the XXX algorithm."

### 机制论证

26. "**The rationale behind the proposed** X strategy **is that** the reliability of a local Kriging model is closely related to the spatial support provided by its neighboring training samples."
27. "**The main novelty therefore lies not in** weighted ensemble prediction itself, **but in** the distance-aware reliability modeling of local models."
28. "**Different from these studies**, the proposed X **focuses on** the spatial reliability of local models."
29. "The relation model is based on the idea that **the relationship between solutions is more tractable to learn than the exact values or categories**."
30. "They **forego the prediction of absolute categories**, **opting to concentrate on** forecasting the relative rankings or relationships among solutions."
31. "**which is more practical since** good or bad solutions are relative but not absolute."
32. "**An imbalanced dataset could highly deteriorate the performance of classifiers.**"
33. "high entropy indicates greater uncertainty in classifications, **whereas** low entropy signifies high confidence."
34. "the adaptive penalty parameter r **enhances the convergence maintenance at the early stage** of the evolution **and enhances the diversity maintenance when** the solutions are well converged."
35. "**To the best of our knowledge, this is the first time that** the radial projection is used to project high-dimensional objective vectors into a 2-dimensional radial space for diversity maintenance."
36. "**It should be highlighted that no specific parameter is involved in** RSEA."
37. "**Although this may weaken the exploitation capability**, the number of generations **can be increased to compensate for this**."

### 实验设置与结论

38. "Since the Pareto fronts of benchmark MOPs are known, **the widely used inverted generational distance (IGD) metric is used** ... **By contrast**, since the Pareto fronts of real-world MOPs are unknown, **the hypervolume (HV) metric is used**."
39. "**To avoid experimental randomness**, the Wilcoxon rank sum test **is conducted at a significance level of 0.05**."
40. "XXX obtains **21, 25, 19, 27, and 24 better** IGD⁺ values, **1, 0, 1, 0, and 0 worse**, and **5, 2, 7, 0, and 3 similar** IGD⁺ values compared to ..., respectively."
41. "PIEA **won on 44 of 63 instances, tied on 4, and lost on 15**, demonstrating the superiority of the adopted approximation method."
42. "**The superior performance of** MP-SAMaOEA **can be attributed to the following.** ... **Consequently**, ..."
43. "Specially, KHM-EF demonstrates even more pronounced superiority when the problem dimension is high, **whereas it is less effective for** linear hyper-plane and degenerate front problems."
44. "**According to the no-free-lunch theorem, it is unrealistic to expect a single algorithm to achieve uniformly superior performance across all problem classes.**"
45. "**Considering that** η << n < N, **according to the operation rules of symbol** O, the time complexity **can be simplified as** O(dN³)."
46. "Variants with **intermediate thresholds** (e.g. 0.25 to 0.75) **outperform extreme settings, highlighting the importance of both** exploitation **and** exploration."
47. "**all algorithms are assessed without considering constraints to maintain fairness**."〔诚实声明，增信〕

### 结论与展望

48. "**Although** DACKEA shows competitive performance, **several issues remain. First**, ... **To address this, we plan to** ... **Second**, ... **Therefore, we also plan to** ..."
49. "However, XXX **lacks strategies to handle** problems with multi-modal or irregular PFs. **Therefore, further research is warranted to** ..."
50. "**In the future, we aim to extend** the indicator-based concept **to** another classification-based framework, **to exploit its potential further**."
51. "This work demonstrates that the idea of X **is very promising**."

---

## 十三、选题建议：五个可做的切口

七篇文献画出的技术地图上，有几个位置是明确空着的。按可行性排序，附风险提示。

### 切口 1：径向空间中的关系代理 ★★★★★

**空白在哪。** 目前所有关系模型（PCSAEA、REMO、R2AEA）的标签都来自 Pareto 支配或 θ-支配。但在 m ≥ 10 时支配关系几乎全部失效——非支配解比例趋近 1，三类标签（dominated / non-dominated / mutually non-dominated）严重退化。R2AEA 自己承认："as the number of objectives increases, the performance of R²AEA declines"。

**怎么做。** 把 RSEA 的径向投影当作**标签生成器**：对已真实评价的解做 2 维径向投影，在 2 维空间里算网格拥挤度和收敛度，用这个复合度量构造成对比较标签。这样标签在高 m 下不退化，而且天然平衡。

**为什么这个组合成立。** 它同时解决两个已知难题——PCSAEA 的"fitness 函数如何设计"（它用 SPEA2+SDE 混合式，作者自己说这是 rough 的）和高维目标下的标签失效。

**关键风险（务必注意）。** **不要做"RSEA + Kriging 直接昂贵化"。** 径向投影要求全部 m 个目标值已知才能投影，在昂贵场景下这意味着必须先预测全部 m 个目标——退回回归范式，丢掉关系范式的全部优势。**正确接法是：径向空间只用于标签构造与选择（基于已真实评价的解），绝不用于预测。** 这一点必须在论文里讲清楚，否则审稿人会立刻质疑。

**可用标题**：Radial-space relation surrogate for expensive super-many-objective optimization

### 切口 2：分区异构的模型管理 ★★★★☆

**空白在哪。** PCSAEA 的可靠性是一个全局标量（p⁺/p⁻），R2AEA 的熵是逐解一个数，DACKEA 的距离权重是逐解一个数——**没有一篇有空间结构**。

**怎么做。** 在 2 维径向空间（或聚类后的子空间）上做**网格级的局部可靠性图**：哪个区域代理可信就直接用，哪个区域该反向用，哪个区域该弃用。把 PCSAEA 的"三态 model management"升级为"分区异构 model management"。

**优势。** 这个空白很干净，而且与 DACKEA 的 distance-aware 思想同源但正交（它管融合权重，你管使用模式），容易讲清区别。

### 切口 3：闭环的阶段切换 ★★★☆☆

**空白在哪。** 现有的收敛/多样性调度全是**开环**（时间驱动）：RSEA 的 `r = 1−(t/t_max)²`、PCSAEA 的 `r = FE/FEmax`、R2AEA 的固定阈值 `tr = 0.5`、KHM-EF 的 τ 递减。R2AEA 的敏感性分析已经暗示最优 tr 依问题而变。

**怎么做。** 改成**闭环**：用验证集准确率或 ECE 校准误差实时触发阶段切换。

**评价。** 工作量小、故事完整、审稿人容易认可，但**创新性偏增量**，单独撑不起一区，适合作为主创新之外的第二个模块。

### 切口 4：关系模型的输入侧降维 ★★★★☆

**空白在哪。** PCSAEA 的 PNN 输入是两个决策向量拼接，D=21 时输入就是 42 维。PIEA 明确批评过 REMO："**doubles the data dimensions**"。R2AEA 做了降维（problem transformation, D → 2r）也做了关系模型，但**是串联在不同阶段，没有把降维喂进关系模型本身**。

**怎么做。** 先做 problem transformation 把 D 降到 2r，再在低维空间配对（输入变成 4r 维而非 2D 维）。这样成对样本数 N(N−1) 的优势才能在高维真正兑现。

**优势。** 这是一个明确被两篇论文各自触及但没人合上的缺口，而且能直接对标 REMO 和 R2AEA 两个强对手。

### 切口 5：m 与 D 同时做大 ★★★★★（最推荐，但工作量最大）

**空白在哪。** 见第一节坐标系——PIEA 做 D=300 但 m≤11，MP-SAMaOEA 做 m=20 但 D≤21。**m ≥ 20 且 D ≥ 100 的区域基本是空白。**

**怎么做。** 这不是一个技术切口，而是一个**定位策略**：把切口 1 或 4 的技术方案，放到 m ∈ {10,20,30} × D ∈ {100,200} 的实验设置上验证。

**立论方式照抄 PIEA**：先做一张统计表（按 m 和 D 双维度给主流方法分箱），把空白量化成可视事实；然后用"三条 challenge，第三条制造交叉难点"的方式论证必要性。

**风险提示。** 这个区域空着可能有客观原因——Kriging 在高维退化、支配关系在高 m 失效、参考向量数在高 m 爆炸。**先跑几个小实验确认技术路线可行，再动手写。** 如果发现某个环节根本跑不通，那本身就是有价值的发现，但要及早知道。

---

## 十四、八周执行计划

按你的目标层次，一区论文从零到投稿的合理周期是 3–4 个月。下面是前八周的密集期安排。

### 第 1 周：确认技术路线可行性

- 装好 PlatEMO，跑通 3–4 个对比算法的基线（K-RVEA、CSEA、REMO、PIEA），确认你能复现它们论文里的量级
- 把选定切口的核心机制写成 50 行以内的原型，在 DTLZ2（m=10, D=50）上跑一次，只看能不能跑、IGD 是不是在合理范围
- **门槛检查**：原型的 IGD 不需要比对手好，但必须与对手同量级。差一个数量级说明路线有问题，立即回到第十三节换切口

### 第 2 周：文献表与 gap 论证

- 精读 15–20 篇（本文七篇 + 你切口相关的对手），建 Excel 表记录：算法名、代理类型、m 范围、D 范围、FEmax、指标、创新点、缺陷
- 把这张表按 m 和 D 双维度分箱，做出 PIEA 式的 Table 1
- **产出**：引言第三块（相关工作分类）+ 第四块（缺陷指认）的中文草稿

### 第 3–4 周：算法定型与消融设计

- 确定 2–3 个创新模块，**起名时就想好消融变体的名字**（学 KHM-EF）
- 每个模块写清"rationale behind"——写不出机理论证的模块要砍掉
- 设计消融方案：每个贡献对应一个变体，凑到 6–9 个
- 写复杂度分析（照第八节四步模板）
- **产出**：§3 方法章节完整中文草稿 + Algorithm 1 伪代码

### 第 5–6 周：主实验

- 基准实验：DTLZ1–7 + WFG1–9 + MaF1–7，m ∈ {5,10,15,20}，30 次独立运行
- 7 个对比算法，IGD⁺ + HV 双指标
- Wilcoxon rank-sum + Friedman + average ranking
- **这一步最耗机时，提前排好队列，用脚本批量跑**。300 次评价 × 30 次运行 × 23 问题 × 4 个 m × 8 个算法，估算一下总时长再动手

### 第 7 周：补齐七件套

- 消融实验（6–9 变体）
- 参数敏感性（尽量覆盖全部实例，学 PIEA）
- 运行时分析（组件级分解 + 横向对比，可借用 R2AEA 的"单次评价设为 1 分钟"技巧）
- 真实工程案例：水资源规划（m=5, D=3）、汽车座舱设计（m=9, D=11）、车辆碰撞安全（m=3, D=5）、TREE1–6（电力系统，D=120）都是现成可用的，PlatEMO 里有实现
- 代理精度本身的对比（这是差异化项，别省）

### 第 8 周：成文与自查

- 按第二节骨架搭好英文全文
- 摘要按第三节六句法写
- 结论按第十节五步模板写
- 走一遍第十五节检查表

**之后**：找导师和同门各读一遍，重点让他们挑"哪句话看不懂"和"哪个结论没有实验支撑"。然后润色，投稿。

---

## 十五、投稿前检查表

### 内容完整性

- [ ] 引言里有 m > 3（或 m > 10）的明确定义，与实验设置的 m 取值一致
- [ ] 昂贵性有具体工程例子和数量级对比，不是空说
- [ ] 相关工作用了成熟的分类维度（代理角色 / 填充准则），每类结尾有内嵌批判
- [ ] 有一张 PIEA 式的统计表把 gap 量化
- [ ] 缺陷指认属于"盲点型"或"占位型"，不是"这件事很难"
- [ ] 每条贡献都对应一个独立的消融表
- [ ] 每个创新模块都有 "the rationale behind ... is that ..." 的机理论证
- [ ] 有 §Motivation 单独立节
- [ ] 有 §Discussion 用 "not in A, but in B" 切割最接近的 3–4 篇工作
- [ ] 有完整的 O(·) 复杂度分析（符号约定 → 逐模块 → 取主导项）
- [ ] 有 Algorithm 1 伪代码，Input 里列全所有符号
- [ ] 结论段承认了真实的局限，并给了对应的 future work

### 实验完整性（七件套）

- [ ] 基准套件：DTLZ + WFG + **MaF** 三套齐全（一区标配，别只做两套）
- [ ] 目标数覆盖 m ≥ 15 或 20（如做超多目标）
- [ ] FEmax ≤ 500
- [ ] 对比算法 ≥ 7 个，分属不同类别
- [ ] 独立运行 **30 次**（不是 20）
- [ ] IGD⁺ + HV 双指标；真实问题用 HV
- [ ] Wilcoxon rank-sum α=0.05 + **Friedman test + average ranking**
- [ ] 消融实验 6–9 个变体
- [ ] 参数敏感性覆盖大部分实例
- [ ] 运行时分析有组件级分解
- [ ] **真实工程案例 1–3 个**
- [ ] 代理模型本身的精度对比（Recall / Precision / F1 或 RMSE）
- [ ] 尺度扩展实验（更大 m 或更高 D）
- [ ] 写明 CPU 型号、主频、内存、MATLAB 版本、PlatEMO 版本

### 写作细节自查

- [ ] `+/−/≈` 的方向在表注里写死，全文统一（RSEA 与其他篇相反，别抄错）
- [ ] 所有数量词与实际列表核对（PCSAEA 有 "six" vs 实际 7 个的笔误）
- [ ] 摘要里的缩写在摘要内自足
- [ ] 报数之后必有归因句，不只报数
- [ ] 有划定优势边界的句子（"whereas it is less effective for ..."）
- [ ] 沿用对手参数设置的地方写明"为公平起见"
- [ ] 图表能独立看懂（表注说明检验方法、加粗含义、底色含义）
- [ ] 参考文献 40–60 篇，近三年占三分之一以上
- [ ] 全文查一遍 however / moreover 是否重复过多，用第十一节的动词表替换

### 投稿目标参考

| 期刊 | 分区 | 特点 | 适配度 |
|---|---|---|---|
| IEEE Trans. Evolutionary Computation | 一区 Top | 最看重理论深度与创新性，审稿慢（8–14 个月） | 需要强创新点 + 完整七件套 |
| Information Sciences | 一区 | 接受面较宽，重视实验充分性 | PIEA 发在这里，可直接对标 |
| Swarm and Evolutionary Computation | 一区 | 本领域主场，七篇里五篇在这 | **最推荐首投** |
| IEEE Trans. Cybernetics | 一区 | 偏系统与控制视角 | 需要包装成系统问题 |
| Applied Soft Computing | 二区 | 审稿较快，RSEA 发在这 | 备选 |

**首投建议 SWEVO**：七篇里 MP-SAMaOEA、KHM-EF、DACKEA、PCSAEA、R2AEA 都在这里，编辑和审稿人熟悉这个话题谱系，你引用他们的工作也顺理成章。如果被拒，按意见改完转投 Information Sciences。

---

## 附：七篇文献的完整题录

1. Cheng He, Ye Tian, Yaochu Jin, Xingyi Zhang, Lianghao Li. *A radial space division based evolutionary algorithm for many-objective optimization*. Applied Soft Computing, 61 (2017) 603–621.
2. Ye Tian et al. *A pairwise comparison based surrogate-assisted evolutionary algorithm for expensive multi-objective optimization*. Swarm and Evolutionary Computation, 80 (2023) 101323.
3. Y. Li, W. Li, S. Li et al. *A performance indicator-based evolutionary algorithm for expensive high-dimensional multi-/many-objective optimization*. Information Sciences, 678 (2024) 121045.
4. Shuwei Zhu et al. *Regression and relation-assisted evolutionary algorithm for high-dimensional expensive multi-objective optimization*. Swarm and Evolutionary Computation, 97 (2025) 101978.
5. Xiaotong Liu, Chaoli Sun, Hao Wang, Liming Zhang. *A surrogate-assisted evolutionary algorithm with multi-perspective infill sampling for expensive super-many-objective optimization problems*. Swarm and Evolutionary Computation, 101 (2026) 102275.
6. Chang Liu, Baide Chen. *A kriging-assisted evolutionary algorithm with hybrid-metric infill sampling criterion for expensive many-objective optimization*. Swarm and Evolutionary Computation, 107 (2026) 102440.
7. *Distance-aware cooperative Kriging-assisted evolutionary algorithm for expensive many-objective optimization*. Swarm and Evolutionary Computation, 107 (2026) 102473.

---

## 十六、消融实验的完整拆解

> 本节针对七篇一二区文献（五篇 SWEVO、一篇 Information Sciences、一篇 ASOC；没有 TEVC）逐篇拆消融。七篇都纳入。读完应能直接回答四件事：每个模块有没有做消融、对照组怎么造、指标是不是只报 IGD、数据集是全做还是挑的。末尾对照 HPDC-MaOEA 现有的 §4.3–4.6 给出补齐清单。

### 16.1 先给总览

七篇消融的完成度差一个档次。**没有一篇把"声称的每个模块 × 机理级指标 × 全实例"三件事同时做完。** 这不是偶然——消融比主实验贵，所有人都在子集上做，关键是**选哪些、为什么选、有没有把因素缠在一起**。

| 论文 | 声称模块数 | 真正做了消融的 | 变体构造 | 机理级指标（非 IGD/HV） | 消融实例数 / 主实验实例数 | 选题理由写了吗 |
|---|---|---|---|---|---|---|
| **PCSAEA** | 2 | 2（代理类型 + 三态管理；另加配对规模） | 替换 + 多档位 | **Accuracy / Recall / Precision / F1** | 18 / 约 40+ | 无 |
| **PIEA** | 2+1 扩展 | 2（指标建模、history 选择）+ 指标组合；infill 本身未拆 | 替换 + 组合矩阵 | 墙钟时间；用"随 m 翻转"间接说累积误差 | 63 / 144（建模对象）；45 / 144（指标组合）；27 / 144（history） | 无（只写 several problems） |
| **R2AEA** | 2 | 2（RWO 回归、RMO 的熵 infill）+ 切换阈值 | 删除/替换混杂 + 多档位 | **PF 散点图**（RWO 挤在一小段、RMO 铺开） | 16 / 48；tr 敏感性仅 7 | **有**（WFG 上 RMO 更有效；DTLZ2/7 代表规则/不规则 PF） |
| **MP-SAMaOEA** | 2 | **只测了 1**（多视角填充）；k-means **零消融** | 删除，但 NDS→roulette 混杂 | 无（口头说提升代理质量，不报 RMSE） | 12 / 约 48 | **有**（DTLZ3 多峰测收敛，DTLZ2 凹测多样性，DTLZ4 偏置、DTLZ7 不连续） |
| **KHM-EF** | 3 | 2（HM 填充 + EF）；全历史建模型未测 | **组合矩阵** HM/EI/LCB × 有/无 EF | **Spacing**（纯多样性）+ 组件运行时间 | 21 / 约 48 | 无 |
| **DACKEA** | 2 | 2，拆成 **9 个变体**（最细） | 每次改一块钉死其余（替换树） | 无（用 PF 类型当探针） | 21 / 69（**只用 DTLZ**） | 无 |
| **RSEA** | 3 | 1（投影权重的置换/反转）；网格与 r 未测 | 几何对照（固定 / 反转 / 每代打乱） | 弱（纯 IGD） | 21（主实验含 15 目标，置换没做 15） | 无 |

四条立刻能用的结论：

1. **替换法比删除法干净。** "把提出的填充换成 EI/LCB"（KHM-EF）、"把成对代理换成回归 RBF / 分类 PNN"（PCSAEA）、"把距离加权换成均值融合 / 随机选模型"（DACKEA）——审稿人看到的是"相对已知基线的增益"，不是"删掉一块当然变差"。
2. **声称了就必须测。** MP-SAMaOEA 的 k-means、KHM-EF 的"全历史建 Kriging"、RSEA 的自适应网格、PIEA 的 infill 策略本身，都是贡献列表里有、消融里没有。审稿人会抓这个。
3. **机理指标比 IGD 更能证明"模块起到了它声称的作用"。** PCSAEA 的 Accuracy 证明成对模型学得更准；KHM-EF 的 Spacing 证明 EF 真的在保均匀；R2AEA 的 PF 散点证明两阶段真的是"先收敛后铺开"。**只报 IGD 变好，等于没证明机理。**
4. **消融几乎都在子集上做。** 唯一写了选题理由的是 MP-SAMaOEA 和 R2AEA。其余要么不解释，要么用 "several problems" 一带而过。**选题可以挑，理由必须写。**

### 16.2 四种变体构造方式

按推荐度排序。写自己的消融时，优先用后面两种。

**类型 A「删除法」——最弱，但最常见。** 只做"有该模块 vs 没有"。R2AEA 的 `R²AEA-R`（去掉预测熵）、DACKEA 的 `DACKEA-S1/S2`（去掉探索/开发阶段）、MP-SAMaOEA 的 `DEC/OBJ`（去掉一个视角）都是。问题是：删掉一块之后算法当然变差，审稿人会问"是这个模块本身好，还是任何一块补丁都会让它变好"。单独用 A 撑不起一区。

**类型 B「替换法」——一区标配。** 把提出的模块换成领域里已有的经典做法：
- PCSAEA：成对 PNN → RBF 回归 / PNN 分类
- KHM-EF：HM → EI / LCB
- DACKEA：距离加权 → 均值融合 / 随机选模型；整套填充 → EI
- PIEA：逼近指标 → 每目标一个 SVM + NSGA-III（`PIEA_o`）；history 选择 → 等概率随机

替换法回答的问题是"**相对已知替代方案，你的设计是否更好**"，这才是贡献。

**类型 C「多档位法」——用来拆模块内部的关键设计。** 不是有无，而是取几个合理档位：
- PCSAEA：配对规模 N/2、N/3、N/4；三态管理分别锁死在"始终正向 / 始终反向 / 动态"
- PIEA：窗宽 τ ∈ {5,10,15,20,25,30}；w_max × η 九组网格
- R2AEA：切换阈值 tr ∈ {0, 0.25, 0.5, 0.75, 1}，两端等价于"只用 RMO / 只用 RWO"

C 的价值是证明"**不是随便取个参数就行，这个具体选择有理由**"。PCSAEA 的 N/4 档位还配了 Average Accuracy（0.604 > 0.585 > 0.571），这是档位选择的机理证据。

**类型 D「组合矩阵法」——最干净，也最贵。** 两个模块叉乘：
- KHM-EF：填充 ∈ {HM, EI, LCB} × EF ∈ {开, 关}，6 个格子。能同时回答"HM 是不是比 EI/LCB 好"和"EF 的增益是否独立于填充准则"
- PIEA 的 Variant 1–6：三个指标的 2³−1 组合（缺全 0），证明"三个一起用比任意子集好"

D 能看出交互作用。KHM-EF 的关键发现是：即使关掉 EF，HM 单独也全面优于 EI 和 LCB——这说明 HM 的贡献不依赖于 EF。**如果你的两个模块被声称是"相互协调"的，D 是唯一能验证这句话的设计。** HPDC-MaOEA 的贡献正好是两个相互协调的接口，这一点对你特别重要，见 16.7。

### 16.3 逐篇拆解：变体、指标、数据集

#### PCSAEA（SWEVO 2023）——机理指标做得最好的一篇

**变体（3 组，全部在 WFG1–9 × m=2,3 上，共 18 实例）：**

| 变体 | 改了什么 | 类型 | 相对完整算法的 W/T/L |
|---|---|---|---|
| `PC-SAEA′ (regression)` | 成对 PNN → RBF 回归，直接预测 fitness | B | 0 / 18 / 0（完败） |
| `PC-SAEA′′ (classification)` | 成对 PNN → PNN 分类，判断更接近好解还是差解 | B | 0 / 12 / 6 |
| `PC-SAEA′ (N/2)` | 配对规模改为 N/2 最好 × N/2 最差 | C | 0 / 14 / 4 |
| `PC-SAEA′′ (N/3)` | 配对规模 N/3 | C | 1 / 11 / 6 |
| `PC-SAEA′ (directly used)` | 三态管理锁死为始终正向用 | C | 0 / 14 / 4 |
| `PC-SAEA′′ (reversely used)` | 锁死为始终反向用 | C | 0 / 12 / 6 |

注意同一记号 `′/′′` 在三张表里复用、含义不同，这是反面教材。

**机理指标（这是它比其他六篇高一档的原因）：**
- Table 5：代理自身的 Recall / Precision / Accuracy / F1，在 DTLZ2、DTLZ3、WFG7、MaF4 上对比 CPS-MOEA、K-RVEA、CSEA、EDN-ARMOEA、REMO。PC-SAEA 16 项中 12 项最优。**这证明的是"成对模型学得更准"，不是"算法 IGD 更好"。**
- Table 7 末行 Average Accuracy：N/4 = 0.604 > N/3 = 0.585 > N/2 = 0.571，直接支撑"更清晰的分类边界"这个设计理由。
- 运行时间：回归 1627 s / 成对 1118 s / 分类 245 s。

**数据集：挑了 WFG 全套 18 实例，主实验的 DTLZ、MaF、三个真实问题都没进消融。** 论文没有解释为什么。可推断（非原文）：WFG 尺度不一、可变连杆，用来压模块稳健性。代理精度那组另挑了 4 个问题（标准凹 / 多峰 / 偏置可分 / 倒置），覆盖景观类型。

**没消融的：** fitness 函数本身（dominance-only vs crowding-only）；PNN 相对 FNN/SVM 等成对架构。

**可抄的结论句：**
> "pairing the N/4 best solutions with the N/4 worst solutions leads to the highest test accuracy."
> "the original PC-SAEA outperforms the two variants, hence the effectiveness of the proposed reliability based model management strategy can be evidenced."

#### PIEA（Inf. Sci. 2024）——组合矩阵最完整，但有因素缠绕

**变体：**

| 变体 | 改了什么 | 类型 | 数据集 | 结果 |
|---|---|---|---|---|
| `PIEA_o` | 每目标一个 SVM + NSGA-III 优化，infill 仍看指标值 | B | DTLZ1–7 全套，m={3,7,11}，d={100,200,300}，**63 实例** | 44 胜 / 4 平 / 15 负 |
| Variant 1–6 | 三指标的全部非空子集 | D | DTLZ4、DTLZ7、WFG2、WFG4、WFG9，m×d 全开，**45 实例** | ranking：PIEA 3.04 最优，单用 I_MD 的 V3 最差（6.07） |
| `random` + τ 六档 | history 选择 → 等概率；窗宽 5 到 30 | B+C | 仅 200 维 WFG1–9，m={3,7,11}，**27 实例** | history 优于 random |
| w_max × η 九组 | {10,20,30} × {1,5,10} | C | **全部 144 实例** | 参数网格，不是模块消融 |
| `PIEA_c` | 回归 SVM → 分类 SVM | B | 独立成节的概念迁移实验 | — |

**机理指标：** Table 11 平均墙钟时间——单模型 PIEA 明显短于逐目标 `PIEA_o`，且随 m、d 增长更平缓。这支撑"只建一个模型、减轻累积误差/建模开销"。累积误差本身没有直接测（没有逐目标 RMSE 随 m 的曲线），论证靠的是 **IGD+ 随 m 的交叉模式**：3 目标上 `PIEA_o` 有时更好，目标数升高后 PIEA 反超。这是间接证据，能用，但不如直接测误差累积硬。

**数据集是七篇里分层最清楚的，但也是最不透明的：** 建模对象用全部 DTLZ（63），指标组合用"several problems"（45），history 只用 200 维 WFG（27），参数网格才铺满 144。**论文完全没解释为什么指标组合挑 DTLZ4/7 + WFG2/4/9。** 可推断：DTLZ4 偏置、DTLZ7 不连续、WFG2 不连续、WFG4 多峰凹、WFG9 欺骗+不可分——覆盖多样性/多峰/不规则 PF。history 选 WFG 是因为尺度混杂、更吃指标切换。这些理由应该写进正文，它没写。

**硬伤：`PIEA_o` 同时换了"建模对象 + 优化器（NSGA-III）"，两个因素缠在一起。** 如果 PIEA 赢了，你分不清是"逼近指标比逼近目标好"还是"它的优化器比 NSGA-III 好"。一区审稿人会抓这个。HPDC-MaOEA 的 §4.3 有同类问题，见 16.7。

**没消融的：** infill/SAE 策略本身（η 预选 + 多样性再选 vs EI / 随机 / 只选指标最优）。

#### R2AEA（SWEVO 2025）——唯一用散点图讲机理的，但预算不对等

**变体：**

| 变体 | 改了什么 | 类型 | 数据集 | 结果 |
|---|---|---|---|---|
| `WO` vs `RWO` | 去掉回归，权重优化用真实目标；**且 FEmax=3000 vs RWO 的 500** | A/B 混杂 | DTLZ1–7 + WFG1–9，m=3，D=100，**16 实例** | 6/4/6 |
| `R²AEA-R` | 去掉预测熵，退回 REMO 原 infill（只选预测分最高） | A 兼 B | DTLZ1–7 + LSMOP1–9，m=3，D=100，**16 实例** | 1/5/10 |
| tr ∈ {0, 0.25, 0.5, 0.75, 1} | 切换阈值；两端 = 只用 RMO / 只用 RWO | C（两端等价 A） | **仅 DTLZ1–7，7 实例** | 平均秩 5 / 2.43 / **2.28** / 2.43 / 2.85，tr=0.5 最优 |

**机理指标：** Fig. 9 解分布（bi-obj DTLZ4/DTLZ7，D=100）——RWO 很快挤到一小段 PF、多样性差；RMO 靠不确定性铺开。这是两阶段"先剥削后探索"最直接的证据，比任何 IGD 表都有说服力。**HPDC-MaOEA 的双模式声称的也是两种准则产生不同批次，R2AEA 这张散点图是你 §4.5 可以对照的写法。**

**数据集是七篇里唯一写了选题理由的之一：**
> "To highlight the influence of r_m in RMO, the experiments are conducted on the WFG test suite, where RMO demonstrates superior effectiveness."
> "two representative functions, i.e. DTLZ2 and DTLZ7, with regular PF and irregular PF, respectively"（注意：正文写 DTLZ2/7，图注却是 DTLZ4/7，文图不一致，反面教材）

**硬伤：WO/RWO 的评价次数 3000 vs 500，消融不干净。** "500 次真实评价的 RWO ≈ 3000 次的 WO"这个叙事很诱人，但因果被预算混淆。正确做法是两边都用 500，或者明确说这是"样本效率对比"而不是"模块消融"。

**没消融的：** voting 权重本身；RWO 的问题改写相对其它降维；RBF vs GP、FNN vs 其它关系模型。voting 与 entropy 也没拆成 2×2。

#### MP-SAMaOEA（SWEVO 2026）——选题理由写得最好，但模块漏测

**变体（只有 2 个，只覆盖 1 个模块）：**

| 变体 | 改了什么 | 类型 | W/T/L |
|---|---|---|---|
| `MP-SAMaOEA-DEC` | 只保留决策空间欧氏距离，**并把非支配排序换成轮盘赌**选 5 个 | A（混杂） | 8 / 0 / 4 |
| `MP-SAMaOEA-OBJ` | 只保留目标空间夹角，同样换成轮盘赌 | A（混杂） | 12 / 0 / 0 |

**k-means 辅助多样性增强是贡献列表第 2 条，但完全没有消融。** 这是明显缺口。

**机理指标：无。** 消融只用 IGD+。论文口头归因：
> "the solutions selected by the infill criterion proposed in MP-SAMaOEA can enhance the quality of the overall surrogate."

但**没有测代理精度**。声称"提升代理质量"却不报 RMSE 或预测误差，这是七篇里的普遍病（只有 PCSAEA 治好了）。

**数据集：DTLZ2/3/4/7 × m∈{6,10,20}，12 实例。这是七篇里选题理由写得最清楚的一段，直接可套：**
> "These test functions are selected for the following reasons [55]: DTLZ3 is designed to evaluate the convergence of algorithms due to the multi-modal characteristics. DTLZ2 is primarily designed to assess the diversity of algorithms, as its Pareto front is non-convex. Furthermore, DTLZ4 and DTLZ7 are also employed to evaluate the diversity of algorithms due to their bias and discontinuity characteristics."

**这个句式的价值：它把"挑了 4 个问题"变成"每个问题对应一种被测能力"。** 收敛能力 → DTLZ3（多模态）；多样性 → DTLZ2（非凸）；多样性在困难分布下 → DTLZ4（偏置）、DTLZ7（不连续）。审稿人就无法说你挑了容易的。

**硬伤：DEC/OBJ 同时改了两件事**（删一个视角 + NDS 换 roulette），因果不纯。跟 PIEA 的 `PIEA_o` 是同一类错误。

#### KHM-EF（SWEVO 2026）——矩阵设计最干净，机理指标第二好

**变体（组合矩阵，填充 3 档 × EF 2 档）：**

| 变体 | 填充准则 | EF | 说明 |
|---|---|---|---|
| `KHM` | HM（提出的） | 关 | 文中称 baseline variant |
| `KEI` | EI | 关 | 类型 B 替换 |
| `KLCB` | LCB | 关 | 类型 B 替换 |
| `KHM-EF` | HM | 开 | 完整算法 |
| EI+EF、LCB+EF | EI / LCB | 开 | 文中称 "the EI and LCB variants using the EF strategy"，但 Table 10 是图片，精确变体名未在正文给出（反面教材） |

**这个矩阵的关键发现：即使关掉 EF，HM 单独也在所有测试问题上优于 EI 和 LCB。**
> "when the EF strategy is not applied, HM alone outperforms the variants using EI or LCB alone on all test problems."

这句话证明了 HM 的贡献**独立于** EF——这是删除法永远得不到的结论。

**机理指标（两类，都很值得抄）：**
1. **Spacing**（Fig. 10，DTLZ2）：纯多样性指标，对 KHM/KLCB/KEI 各画有/无 EF 的曲线。
   > "The Spacing metric focuses on measuring the uniformity of solution distribution and is a purely diversity-oriented metric."
   > "the diversity of the model is significantly improved after adopting the elite front strategy."

   **EF 声称的作用是"缓解种群拥挤导致的多样性差"，Spacing 直接测的就是均匀性。指标与声称一一对应，这才叫机理验证。**
2. **组件级运行时间**（Table 8/9）：EF 自身开销 <0.01%；但因为缩小了种群，KHM → KHM-EF 在 M=5 时从 6.58e2 降到 5.67e2 秒（约 −13.8%）。**这验证了贡献列表里"while also accelerating the runtime by reducing the population size"那半句。声称了效率收益，就要有秒数支撑。**

**数据集：WFG1/2/5/8 + DTLZ2/4/7 × m∈{3,5,10}，21 实例。消融节没解释为什么挑这 7 个。** 主实验有 PF 几何分类可作推断：覆盖了凸（WFG1/2）、凹（DTLZ2）、偏置（DTLZ4、WFG1）、不连续（DTLZ7、WFG2）、多峰欺骗（WFG5），**但砍掉了线性（DTLZ1、WFG3）和退化（DTLZ5/6、WFG3）**——而主实验结论恰好承认算法在线性和退化前沿上较弱。**把主实验已暴露的短板题型从消融里拿掉，审稿人如果细心会发现。别学这个。**

**没消融的：** 贡献 1（用全部历史样本建 Kriging、只用精英子集迭代）。

#### DACKEA（SWEVO 2026）——变体拆得最细，但只在 DTLZ 上做

**9 个变体，两个模块各拆一组，每次只改一块、其余钉死：**

| 变体 | 改了什么 | 类型 | W/T/L（相对完整算法） |
|---|---|---|---|
| `DACKEA-GP` | 去掉局部模型，只用全局 Kriging | A | 0 / 18 / 3 |
| `DACKEA-ME` | 距离加权 → 固定均值融合 | B | 0 / 16 / 5 |
| `DACKEA-RM` | 距离加权 → 随机选模型 | B | 0 / 17 / 4 |
| `DACKEA-S1` | 去掉探索阶段 | A | 差异最小的一个变体（大量 ≈） |
| `DACKEA-S2` | 去掉开发阶段 | A | 在 DTLZ1/3/5/6 上明显更差 |
| `DACKEA-RA` | 自适应切换 → 随机切换 | B | 完整算法占优 |
| `DACKEA-AD` | 多元高斯密度 → 角度密度 | B | 完整算法占优 |
| `DACKEA-NU` | 局部支配势 → 被支配邻居比 | B | 在 DTLZ6/7 上差距明显 |
| `DACKEA-EI` | 整套填充 → 标准 EI | B | **完整算法在 19 个问题上更优**（正文明确给出） |

（第一组的 18/16/17 是正文原句 "DACKEA outperforms DACKEA-GP, DACKEA-ME, and DACKEA-RM on 18, 16, and 17 test problems, respectively"；第二组正文只逐个叙述、未给汇总三数，故此处按正文表述记录。）

**这是七篇里"替换法"用得最充分的样本。** 六个 B 型变体，每个都对应一个"读者会想到的简单替代方案"，逐个排除。

**机理指标：无。** 消融只用 IGD。机理论证靠**"在某类 PF 上谁输"的叙事**：GP 在 DTLZ1/3（多峰）和 DTLZ5（退化）差 → 说明单全局模型不够；ME 在 DTLZ1/4 差 → 固定均值不能适应空间可靠度；S2 在 DTLZ1/3/5/6 差 → 纯不确定性缺收敛导向。**这叫"用问题特性当机理探针"，是没有专门指标时的次优方案，可以用，但不如直接测。** 尤其它的贡献 1 明确声称 "enhancing the final prediction accuracy"，却全篇不报 RMSE。

**数据集：只在 DTLZ1–7 上做，m∈{3,5,10}，D 一律 10，21 实例。主实验是 69 实例（DTLZ+WFG+MaF）。** 论文没解释。可推断：用几何覆盖最全的 DTLZ 做组件诊断，WFG/MaF 留给 SOTA 对比以控计算量。**风险是：组件结论无法推广到 WFG 的欺骗/不可分/平坦和 MaF 的 badly-scaled。**

**注意 `DACKEA-S1` 是 1/10/10——探索阶段的贡献很弱**（10 个平局），论文没有深入解释。如果你的某个模块出现这种结果，要么补机理实验说明它在特定场景才起作用，要么诚实承认贡献有限。

#### RSEA（ASOC 2017）——机理级验证的经典设计，值得单独学

**变体（不是删模块，是几何对照）：**

| 变体 | 投影权重 | 类型 |
|---|---|---|
| `RSEA` | 固定原始权重 W1=(cos θ_i), W2=(sin θ_i), θ_i=2π(i−1)/m | 基准 |
| `RSEA*` | 权重元素的排列**随进化过程变化** | B |
| `RSEA^T` | 权重**反转但固定** | B |

**为什么这个设计比"有投影 vs 无投影"高明。** 它把问题拆成两个正交的追问：

1. **朝向重不重要？** 反转权重 = 把各目标在圆周上的锚点转 180°。结果 `RSEA` 与 `RSEA^T` 在所有实例上几乎全部 ≈：
   > "RSEA and RSEAT perform similarly with RSEAT on all the test instances, which means the fixed projection weight vectors will not effect the performance of RSEA."

   结论：**具体朝向不关键**，RadViz 式拥挤度近似旋转不变。
2. **时间一致性重不重要？** `RSEA*` 每代打乱排列 = 每代换一套 2D 坐标系，网格拥挤度跨代不可比。结果 `RSEA*` 显著更差：
   > "the variation in the permutation of the elements in the projection weight vectors will weaken the performance of RSEA, which may due to the fact that the diversity assess criterion provided by the radial projection may be messed with the varying projection weight vectors"

   结论：**关键不是"选哪套投影"，而是"投影必须代际固定"。**

**这组实验排除了"随便什么 2D 降维都一样"这个替代假说，并定位了失败模式是时间不一致而非角度选取。** 这是"机理级验证"的教科书案例——它回答的不是"模块有没有用"，而是"**模块的哪个性质在起作用**"。

**局限：** 只用 IGD，没把径向散点画出来；`RSEA*` 把"变"和"乱"绑在一起了（更干净的对照应该是"随机打乱一次后固定" vs "每代都打乱"）。21 实例，主实验有 15 目标但置换实验没做。

**没消融的：** 自适应网格划分（vs 拥挤距离等其它多样性机制）；惩罚参数 r 的敏感性（实验计划里写了 "the sensitivity of parameter r in RSEA is tested"，**正文找不到对应结果**——反面教材，别承诺了不做）。

### 16.4 机理级验证：怎么证明模块真的起了它声称的作用

这是你问的核心问题之一，也是七篇差距最大的一项。

**基本原则：模块声称什么，就用直接测那件事的指标去验证。** IGD 是终点指标，它证明"算法整体更好"，但不证明"这个模块通过它声称的机制起作用"。二者之间隔着一整条因果链。

| 模块声称的作用 | 该用的机理指标 | 谁做了 |
|---|---|---|
| 提升代理模型精度 | **Accuracy / Recall / Precision / F1**（分类/关系代理）或 **RMSE**（回归代理） | **只有 PCSAEA**。MP-SAMaOEA、DACKEA 都声称提精度但不报 |
| 改善多样性 | **Spacing**（纯多样性指标）、种群分布散点图、PF 覆盖可视化 | KHM-EF（Spacing）、R2AEA（PF 散点） |
| 加速收敛 | IGD/HV **随评价次数的曲线**（不是终点值） | 都有曲线图，但多在主实验节而非消融节 |
| 降低计算开销 | **组件级运行时间分解** + 横向秒数对比 | KHM-EF 最完整；PCSAEA、PIEA 有总时间 |
| 标签更平衡 / 边界更清晰 | 正负样本比例、**验证集准确率**、决策边界可视化 | PCSAEA（N/4 档位的 Average Accuracy） |
| 不确定性估计可靠 | **reliability diagram + ECE**（期望校准误差） | R2AEA 在主实验做了 ECE，**七篇都没把它用进消融** |
| 选择更有效 | 被选解的**存活率**、批次分散度、相对贪心参照的增益比 | 七篇都没做（**HPDC-MaOEA 的 §4.5 做了，这是你的相对优势**） |

**四种可用的机理验证手法，按硬度排序：**

**手法 1（最硬）：直接测模块的输出质量。** PCSAEA 测代理的 F1，KHM-EF 测 Spacing。指标与声称一一对应，无需推理链。

**手法 2：可视化模块产生的中间状态。** R2AEA 的 Fig. 9 把 RWO 阶段和 RMO 阶段的解分布画出来，一眼看出"挤在一小段"和"铺开"。**声称"两个模块产生不同行为"时，画出来比报数字有力。**

**手法 3：用问题特性当探针。** DACKEA 的做法——不直接测机理，而是论证"变体在具有特性 X 的问题上输，说明被删的模块正是处理 X 的"。例如 `DACKEA-GP` 在 DTLZ1/3（多峰）和 DTLZ5（退化）上差，推出"单全局模型在多峰和退化上不够"。**能用，但属于间接证据，最好和手法 1 或 2 搭配。**

**手法 4：设计正交对照排除替代假说。** RSEA 的 `RSEA*` vs `RSEA^T` 是范例——不问"有没有用"，而问"哪个性质在起作用"（朝向 vs 时间一致性）。**这是最能体现研究深度的一种，但需要你对自己的机制有清晰的理论理解。**

**一个反面模式，七篇里出现了三次：** 贡献列表写"提升代理精度"或"减轻误差累积"，消融只报 IGD 变好。审稿人的标准质疑是"IGD 变好可能是任何原因，你怎么知道是精度提升导致的"。**MP-SAMaOEA、DACKEA、PIEA 都有这个漏洞。** 补法很简单：留出一个验证集，报变体与完整算法的代理精度对比，一张小表就够。

### 16.5 数据集选择：可以挑，但理由必须写

**先说事实：七篇的消融全部在子集上做，没有例外。** 消融实例数与主实验实例数之比大致在 1/4 到 1/2 之间（PIEA 的建模对象那组 63/144，DACKEA 21/69，KHM-EF 21/48，MP-SAMaOEA 12/48）。所以**挑数据集本身完全正当，一区审稿人不会因为"没在全部实例上做消融"而拒稿。** 真正会被质疑的是两件事：挑的问题**覆盖不了被测能力**，以及**没说为什么挑这些**。

**唯一写了理由的是 MP-SAMaOEA 和 R2AEA。照 MP-SAMaOEA 的句式写就行**（16.3 已抄录）：把每个问题对应到一种被测能力。

**测试问题的特性对照表（写选题理由时按这张表挑）：**

| 问题 | 关键特性 | 适合验证什么 |
|---|---|---|
| DTLZ1 | 线性前沿 + 多模态 | 收敛能力（大量局部最优） |
| DTLZ2 | 标准凹球面前沿 | 多样性基线（最"干净"的问题） |
| DTLZ3 | 凹 + **强多模态** | 收敛能力（最常用的收敛压力测试） |
| DTLZ4 | **偏置分布**（解集中在少数目标上） | 多样性维持（易退化到边缘） |
| DTLZ5 / DTLZ6 | **退化前沿**（低维流形） | 多样性 + 参考向量/网格的适应性；DTLZ6 更难 |
| DTLZ7 | **不连续前沿**（多个断开区域） | 多样性 + 不规则 PF 适应性 |
| WFG1 | 平坦区 + 偏置 + 混合前沿 | 收敛（平坦区无梯度信息） |
| WFG2 | **不连续凸前沿** | 多样性 + 不规则 PF |
| WFG3 | **退化线性前沿** | 退化适应性（常与 DTLZ5 配对） |
| WFG4 | 凹 + 多模态 | 收敛 |
| WFG5 | **欺骗性**（deceptive） | 收敛（易被误导） |
| WFG6 | **不可分变量** | 变量耦合下的搜索能力 |
| WFG7 / WFG8 | 可分 + 依赖型偏置 | 偏置适应性 |
| WFG9 | **欺骗 + 不可分 + 偏置**（最难） | 综合难度 |
| MaF 系列 | 目标尺度差异大（badly scaled）、不规则 | 超多目标的归一化与尺度鲁棒性 |

**推荐的消融选题策略（4–7 个问题，覆盖四类能力）：**
- 收敛压力：DTLZ3 或 WFG4（多模态）
- 多样性基线：DTLZ2
- 偏置分布：DTLZ4 或 WFG8
- 不规则前沿：DTLZ7（不连续）+ DTLZ5 或 WFG3（退化）
- 如果做超多目标，再加 1 个 MaF

**两个要避免的做法：**
1. **别把主实验已暴露短板的题型从消融里拿掉**（KHM-EF 砍掉线性和退化，而它承认在这两类上弱）。审稿人对照两节会看出来。
2. **别用 "several problems" 一带而过**（PIEA 的做法）。一句话的理由成本极低，收益是堵住一个必然会被问的问题。

**目标数和维度也要交代清楚。** 消融通常只取主实验的一个或两个 m（DACKEA 取 3/5/10 全部，R2AEA 只取 m=3，MP-SAMaOEA 取 6/10/20 全部）。**如果你做超多目标，消融至少要覆盖一个大 m（如 m=20），否则"模块在超多目标下有效"这个声称没有直接支撑。**

### 16.6 因素缠绕：最容易被审稿人抓的错误

七篇里有三篇犯了同一个错误：**一个变体同时改了两件事，导致因果无法归属。**

| 论文 | 变体 | 同时改了 | 后果 |
|---|---|---|---|
| PIEA | `PIEA_o` | 建模对象（指标→逐目标）**+ 优化器**（自己的→NSGA-III） | 分不清是建模对象的功劳还是优化器的差异 |
| MP-SAMaOEA | `DEC` / `OBJ` | 删一个视角 **+ 选择算子**（非支配排序→轮盘赌） | 分不清是视角少了还是选择变差了 |
| R2AEA | `WO` vs `RWO` | 去掉回归 **+ 评价预算**（3000 vs 500） | 预算不对等，"样本效率"结论被混淆 |

**自查方法：把每个变体写成"完整算法 − X"或"完整算法（X → Y）"的形式。如果一个变体需要写成"完整算法 − X 且（Z → W）"，就是缠绕了。**

处理办法有三个，按优先级：
1. **拆成两步。** 先测"只改 X"，再测"只改 Z"，必要时加"同时改"作为第三个变体。这是最干净的。
2. **如果技术上无法解耦**（例如删掉分组模块后宿主算法的某个参数必须跟着变），就**明确声明这是"联合效应"而非单模块效应**，不要把它写成模块 X 的证据。R2AEA 就该这样写。
3. **补一个条件匹配的对照。** 把被牵连的因素在两边设成同一个值，即使这个值对某一方不是最优。

### 16.7 对照 HPDC-MaOEA：你现在的状况与补齐清单

你的论文有两个模块（混合 PBI 质量分组、双模式候选解选择），§4.3 是模块消融，§4.4 和 §4.5 分别是两个模块的专项分析，§4.6 是稳健性。**先说好的部分——你的机理级实验比七篇里的多数都强。**

**已经做得好的三件事：**

1. **§4.4 的机理指标是七篇里没人做到的层次。** Jaccard 重叠（0.5470 对方向分数、0.3996 对代表解余量）证明"混合组与任一单视角都不同"；Precision@25%（Hybrid 0.28698 vs 方向分数 0.27748 vs 代表解余量 0.27617）直接测"被选中的解是否留存到最终种群"。**这是手法 1（直接测模块输出质量），比 PCSAEA 的 F1 更贴合你的模块声称。**
2. **§4.5 的批次诊断指标（批量规模、批次分散度、后期存活率、池内增益比）七篇里无人做过。** 尤其"仅指标选择的存活率 0.761 很高，但 DTLZ7 上 IGD 32.509 很差"这个观察，说明你**主动指出了单一指标的不充分性**，这正是一区审稿人欣赏的严谨。
3. **你的表述已经很克制。** "这些测量描述的是所选批次，需要与终点 IGD 联合解读"、"它们并不能确立时变调度在终点性能上的优势"——这种区分"测到了什么"和"没测到什么"的写法，比七篇里那些"充分证明了有效性"的空话强。

**必须补齐的五件事，按紧急度排序：**

**第一，§4.3 的因素缠绕（最紧急）。** 你自己在正文里已经诚实指出了：
> "仅候选解模块的 $k=6$ 配置与完整方法在分组和 $k$ 两方面都不同，因此并不能在论文设置下隔离出'移除分组'的效应。"

这和 PIEA 的 `PIEA_o`、R2AEA 的 `WO` 是同一类问题。**你的 TODO 里已经写了要补 k=15,30 下的仅候选解配置——这件事必须做完，否则"移除分组"这一格是空的，两个模块只有一个能被单独归因。** 诚实声明能减轻但不能消除这个问题。

**第二，缺 2×2 交互矩阵。** 你的贡献把两个模块描述为"两个相互协调的设计组件"、"两个接口保持分离"。**这个"相互协调"的声称目前没有实验支撑**——你的 TODO 也承认"两个模块的联合交互作用仍待检验"。

照 KHM-EF 的矩阵补齐后应该是这样（k 固定为论文设置）：

| | 无候选解模块 | 有双模式候选解 |
|---|---|---|
| **无混合分组**（REMO 原生二元分组） | 基线 | 格子 A |
| **有混合 PBI 分组** | 格子 B | 完整 HPDC-MaOEA |

这个 2×2 能回答三个问题：分组的独立增益（B − 基线）、候选解的独立增益（A − 基线）、以及**交互项**（完整 − A − B + 基线）。**如果交互项为正，"相互协调"就有了硬证据；如果接近零，说明两个模块是可加的，那就把措辞从"相互协调"改成"作用于两个独立接口"** ——后者其实也是好结论，而且和你 §3.1 "两个接口保持分离"的表述更一致。

**第三，§4.4 和 §4.5 的选题理由没写。** §4.4 用了 DTLZ2、DTLZ4、DTLZ7、WFG3、WFG7 五个问题，§4.5 用了 DTLZ2、DTLZ7、WFG3、WFG7 四个。**目前正文只说"覆盖……的情形"，没说为什么是这五个。** 按 16.5 的对照表，你这五个的覆盖其实很合理，补一句就行：

> 这五个问题覆盖了分组机制需要应对的主要景观类型：DTLZ2 提供标准凹前沿作为基线，DTLZ4 的偏置分布考验正组是否退化到少数方向，DTLZ7 的不连续前沿与 WFG3 的退化前沿分别对应两类不规则 PF，WFG7 则引入依赖型偏置。

**注意 §4.4/§4.5 都没有 DTLZ3 或 WFG4 这类强多模态问题，也就是说你的机理实验缺一个"收敛压力"维度。** 建议在 §4.5 补一个 DTLZ3——你的候选解模块声称平衡开采与探索，多模态问题正是检验探索是否有效的场景。

**第四，缺关系代理自身的精度对比。** 你的混合分组声称"在构造正组时区分同一二元标签的解"，最终目的是让关系模型学到更有用的东西。**你测了正组构成（Jaccard）和保留率（Precision@25%），但没测关系模型本身的成对预测精度。** 你的算法里已经有留出集成对错误率 $e_r$（算法 1 第 10 行），**这个量是现成的，只要按变体分别记录并报一张表，就补上了七篇里只有 PCSAEA 做到的那一项。** 这是性价比最高的一项补充。

**第五，消融只在 m=10 和 m=20 做，缺 m=3 或 m=5 的对照。** 你的主实验有 m=3（表 4 显示 m=3 时胜率仅 34.4%，m=20 升到 62.5%）。**"模块在高目标数下更有效"是你最强的卖点之一，但消融没有低 m 的对照来支撑这个趋势。** 建议在 §4.3 补 m=3 或 m=5 一列，让"随 m 上升模块增益变大"这个趋势在消融层面也成立。这会显著强化"面向超多目标"的定位。

**关于 §4.6 稳健性：** 你已经做了核心控制量的稳健性，这对应七件套的"参数敏感性"。注意 PIEA 是在全部 144 实例上做九组参数网格，规模比你大。如果机时允许，把稳健性的实例数扩大是加分项，但优先级低于上面五项。

### 16.8 消融实验自查清单

写完消融后逐条核对：

**覆盖度**
- [ ] 贡献列表里每一条模块级贡献，都有至少一个对应变体（MP-SAMaOEA 的 k-means、KHM-EF 的贡献 1 都漏了）
- [ ] 如果两个模块被声称"协同/互补"，有 2×2 交互矩阵
- [ ] 消融覆盖了主实验的高 m 或高 D 端（不能只在最容易的规模上做）

**变体构造**
- [ ] 每个变体能写成"完整算法 − X"或"完整算法（X → Y）"，没有同时改两件事
- [ ] 至少有一半变体是**替换法**（换成 EI/LCB/均值融合/随机选等已有做法），不是纯删除
- [ ] 关键设计有多档位对照（配对规模、阈值、权重等）
- [ ] 所有变体的评价预算 FEmax 相同（R2AEA 的 3000 vs 500 是反面教材）
- [ ] 变体命名不复用同一记号表示不同含义（PCSAEA 的 `′/′′` 是反面教材）
- [ ] 变体名在正文里写全，不要只出现在图片化的表格里（KHM-EF 的反面教材）

**指标**
- [ ] 除 IGD/HV 外，至少有一项**机理级指标**直接对应模块声称的作用
- [ ] 声称"提升代理精度"→ 报了 Accuracy/F1/RMSE
- [ ] 声称"改善多样性"→ 报了 Spacing 或分布可视化
- [ ] 声称"降低开销"→ 报了组件级秒数
- [ ] 有统计检验（Wilcoxon rank-sum，α=0.05）和 W/T/L 统计

**数据集**
- [ ] 写明消融用了哪些问题、m 和 D 取值、共多少实例
- [ ] **写了为什么挑这些问题**，每个问题对应一种被测能力（照 MP-SAMaOEA 的句式）
- [ ] 覆盖收敛压力（多模态）、多样性基线、偏置、不规则前沿四类
- [ ] 没有把主实验已暴露短板的题型排除在外

**表述**
- [ ] 报数之后有归因，不只说"证明了有效性"
- [ ] 区分"测到了什么"和"没能确立什么"（HPDC-MaOEA 现有表述做得好）
- [ ] 某个变体差异不显著时（大量 ≈），主动解释而非略过（DACKEA-S1 的 1/10/10 没解释）
- [ ] 实验计划里承诺的每一项都真的做了（RSEA 承诺测 r 敏感性但正文没有）

### 16.9 消融实验的英文句式

**引出消融节**
> "To verify the effectiveness of each proposed component, ablation studies are conducted on ... In the following, [Variant-1] denotes the variant in which ... is replaced by ..., while [Variant-2] removes ..."

**说明选题理由（照抄 MP-SAMaOEA）**
> "These test functions are selected for the following reasons: [P1] is designed to evaluate the convergence of algorithms due to the multi-modal characteristics. [P2] is primarily designed to assess the diversity of algorithms, as its Pareto front is non-convex. Furthermore, [P3] and [P4] are also employed to evaluate the diversity of algorithms due to their bias and discontinuity characteristics."

**替换法的引出**
> "To further examine whether the performance gain originates from the proposed [module] rather than from the additional computational effort, [Variant] replaces it with the widely used [EI / LCB / mean-based fusion] while keeping all the other components unchanged."

**报结果**
> "[Algorithm] outperforms [V1], [V2], and [V3] on 18, 16, and 17 test instances, respectively."
> "the original [Algorithm] outperforms the two variants, hence the effectiveness of the proposed [module] can be evidenced."

**矩阵设计的关键结论（照抄 KHM-EF）**
> "when the [module-2] strategy is not applied, [module-1] alone outperforms the variants using [baseline-A] or [baseline-B] alone on all test problems."

**机理指标的引出（照抄 KHM-EF）**
> "The Spacing metric focuses on measuring the uniformity of solution distribution and is a purely diversity-oriented metric."
> "the diversity of the model is significantly improved after adopting the [module] strategy."

**正交对照排除替代假说（照抄 RSEA 的思路）**
> "[Variant-fixed] performs similarly with [Algorithm] on all the test instances, which means the specific choice of ... does not affect the performance, whereas [Variant-varying] weakens the performance, indicating that the ... must remain consistent across generations."

**档位选择的机理证据（照抄 PCSAEA）**
> "pairing the N/4 best solutions with the N/4 worst solutions leads to the highest test accuracy."

**诚实声明未能隔离的效应**
> "Since [factor] also changes together with [module] in this configuration, this comparison measures their joint effect rather than the isolated contribution of [module]."

**为不显著结果辩护**
> "The limited difference on [problem set] suggests that [module] mainly takes effect when ..., which is consistent with its design rationale."

---

## 十七、HPDC-MaOEA 投稿 SWEVO 的实验决策记录

> 针对"只做 DTLZ+WFG 主实验、消融按模块分别用 IGD、辅助实验挑单个数据集"这套方案的逐项裁定。依据是七篇对标文献的实际做法，不是抽象标准。

### 17.0 最紧急的一件事：独立运行次数

**这件事优先于本节其他所有内容。** 你的表 1 运行次数一列是空的，`实验数据核对.md` 第 38 条说明了原因——旧文件里的 30 次被标为 assumed，你没有当作事实写。这个处理是对的（不编数据），但**空着投出去会被直接退回**。

七篇的做法：PCSAEA 30 次、R2AEA 30 次、KHM-EF 30 次、RSEA 30 次、PIEA 21 次、MP-SAMaOEA 20 次、DACKEA 20 次。**没有一篇不写。**

要做的事：回查运行记录确认实际次数。**如果实际是 20 次以上，直接写上即可**（DACKEA 和 MP-SAMaOEA 都是 20 次，SWEVO 接受）。如果不足 20 次，补跑到 20。同时要确定的还有：检验方法、显著性阈值、多重比较策略（这三项第 38 条也列为待回查）。

顺带两个同类问题，都在 `实验数据核对.md` 里记着：
- **M10 DTLZ1 上 PCSAEA 与 KRVEA 的均值和标准差完全相同**（第 39 条）。这是明显的数据异常，审稿人一眼能看到。必须回查，要么修正，要么在表注说明原因。
- **平台与硬件、IGD 参考集规格未交代**（§4.1 的 TODO）。PIEA 写了 i7-9800X 3.80GHz / 16GB，KHM-EF 写了 AMD Ryzen 7 5800H / MATLAB R2024a。照着补一句就行。

### 17.1 主实验：要不要加 HV

**结论：要加，而且这是你目前最值得补的性能实验。**

**七篇的做法：** 五篇用 IGD + HV 或 IGD⁺ + HV（PCSAEA、R2AEA、MP-SAMaOEA、KHM-EF、RSEA），DACKEA 基准用 IGD、真实问题用 HV，**只有 PIEA 单用 IGD⁺**。

**为什么 PIEA 能只用一个指标：** 它有三样补偿——144 个实例的规模、average ranking 跨问题排序、以及在全部实例上做的九组参数网格。你的规模是 16 问题 × 2 个主设置，没有这个体量。

**为什么对你的论文 HV 特别关键（这是主要理由，不是"因为别人都做"）：**

你的两个模块声称的都是**多样性和平衡**相关的作用——混合分组要避免正组退化到少数方向，双模式选择要平衡探索与开采。而 IGD 把收敛性和多样性混在一个数里，且在 FE=300、M=20 这种远未收敛的场景下，**IGD 的变化主要由收敛性主导，多样性的改善会被淹没**。HV 对分布均匀性和覆盖范围更敏感。

更具体地说：你 §4.5 已经测了批次分散度（仅探索 2.031、仅指标 1.031、双模式 1.479），**这是决策空间的分散度。HV 能提供目标空间的对应证据**，两者配起来才完整。现在的情况是你测了"批次更分散"，但没有目标空间指标证明这个分散转化成了更好的前沿覆盖。

**实施注意（M=20 上 HV 有技术坑）：**
- **精确 HV 在 M=20 不可行**（计算量随 M 指数增长）。用 Monte Carlo 近似，PlatEMO 的 HV 实现支持。**必须在正文写明采样点数**，否则结果不可复现。
- 参考点取法要写清楚（常见做法是取 nadir point 或其 1.1 倍），并说明是否做了归一化。
- **M=20 的 HV 值会非常小甚至大量为 0**（前沿离参考点太远时）。如果出现大面积 0，说明参考点选得不合适，要调整而不是照报。

**附带建议：把 IGD 换成 IGD⁺。** IGD 不是 Pareto compliant，IGD⁺ 是。七篇里 MP-SAMaOEA、KHM-EF、PIEA 都用 IGD⁺。你的路由研究已经有 M=20 的 IGD⁺ 结果，说明流程跑得通。**参考集不变，只是距离计算方式不同，改动成本极低，但堵住了一个"指标不 compliant"的质疑。** 如果重跑主表成本太高，至少在正文说明"IGD 在本领域广泛使用，IGD⁺ 结果见附录/补充材料"。

### 17.2 MaF 能不能不测

**结论：可以不测，有充分先例。补一句说明即可。**

**七篇的实际情况：** 主实验用满 DTLZ+WFG+MaF 三套的**只有 DACKEA 一篇**。MP-SAMaOEA、KHM-EF、PIEA、R2AEA **四篇都只用 DTLZ+WFG**，分别发在 SWEVO 2026、SWEVO 2026、Information Sciences 2024、SWEVO 2025。PCSAEA 用了 MaF1–5、RSEA 用了 MaF2–5，但都是补充而非主体。

**所以"只做 DTLZ+WFG"完全站得住，尤其你要投 SWEVO——四篇先例里三篇在 SWEVO。**

**几何覆盖上你也不亏。** WFG1–9 已经覆盖平坦区+偏置（WFG1）、不连续（WFG2）、退化线性（WFG3）、多模态（WFG4）、欺骗（WFG5）、不可分（WFG6）、依赖型偏置（WFG7/8）、综合难度（WFG9）；DTLZ1–7 覆盖线性、凹、强多模态、偏置、退化、不连续。**MaF 的独特价值主要是目标尺度差异大（badly scaled）**，这一项 DTLZ/WFG 确实覆盖不到。

**建议写法（放在 §4.1）：**
> "The DTLZ and WFG suites are adopted as they jointly cover the main PF geometries relevant to many-objective optimization, including linear, concave, degenerate, disconnected, biased, deceptive, and nonseparable landscapes."

**如果想预防审稿人追问，最低成本的加固是补 MaF1 和 MaF7 两个**（前者尺度差异、后者不连续+多目标退化），而不是补全 MaF1–7。两个问题的增量机时很小，但能让"三套齐全"这句话成立。优先级低于 HV。

### 17.3 真实工程实验

**结论：可以后补，不是投 SWEVO 的阻塞项。但如果要补，选 m 大的问题，别选 m=5 的。**

**七篇里 KHM-EF 和 PIEA 都没有真实案例**，分别发在 SWEVO 2026 和 Information Sciences 2024。所以缺这一项不阻塞。

**但对你的论文它的收益比对别人大**，因为你的定位是超多目标（M=10, 20），而真实工程问题里 m 大的现成案例正好能支撑这个定位：

| 问题 | m | D | 来源 | 适配度 |
|---|---|---|---|---|
| **CSI**（车身结构设计） | **13** | 7 | PCSAEA 用过，PlatEMO 内置 | 最适配，m=13 直接支撑超多目标 |
| **WRM**（水资源管理） | **12** | 3 | PCSAEA 用过 | 适配，但 D 太小 |
| **GAA**（通用飞机设计） | **11** | 27 | PCSAEA 用过 | 适配，m 和 D 都合适 |
| CCD（汽车座舱） | 9 | 11 | DACKEA 用过 | 可用 |
| 水资源规划 | 5 | 3 | MP-SAMaOEA 用过 | **m=5 太小，撑不起你的超多目标定位** |

**推荐 CSI（m=13）+ GAA（m=11）两个**，都在 PlatEMO 里有实现，且 PCSAEA 已经用过（可以引用它说明问题设置，省去自己交代细节）。**PF 未知，所以用 HV**——这又是一个必须先把 HV 流程跑通的理由。

**一个坑：** PCSAEA 把每个约束当作一个目标来处理（所以 CSI 是 13 目标）。R2AEA 的做法是明确声明 "all algorithms are assessed without considering constraints to maintain fairness"。**两种都可以，但必须写明你用的是哪种**，否则目标数对不上。

### 17.4 goodGroupPrecision 只用一个数据集

**结论：单数据集有先例，但你现在已有五个问题的结果，不要缩减。真正的问题不在数量，在于这个指标目前的说服力偏弱。**

**先说数量：单数据集完全有先例。** KHM-EF 的 Spacing 只在 DTLZ2 上画，DACKEA 的 τ 敏感性只用 3 目标 DTLZ1，R2AEA 的 PF 散点图只用两个问题。**机理实验挑 1–2 个问题是这个领域的常规做法，不会因此被质疑。**

**但你的 §4.4 已经做了五个问题（DTLZ2/4/7 + WFG3/7）× 2 个目标数 × 25 次运行 = 250 条轨迹，还带 Holm 校正。这个规模超过上述任何一篇。缩减它是自损。**

**真正需要注意的是说服力问题。** 你现在的 Precision@25% 数字是 Hybrid 0.28698、方向分数 0.27748、代表解余量 0.27617——**三者差距不到 1 个百分点**。而且你自己诚实记录了"要求 Hybrid 精度同时超过两个单视角的更严格检验只在 40 个单元格中的 1 个通过"。

审稿人看到这组数字，合理的质疑是：**"这个差距是否足以支撑'混合分组更能预测好解'这个声称？"** 你现有的表述已经很克制（"一个不大的整体保留优势"），这是对的，但克制的表述配上微弱的数字，模块贡献的证据链就偏软。

**建议的加固方向（按性价比排序）：**

**第一，补关系代理的成对错误率 $e_r$ 对比（强烈建议，成本极低）。** 这是本节最重要的建议。你的算法 1 第 10 行已经记录留出集成对错误率 $e_r$，**这个量是现成的**。按变体分别记录（原生二元分组 vs 混合分组），报一张小表。

为什么这比 Precision@25% 更有力：**你的模块 1 声称的是"让关系模型学到更有用的东西"，$e_r$ 直接测的就是关系模型学得好不好。** Precision@25% 测的是"被选中的解是否留存到最终种群"，这是下游结果，中间还隔着关系模型训练、候选生成、环境选择好几个环节。因果链更短的证据更硬。

这同时补上了七篇里只有 PCSAEA 做到的那一项（代理自身精度对比）。**PCSAEA 正是靠这一项在机理验证上高出一档。**

**第二，如果要挑单个数据集做补充分析，选 DTLZ2 在 M=20，并按机制而非结果给理由。**

措辞很重要。**不要写"选择表现较好的数据集"**——这会被读成 cherry-picking。要写成：

> "DTLZ2 is selected for this analysis because its concave PF provides a clean geometry in which the PBI value is well defined and monotonically related to solution quality, so that the effect of the grouping signal is not confounded by convergence failure. The same analysis on [第二个问题] is reported to confirm that the observation is not specific to a regular PF."

**理由要落在"这个问题能让机制的效应可被观察"，而不是"这个问题上我赢了"。** 如果只用一个规则前沿的问题，最好再加一个不规则的（DTLZ7）作为对照，这样"不是只在规则 PF 上成立"就有了支撑——你现有的五问题设计其实已经满足这一点。

**第三，§4.4/§4.5 缺一个强多模态问题。** 你用的 DTLZ2/4/7 + WFG3/7 覆盖了标准凹、偏置、不连续、退化、依赖型偏置，**但没有 DTLZ3 或 WFG4 这类强多模态问题，也就是缺"收敛压力"这个维度**。你的双模式选择声称平衡开采与探索，多模态问题正是检验探索是否真的有效的场景。补一个 DTLZ3 到 §4.5 的探针里，优先级中等。

### 17.5 参考解个数 k 的实验

**结论：必做，但要区分两个不同目的，你现在做对了一半。**

你把 REMO 的默认 $k=6$ 改成 $1.5M$（M=10 时 15、M=20 时 30）。这件事需要两类实验，性质完全不同：

**目的 A：敏感性——为什么选 1.5M。** 这类可以只用 1–2 个问题扫几个档位（$k \in \{6, M, 1.5M, 2M\}$）。**有先例，成本低。**

**目的 B：公平性——你的增益是不是只来自更大的 k。** **这类必须在完整消融集上做，不能只挑一个问题。** 因为 $k$ 是**宿主算法的参数**，不是你的模块。如果你只报"我的方法（k=15）优于 REMO（k=6）"，审稿人的第一反应就是"**把 REMO 的 k 也调到 15，是不是就追上了？**"

**你的 §4.3 已经做对了这件事**——表 5 和表 6 都包含 REMO 在 $k=6$ 和 $k=15/30$ 两种配置。**这是本节要表扬的地方，很多论文会漏掉。**

**而且你的数据里躺着一个对你非常有利的发现，正文完全没有点出来。** 把表 5 和表 6 的 W/T/L 行按 k 分组看（数字是 HPDC 视角的胜/平/负，所以对手的数字越差说明对手越弱）：

| 配置 | M=10（表 5） | M=20（表 6） | 放大 k 的效果 |
|---|---|---|---|
| REMO, $k=6$ | 9/5/2（最小均值 2 次） | 7/6/2（最小均值 1 次） | — |
| REMO, $k=15/30$ | **10/5/1（最小均值 0 次）** | **10/4/1（最小均值 0 次）** | **变差** |
| REMO+HPC, $k=6$ | 8/4/4 | 7/4/4 | — |
| REMO+HPC, $k=15/30$ | **3/12/1** | **3/11/1** | **明显变好** |

**两个方向相反，这个不对称就是你要的证据：单纯放大 $k$ 会让纯 REMO 变差（最小均值次数从 2 和 1 双双降到 0），但配上混合分组后放大 $k$ 反而带来明显收益（输给 HPDC 的次数从 4 降到 1，平局从 4 升到 12/11）。**

**换句话说，更大的代表解集只有在混合分组的配合下才有用。这直接反驳了"你的增益只来自更大的 k"这条质疑。** 建议在 §4.3 明确写出来：

> "The effect of enlarging $k$ is not uniform across configurations. For the plain host algorithm, REMO with $k=1.5M$ obtains the lowest mean IGD on no instance, whereas REMO with $k=6$ does so on 2 ($M=10$) and 1 ($M=20$) instances, indicating that a larger representative set alone does not improve the host. In contrast, when combined with the hybrid grouping, enlarging $k$ reduces the number of instances on which the configuration is outperformed by HPDC-MaOEA from 4 to 1 at both objective numbers. The larger representative set therefore becomes beneficial only in the presence of the hybrid grouping, which attributes the observed gain to the grouping mechanism rather than to the increased number of representatives."

**这段话零成本（数据已有），但挡掉的是审稿人几乎必然会提的一条意见。而且它还顺带给"为什么要把 k 从 6 改成 1.5M"提供了正面理由**——不是拍脑袋调参，而是这个改动只在你的机制下才生效。

### 17.6 p_mix 的四臂实验够不够，其他超参数能不能不分析

**结论：四臂设计够了，而且比七篇里的多档位实验更有说服力。其他超参数基本可以不分析，但 $r_g$ 是例外。**

**先说四臂为什么好。** 你做的是：全指标、全探索、前期探索后期开采（时变调度）、固定 0.5。这**不是简单的参数扫描，而是四种质性不同的策略**：

- 两个极端（全指标、全探索）等价于删除法，回答"两个模式是否都必要"
- **时变调度是一个真正的替代方案**，回答"为什么不用更"聪明"的调度"
- 固定 0.5 是你的选择

对比 R2AEA 的 $tr \in \{0, 0.25, 0.5, 0.75, 1\}$——那只是在两端之间线性插值，**没有"时变调度"这种质性不同的对照**。你的设计更强。

**最值得强调的一点：如果固定 0.5 打败了时变调度，这是一个反直觉的结果，要明确写出来。** 这个领域的默认假设是"自适应/时变优于固定"（RSEA 的 $r=1-(t/t_{\max})^2$、PCSAEA 的 $r=FE/FE_{\max}$、以及**你自己模块 1 的 $\alpha_t=1-t$** 都是时变的）。**在模块 1 用时变、模块 2 用固定，这个不对称本身需要解释，而你的四臂实验正好提供了解释的依据。**

建议写法：
> "Although a time-varying schedule is a natural choice and is indeed adopted for the grouping weight $\alpha_t$, the candidate selection mode benefits from a fixed probability. A schedule that explores early and exploits late performs worse than $p_{\mathrm{mix}}=0.5$, indicating that the two selection criteria remain complementary throughout the search rather than being useful only in specific phases."

**其他超参数不分析，有先例吗？有。** MP-SAMaOEA **完全没有参数敏感性分析**，照样发 SWEVO 2026。DACKEA 只测了一个参数 $\tau$，且只在 3 目标 DTLZ1 上。KHM-EF 测了三个（$w_{\max}, \tau_0, \eta$），只在两个问题上。**PIEA 在 144 个实例上做九组网格是异常值，不是基线。**

**所以"只分析 $p_{\mathrm{mix}}$ 和 $k$"是可以的，但有一个例外需要处理：$r_g$（正组比例）。**

**为什么 $r_g$ 特殊：它出现在你的理论条件里。** 你的命题 1 第 (ii) 条依赖 $\alpha_t \leq 1/2$，而 §3.2.3 明确写了"在 $r_g < 1/2$ 下，非正组提供一个宽阔的对照集"。**只要一个参数出现在命题或理论条件中，审稿人就会问边界情况。** 建议在 1–2 个问题上扫 $r_g \in \{0.15, 0.25, 0.35\}$ 三档，一张小图或小表即可。成本很低，但堵住的是一个必然会被问的问题。

**另一个零成本的防御措施：加一张完整的参数表。** 列出所有参数、取值、以及**哪些是从 REMO/RSEA 原样继承的**。

| 参数 | 取值 | 来源 |
|---|---|---|
| $k$（代表解个数） | $1.5M$ | **本文修改**（REMO 原为 6），见 §4.x |
| $p_{\mathrm{mix}}$ | 0.5 | **本文引入**，见 §4.x |
| $r_g$（正组比例） | — | 本文引入 |
| $\theta$（PBI 惩罚） | — | 沿用 MOEA/D |
| $\delta$（类平衡） | 有界二分自适应 | 沿用 REMO |
| $n_{\max}, g_{\max}, n_{\min}$ | — | 本文引入 / 沿用 REMO |
| 环境选择 | 径向网格 | 沿用 RSEA |

**这张表的作用是让"本文引入的参数只有 N 个"变成一个可核对的事实**，避免审稿人产生"这个算法参数太多"的印象。R2AEA 用类似的表（Surrogate Type / Model / Strategy for Large D）来交代对比算法，效果很好。

### 17.7 还需要补什么：按投 SWEVO 的紧急度排序

**必做（不补会被要求 major revision 或直接退回）**

| # | 项目 | 依据 | 成本 |
|---|---|---|---|
| 1 | **独立运行次数 + 检验协议** | 七篇全部明确交代，你目前空着 | 回查记录，或补跑到 20 次 |
| 2 | **HV 指标** | 七篇里六篇有第二个指标；你的模块声称多样性收益，IGD 测不出来 | 中（M=20 需 Monte Carlo） |
| 3 | **模块运行时间占比** | 你 §3.5 自己算出模块 2 的 $O(N_cND)$ 是主导项，声称了开销可控就必须实测 | 低（跑几次记时间） |
| 4 | **平台/硬件/IGD 参考集规格** | §4.1 的 TODO，七篇都写 | 极低 |
| 5 | **M10 DTLZ1 数据异常** | PCSAEA 与 KRVEA 数值完全相同，审稿人一眼可见 | 回查 |

**强烈建议（决定评审印象）**

| # | 项目 | 依据 | 成本 |
|---|---|---|---|
| 6 | **关系代理成对错误率 $e_r$ 按变体对比** | 补上七篇里只有 PCSAEA 做到的机理验证；$e_r$ 是现成量 | **极低，性价比最高** |
| 7 | **2×2 交互矩阵** | 贡献声称两模块"相互协调"，目前无证据；你的 TODO 也承认 | 中（补 $k=15/30$ 的仅候选解配置） |
| 8 | **点出"单纯放大 k 无益"** | 数据已有，只需在正文写一句，见 17.5 | **零成本** |
| 9 | **收敛曲线（IGD vs FE）** | 七篇都有；FE=300 的预算下曲线能直观说明早期效率 | 低 |
| 10 | **WFG3 全面落后的机理解释** | 你诚实报告了七个对比算法在 WFG3 上全部更优，但没解释原因 | 低（分析+写作） |

关于第 10 项：**WFG3 是退化线性前沿**，而你的模块 1 在超多目标下用非支配解的径向单位向量作自适应方向集（式 2）。**退化前沿上非支配解集中在低维流形，径向方向会高度重合，方向集退化**——这很可能就是原因。KHM-EF 的做法值得学：主动承认在线性和退化前沿上较弱，给机理解释，再引 no-free-lunch。**你已经诚实报告了数据，补一段机理解释就能把劣势转成"对方法边界的清晰认识"。**

**可选（加分但不阻塞）**

| # | 项目 | 依据 |
|---|---|---|
| 11 | **Friedman test + average ranking** | 七篇全都没做 Friedman，做了是差异化项 |
| 12 | **$r_g$ 敏感性（3 档 × 1–2 问题）** | 它在命题 1 的条件里，见 17.6 |
| 13 | **真实工程案例（CSI m=13 / GAA m=11）** | 见 17.3 |
| 14 | **§4.5 补一个 DTLZ3** | 补上"收敛压力"维度，见 17.4 |
| 15 | **消融补 m=3 或 m=5 一列** | 你主表显示胜率随 M 上升（34.4% → 62.5%），消融层面也成立会强化超多目标定位 |
| 16 | **MaF1 + MaF7** | 让"三套齐全"成立，见 17.2 |

### 17.8 投稿路线

**首投 SWEVO 是对的。** 七篇里五篇在 SWEVO（MP-SAMaOEA、KHM-EF、DACKEA、PCSAEA、R2AEA），编辑和审稿人熟悉 REMO / 关系学习 / 径向选择这条谱系，你引用他们的工作也顺理成章。**你的对比算法里 REMO、PIEA、R2AEA、PCSAEA 全是这个圈子的工作，这是优势。**

**被拒后的转投顺序：**
1. **Information Sciences**（一区）——PIEA 发在这里，接受面较宽，重视实验充分性。如果 SWEVO 的拒稿意见是"创新性不足"，这里仍有机会；如果是"实验不充分"，先补完再投。
2. **Applied Soft Computing**（二区）——RSEA 发在这里，审稿较快。
3. **Expert Systems with Applications / Knowledge-Based Systems**（二区）——需要把应用价值写得更重，真实工程案例就变成必需项而非可选项。

**一个策略建议：** 必做的 5 项 + 强烈建议的 6、7、8 三项补齐后再投 SWEVO。第 6 和第 8 项成本极低（一个是现成量、一个是写一句话），第 7 项是你的贡献声称所必需。**这三项补上后，你的机理验证层次会超过七篇里的大多数**——因为 §4.4/§4.5 那套指标本来就是别人没做的，只是需要 $e_r$ 把因果链补短。





