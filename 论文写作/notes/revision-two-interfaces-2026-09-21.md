# PAQC / CDIS 双接口叙事调整（2026-09-21）

- PAQC：训练监督构造；CDIS：昂贵评估分配。同步摘要写作提示、引言、贡献、方法总览、分支术语/公式、框架图注、消融解释和结论。
- 模式记号从 exp 改为 amb；保留参考方向向量 `\vw`。未修改算法或实验数据。图 1 沿用现有源脚本重建，修复嵌入 PDF 仍显示 batch distance 的旧图问题；图 2 不变。图 3 图注移除开发版本名。
- 正文统一使用 PACDIS。实现身份仍为 `REMO_UniformMix_Pruned_Weighted_Lambdat030_NoBatchDist`，保留在 TeX 注释中用于追溯。
- 删除正文的 batch-distance 开发历史；指标定义处保留 SDE 和 PIEA 来源引用，避免把整个指标分支归为 Following PIEA。
- 现有消融标签在 32 个设置中支持 Full 优于去 CDIS 12 次、去 PAQC 10 次，反向为 3 和 2 次。该计数不证明效应大小相等或统计交互。原统计与来源核验 TODO 保留。
- CDIS 候选质量分析仅新增待完成方案，不表示已经执行。当前版本 p_mix 敏感性同样待完成。

## 移出正文的历史边界

旧 Lambdat030 GGP 使用 DTLZ2/4/5、M=10/20、FE=500、150 runs，是独立历史轨迹研究。选择策略改变会改变后续存活结果，不能仅因 PAQC 相同就当作当前 PACDIS 的机制证据。
旧五档 p_mix 敏感性使用带 batch-distance 的 Lambdat030 和 IGD，不能用于当前配置的 IGD+ 敏感性结论。本次不重标记、不搬用旧实验。

## 验证

两遍 pdflatex 编译得到 14 页；没有未定义引用或 Overfull 警告；字体全部嵌入。存在既有作者、摘要、相关工作和实验核验 TODO，稿件仍不是投稿终稿。检查新增实验小节、消融表与结论版面。
改动行机械检查：破折号 0、修辞强化语 0、空洞开头 0、冗词 0；术语检测命中用户指定的 oriented 模式名，wise 检测命中技术术语；否定句用于限定证据或描述缺失信息，予以保留。

独立审读发现并修复了框架图旧 PDF 未随源脚本更新的问题；重新导出 PDF/SVG/PNG 和相应清单。样式扫描的配置重复句与 finalizing 命中已删除，最终仅保留用户指定模式名及必要证据边界。
