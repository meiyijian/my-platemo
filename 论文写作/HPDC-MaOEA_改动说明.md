# HPDC-MaOEA 当前版本改动说明

## 总体处理

本轮修改按“发布会原则”统一了论文叙事：主文围绕“双粒度 PBI 监督”和“有界双模式候选分配”两项优势展开；传统方法的能力缺口保留为问题动机，针对本文方法的自我辩护、实现过程和失败式说明从投稿文本中移除。

## 三项直接要求

1. 已删除整张参数表及正文、算法中对该表的全部引用。正文只保留理解机制所必需的符号，不再集中罗列默认值、网络宽度、训练划分、遗传算子等实现设置。
2. 已从全文删除所有聚类、聚类验证、随机流兼容和“可移除开销”表述。方向集现在直接定义为随当前非支配分布变化的径向单位方向。
3. 已将实验设置中的“Parameter Sensitivity”改为“Robustness to Core Controls”，要求主文只展示少量贡献定义型控制量，其余固定实现设置进入复现材料。

## 防御性写作逐处处理

| 位置 | 原表述的问题 | 本轮改法 |
|---|---|---|
| 文件头注释 | 记录旧稿、Git 提交、源码核对和章节占位，属于工作汇报 | 只保留两项贡献和版式说明 |
| 摘要占位 | “low overlap”“only one view”容易让优势呈负面形式 | 改为“两种视图提供双向独有真阳性”“81.5% 由其中一种视图独特贡献” |
| 引言占位 | 引用内部笔记和写作过程 | 改为问题→缺口→两项机制→最强证据的发布会开场 |
| 相关工作定位 | “rather than a new scalarizing function”先替审稿人缩小创新 | 正面定位为“dual-granularity PBI supervision”，明确细粒度排序和粗粒度边界 |
| 方法总览 | “其余组件均未修改”突出没有做什么 | 改为成熟组件提供共同宿主，两项贡献控制监督与预算分配两个关键接口 |
| Hybrid PBI 开头 | “not statistical independence”主动防守 | 直接定义为细、粗两种分辨率协同形成的双粒度监督 |
| 连续方向构造 | 大段解释聚类退化、数值验证和实现兼容 | 全部删除，直接给出种群自适应径向方向定义 |
| 连续偏好 Remark | “不提供多样性压力”“大部分个体退化为收敛度量”等自我削弱 | 改为“收敛敏感的细粒度排序”，并主动说明它与代表解边界的分工互补 |
| 代表解定义 | “不是真实 Pareto 前沿样本”属于先行辩护 | 改为“动态、种群驱动的参考点”，突出随目标维数提升区域覆盖 |
| 平衡变量 $\delta$ | 展开负值、容差及“不保证平衡”的实现细节 | 压缩为继承自 REMO 的有界二分调节，职责是维持两侧有效占用 |
| 混合分组 | “不再引入其他组”没有贡献信息 | 改为紧凑正组聚焦高优先级样本，较大对照组提供充分对比 |
| 混合分数用途 | “never optimised”“only use”是否定式定义 | 改为监督接口，直接连接质量评分、分组和关系样本构造 |
| 混合分数后期行为 | “degenerates into a tie-breaker”削弱连续信号 | 改为粗粒度类别主导、连续分数细化类内顺序 |
| Realised weight Remark | 大量解释连续信号只在早期起主导作用，并列举额外数值 | 整段删除；保留“从细到粗的预算感知粒度调度”这一核心记忆点 |
| 关系学习接口 | “only interface changed”突出改动少 | 改为向成熟 REMO 关系模型提供重新设计的混合分组 |
| 网络与异常处理 | 网络宽度、划分比例、空样本和非有限值回退占用主叙事 | 删除实现型细节，只保留 held-out error 对可靠性感知采集的作用 |
| 关系分数解释 | “heuristic”“not a Pareto win rate”主动降低读者预期 | 改为用于候选排序的组相对聚合偏好 |
| 双模式总述 | “we do not claim optimal balance”直接自我否定 | 改为两种互补准则在显式批量上界内分配昂贵评价 |
| 两种模式的候选池 | “not mode-independent”“confound”把重点放到混杂 | 改为模式特定聚合塑造准则特定候选池；共享冻结池实验负责隔离排序贡献 |
| 探索模式的 $U$ | “not calibrated variance or epistemic uncertainty”围绕不是什么展开 | 正面定义为当前候选池上的 softmax concentration 排序信号 |
| 可靠性权重 | “poorly performing classifier cannot steer...”带失败语气 | 改为可靠性因子随 held-out error 自动抑制歧义引导 |
| 贪婪批选择 | “does not promote objective spread”替审稿人总结缺点 | 改为质量优先、决策空间距离负责分散昂贵评价批次 |
| 指标模式 | “unavailable / not finite”回退说明抢占方法主线 | 主文删除，保留算法的正常工作路径 |
| 模式切换 | “not ensemble / not adaptive / never fused”连续否定 | 改为每次迭代使用一个完整准则，保留两种选择语义并给予相同先验暴露 |
| 批量边界 | “does not act”“never stalls”等防故障解释 | 改为分位排序与批量上界共同构成稳定的排名—预算接口 |
| 复杂度分析 | 把无效聚类称为高常数、可移除开销 | 相关内容全部删除，只报告论文所定义机制的复杂度 |
| 机制实验占位 | “non-redundancy”等审计式措辞偏弱 | 改为 Complementarity、Joint utilisation、Scheduling 三个肯定式论证职责 |
| 参数实验 | 一次列出多项敏感参数，强化“超参数很多”的印象 | 改为少量核心控制量的稳健性，评价质量、批量规模和预算使用稳定性 |
| 结论占位 | “Reserved memory sentence”属于内部写作提示 | 改为强化两项贡献记忆点，并要求以最强算法级结果收束 |

## 有意保留的传统方法问题表述

以下内容属于“问题与缺口”，不是对本文创新的自我削弱，因此保留：二元 PBI 分组压缩类内顺序；固定绝对阈值会把分数分布变化传递到每轮评价数量；单一关系分数只表达组相对位置。它们共同负责证明本文两项机制为什么必要。

## 检查结果

- 全文已无聚类相关词、已无参数表标签或引用。
- 删除两个防御性 Remark 后，交叉引用仍完整，无悬空标签。
- 已通过两轮 `pdflatex` 编译；当前仅保留原稿已有的 TODO 占位和欠满行提示，不存在编译错误。
