# 图一流程图修订（2026-09-09）

依据当前 `HPDC-MaOEA.tex` 方法部分和算法 1、3，将原模块关系图替换为紧凑流程图。参考用户提供的三张图的流程框、判断菱形和循环结构，自行重新绘制。

- 左侧：初始化、预算判断、PAQC 分组、两类模型训练、CDIS、真实评估及环境选择回路。
- 右侧：CDIS 展开，包括预先抽取模式、关系引导候选生成回路、候选去重、指标／探索分支及批量限制。
- 三个菱形均提供 Yes/No；`c` 是累计生成候选数，不是代数。初始预算判断覆盖初始设计已耗尽预算的情况。
- 保留代表解参与交配、分位数筛选、集合不足时补齐、批量上限及剩余预算约束；公式与空候选池等细节留在正文和伪代码。
- 点线仅表示 CDIS 节点的展开关系，不代表额外执行路径。

交付：`fig_framework.png`（4251 × 3708，600 dpi）、`fig_framework.svg`（可编辑文字及矢量图形）、`fig_framework.pdf`（论文使用的矢量图）。独立重建脚本为 `build_framework_flowchart.py`，总构图脚本也已接入新版。

图注和正文引导句已同步修改。双遍编译后图一位于第 3 页；检查图内文字边界及实际论文页面。原稿仍存在未定义的实验章节引用（`sec:exp:paqc`、`sec:exp:candidate`、`sec:exp:sensitivity`）和另一幅原有 PDF 的版本提示；本次没有修改这些内容。

历史图件合集和其他图件不属于本次交付；查看本次独立图一或当前编译论文。
