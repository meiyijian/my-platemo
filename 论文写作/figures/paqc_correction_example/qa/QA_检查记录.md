# 交付检查记录

检查日期：2026-09-08。

结论：两版独立图件、双语图注与 LaTeX 插图代码已完成；以下核验通过。

## 数值与科学内容

- MATLAB R2023a 执行保存的实际分类函数和连续评分函数，输出 `MATLAB_SOURCE_CHECK_PASS`。
- 方向数为 9；A、B、全部八个解的标签、得分和排序均已计算并存档。
- 前两名 A、R 与第三名 B 的截止分差为 0.0222219135801，不依赖并列处理。
- 独立重构连续得分与 MATLAB 函数输出误差小于 1e-12。
- A 属于已知前沿，B 的前沿距离为 2.5；前沿未参与算法评分。
- 保留当前代码对 R 的边界数值处理；取消余弦微调的控制仍得到 A 修正、B 移出。说明文件明确 R 的标签变化不作为机制优势。
- t=0.5、0.75、1 时 A 均不入选；说明明确 A 与 B 发生交叉的临界 t 约为 0.217392，未夸大早期适用区间。
- 固定参考解是教学输入，未宣称运行 RefSelect 或完成完整算法实验。

## 图件

- 中文、英文 PDF 均为一页，实际尺寸为 180 mm × 95 mm。
- PDF 全部为矢量线条和嵌入字体，无嵌入位图；SVG 保留文本。
- PNG 为 4251 × 2244 像素，600 dpi，白色不透明背景。
- 普通标注为 7.5–9.2 pt；实际 PDF 最小字形 5.74 pt 来自数学下标，非正文标注。
- 自动检查未发现越界文字或替换字符。
- 左右主图、左右放大窗分别共享全部坐标与比例；八个解均在主图中，无点位抖动或隐藏。
- 已分别用 PyMuPDF 与 Poppler 渲染检查中文、英文 PDF，确认文本、指标函数、表格、放大窗连线及点位显示正常。
- 已检查灰度版本：A/B 标签、实心/空心状态、参考解菱形、前沿实线、边界虚线与方向点线仍可辨。

## 插图编译

- 英文插图在 elsarticle 双栏模板中连续编译两次通过；日志无未解决引用、Overfull、Underfull 或缺字告警。
- 中文插图在 ctexart/XeLaTeX 双栏模板中连续编译两次通过；无排版溢出、未解决引用或缺字。
- 中文环境存在已有的 CTeX/LaTeX 内核版本提示，MiKTeX 另提示尚未检查更新；两者未阻止生成或影响本次渲染检查，未为消除提示升级系统环境。
- 已查看两份带图注的编译 PDF，图与图注均完整。原论文主文件未参与本次修改。

这是本次构造图及排版的核验，不等同于期刊合规认证或算法有效性实验。
