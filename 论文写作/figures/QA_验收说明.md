# 论文配图验收记录

检查日期：2026-09-06。检查对象为六张独立图及插入五张图后的英文论文。

## 来源与数值

1. 核对当前主类、私有分组函数、二值 PBI、模式选择、候选内循环和指标函数。图中以当前已评价种群训练两种模型，以整个存档做环境选择。探索与指标模式分别使用加权和未加权关系聚合。
2. 解析图的八个 S 值与 L 标签显式保存；四个进度点的混合分数按公式计算，稳定降序取前 2 个。验证后半程二值正标签解的分数严格高于二值非正标签解。
3. 当前分组 CSV 共 48,000 行，包含新增问题和多种真值。按原五问题、最终种群成员真值、top25、三个视图筛选，得到 3,000 行、1,000 个配对运行–阶段，每阶段 250 个运行。四阶段 × 三视图均值均与正文表格吻合（容差 0.000006）。其余行按协议身份排除，不按效果方向筛选。
4. 候选 CSV 共 200 条运行，全部 M=20，全部完成 FE=300；四问题 × 五策略 × 十次运行完整。采用全部记录。V1–V4 的 5,280 个非末尾裁剪批次全部为 6；末尾裁剪批次全部为 2。
5. 两张实验图使用运行级重采样。阶段检查点先在运行内汇总，固定配置内重采样运行；同一条轨迹的三个视图一起重采样。候选最终 IGD 在问题与种子内配对，使用正数比值的对数进行 bootstrap，再反变换。重复次数 4,000，随机种子固定。图中的横向抖动仅改变点的位置，不修改纵轴数值。
6. 性能总览直接重读 10/20 目标的两本当前工作簿，保留每本全部 16 问题、7 对手，共 224 个比值；指标均值和标准差均输出到源 CSV。重复源单元格仍标记。颜色范围覆盖全部比值，不截断极值，不新造统计显著性。

## 图面逐项检查

| 图/面板 | 独立作用 | 汇总与区间 | 实际检查 |
|---|---|---|---|
| 1 | 两个贡献与真实评价闭环 | 代码流程，无统计区间 | 箭头、代表解支路、模型数据来源、标题、回路标注通过 |
| 2a | 连续方向 PBI 几何 | 解析例子 | 投影点、d1/d2、平移坐标、公式通过 |
| 2b | 代表解相对阈值 | 固定 delta=1 的单区域解析例子 | 边界经过代表解；正负标签区域与公式一致 |
| 2c | 权重与组间排序 | 解析线条 | 线型区分标签；C/E/G 与 A 标注、说明文字位置通过 |
| 2d | 排序转化为正组 | 八解、四阶段、每阶段取二 | 数值和边框成员验证通过；没有以颜色单独编码成员 |
| 3 | 两条候选选择路径 | 代码流程，无统计区间 | 模式先抽取；两支各自生成池；批次约束和图注通过 |
| 4a | 各阶段保留率 | 配置/运行等权均值，95% bootstrap CI | 三组同尺度、误差线、阶段边界与图注通过 |
| 4b | 同轨迹上的视图差值 | 配对差值均值，95% bootstrap CI | 零参考线、正负方向、percentage points 单位通过 |
| 5a–d | 批次大小、分散性、接纳与增益 | 所有运行散点、均值、95% bootstrap CI | 零起点、散点未截断、标记形状、指标单位与阶段规则通过 |
| 5e | 最终 IGD 的配对比较 | 每问题十个比值、几何均值、95% bootstrap CI | 对数轴、1 参考线、逐问题分组、全部策略、图例间距通过 |
| S1a–b | 主结果的逐问题总览 | 已报告均值之比，无独立运行区间 | 全部单元格、统一色标、文字对比度、重复数值标记、脚注通过 |

## 导出与论文

- 六图宽度均为 180 mm，输出 PDF/SVG/600 dpi PNG；所有统计文字和线条保留为矢量内容。分数矩阵与性能热图的单元格也使用矢量矩形。
- PDF 实际最小字形：图 1 为 5.60 pt，图 2 为 5.60 pt，图 3 为 5.28 pt，图 4 为 5.60 pt，图 5 为 7.50 pt，补充图为 5.60 pt。包括上下标和重音符号的实际文字片段检查。
- 静态预检为 16 PASS、4 WARN、0 FAIL。四个提醒分别为数学小字形、未输出 TIFF、物理宽度的静态解析、随机数据标记。数学小字形已通过 PDF 实测；本次采用矢量 PDF/SVG，PNG 仅作预览；实际宽度来自 PDF/画布；随机数用于 bootstrap 与横向抖动，图 2 是明确标注的解析例子。没有未处理的失败项。
- 插图后论文共 16 页，五张图及图注分别位于第 3、5、6、13、14 页；已在整篇稿件中逐页渲染并检查。双栏浮动布局可能使图晚于首次引用，这是当前稿件的浮动排版结果。
- 完成最终两次 LaTeX 编译，无未定义引用、重复标签、LaTeX 警告或 overfull。原有章节中的 TODO 属于原稿内容。
- 本次修改的 TeX 范围通过空白检查。仓库全局检查中，原有中文版末尾的空白行提示属于任务开始前的修改，不在本次改动范围内。

本次交付完成了图形设计、数值复核和可视化排版检查；具体期刊的投稿尺寸、页数与格式以最终目标期刊指南为准。
